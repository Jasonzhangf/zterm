#!/bin/sh
set -eu

branch=$(git symbolic-ref --quiet --short HEAD || true)
case "$branch" in
  main|master)
    echo "拒绝在 $branch 提交：请使用 clean owner worktree。" >&2
    exit 1
    ;;
esac

node ./scripts/run-gate.mjs --severity block --name verify:local -- pnpm run verify:local
