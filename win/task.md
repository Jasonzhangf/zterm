# zterm Windows Task Board

## Current

- Close out `daemon.windows_wezterm_backend` before starting the Windows desktop shell.

## Next

1. Pass local unit tests and mock daemon protocol smoke.
2. Pass real Windows WezTerm remote smoke and input smoke.
3. Keep Ctrl+C / console-control semantics explicit until solved.
4. Add a `windows.desktop_shell` registry entry and test design before implementing the shell.

## Not Started

- Windows desktop app package.
- Windows window/menu/platform integration.
- Packaged Windows app smoke.
