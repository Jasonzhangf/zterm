# zterm Android Function Map

This map is the human-readable index for `docs/feature-registry.json`.

Before changing a feature, locate its `feature_id`, read its owner paths, and run its required gates. If no row exists, add the feature to the registry and gate it before touching behavior.

| feature_id | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| `daemon.source_adapter` | `src/server/terminal-source-adapter.ts` | shared terminal source adapter contract for tmux/Herdr/WezTerm backends and mirror capture; kind assertion, source snapshot/session/input/resize/rename/close/read-path contracts, and no mirror revision or client policy truth | `src/server/terminal-source-adapter.test.ts`; `module-import-graph-truth.test.ts`; `feature-registry-truth.test.ts`; `function-wiki-truth.test.ts` |
| `daemon.mirror_writer` | `src/server/terminal-mirror-capture.ts` | validated terminal source snapshot capture, source-neutral canonicalization, and authoritative mirror snapshot commit writes in `terminal-mirror-capture.ts`; no mirror revision, subscriber publication, backend session truth, renderer, or client policy | `src/server/terminal-mirror-capture.test.ts`; `src/server/terminal-mirror-capture-continuity.test.ts`; `src/server/server.mirror-capture-truth.test.ts`; `src/server/terminal-mirror-writer-ownership.test.ts`; `module-import-graph-truth.test.ts`; `feature-registry-truth.test.ts`; `function-wiki-truth.test.ts` |
| `shared.node_contract` | `packages/shared/src/terminal/node-contract.ts` | runtime node identity, lifecycle, disposal, subscription, and typed adjacent node contracts; no session, transport, mirror, buffer, renderer, UI, backend, or routing policy truth | `packages/shared/src/terminal/node-contract.test.ts`; `src/lib/shared-contract-ownership.test.ts`; `module-registry-truth.test.ts`; `resource-registry-truth.test.ts`; `feature-registry-truth.test.ts` |
| `shared.debug_contract` | `packages/shared/src/terminal/debug-contract.ts` | versioned debug snapshots, debug events, filters, sensitivity classes, bounded stores, snapshot producer registry, and expiring debug permissions; no business mutations or terminal body truth | `packages/shared/src/terminal/debug-contract.test.ts`; `src/lib/shared-contract-ownership.test.ts`; `client-debug-snapshot.test.ts`; `runtime-debug-store.test.ts`; `terminal-debug-runtime.test.ts`; `resource-registry-truth.test.ts`; `feature-registry-truth.test.ts` |
| `client.observability` | `src/lib/client-debug-snapshot.ts`; `src/lib/runtime-debug-http-exporter.ts` | bounded client debug snapshot/event/export truth through the shared debug contract; no session transport, terminal channel, mirror, sparse buffer, renderer, or UI truth | `src/lib/client-debug-snapshot.test.ts`; `src/lib/runtime-debug-http-exporter.test.ts`; `packages/shared/src/terminal/debug-contract.test.ts`; `src/lib/shared-contract-ownership.test.ts`; `resource-registry-truth.test.ts` |
| `daemon.observability` | `src/server/runtime-debug-store.ts`; `src/server/terminal-debug-runtime.ts` | bounded daemon debug event/snapshot/permission truth through the shared debug contract; no active session, transport, mirror, sparse buffer, renderer, or UI truth | `src/server/runtime-debug-store.test.ts`; `src/server/terminal-debug-runtime.test.ts`; `src/server/server.debug-truth.test.ts`; `packages/shared/src/terminal/debug-contract.test.ts`; `src/lib/shared-contract-ownership.test.ts`; `resource-registry-truth.test.ts` |
| `daemon.herdr_backend` | `src/server/herdr-process-transport.ts`; `src/server/herdr-backend.ts`; `src/server/herdr-backend-runtime.ts`; `src/server/herdr-frame-canonicalizer.ts`; `src/server/terminal-backend-selection.ts`; `src/server/terminal-control-runtime.ts`; `src/server/terminal-mirror-runtime.ts`; `src/server/terminal-message-runtime.ts`; `src/server/terminal-message-control-runtime.ts`; `src/server/wezterm-backend.ts`; `src/components/tmux/TmuxSessionPickerSheet.tsx` (creation-intent projection) | official Herdr single terminal-session JSONL observe/control attachment, typed input/resize/release/reconnect, stateful VT canonicalization, zterm-owned revision, adapter-owned official `pane read recent` tail history with monotonic daemon `sourceEndIndex`, canonical live visible tail overlay only at host bottom, explicit viewport-relative cursor gap while host-scrolled, and `onLiveActivity` mirror wake; Herdr `terminal_id/pane_id` remain physical attachment identity only and shared `workspace` is the fixed synthetic `herdr-single-session` label; no Herdr workspace/layout truth; official 0.8.0 history cap 1000 is an explicit capability gap; daemon-owned catalog unions tmux and configured running Herdr names, exact-name backend resolution fails on zero or multiple matches; normal client list/open/mux/rename/kill requests are backend-opaque; new-session UI selection is a creation-only `tmux | Herdr` adapter intent; file transfer, file listing, directory creation, file download, file upload, paste-image fallback, and remote screenshot are unsupported and fail with `herdr_file_transfer_unsupported` without a tmux write | `src/server/herdr-backend.test.ts`; `src/server/herdr-backend-runtime.test.ts`; `src/server/herdr-frame-canonicalizer.test.ts`; `src/server/herdr-process-transport.test.ts`; `src/server/herdr-side-channel-truth.test.ts`; `src/server/terminal-backend-selection.test.ts`; `src/server/terminal-control-runtime.input-queue.test.ts`; `src/components/tmux/TmuxSessionPickerSheet.test.tsx`; `src/lib/tmux-sessions.test.ts`; resource/module/mainline registry gates |
| `terminal.copy_mode` | `src/pages/useTerminalPageCopyRuntime.ts`; `native/android/app/src/main/java/com/zterm/android/MainActivity.java` | copy runtime, TerminalPage wiring, `TerminalView` pointer/touch long-press ownership, `VisibleRow` selection/menu guard, header state, Android native WebView long-clickable gate | `src/pages/useTerminalPageCopyRuntime.test.tsx`; `system-copy-state-machine.test.tsx`; `system-copy-longpress-regression.test.tsx`; `TerminalView.selection-guard.test.tsx`; `VisibleRow.selection.test.tsx` |
| `terminal.quickbar` | `src/components/terminal/TerminalQuickBar.tsx`; `src/hooks/useShortcutActionStorage.ts`; `src/lib/terminal-shortcut-actions.ts`; `packages/shared/src/shortcuts/terminal-shortcut-composer.ts` | quickbar UI, persisted built-in/custom shortcut order, terminal page shell wiring, capability-flag pass-through from the registered `terminal.shell_actions` projection, shared shortcut composer encoding/decoding, workspace/layout profile | `TerminalQuickBar.test.tsx`; `terminal-page-status-helpers.test.ts`; `useShortcutActionStorage.test.tsx`; `packages/shared/src/shortcuts/terminal-shortcut-composer.test.ts`; `TerminalPage.real-quickbar-split.test.tsx` |
| `terminal.keyboard_ime` | `src/App.tsx` (native content-bound surface only); `src/pages/terminal-keyboard-lift.ts`; `src/pages/TerminalPage.tsx`; `src/pages/TerminalPageStageShell.tsx`; `src/components/terminal/TerminalQuickBar.tsx`; `src/lib/android-ime-anchor-truth.test.ts`; `native/android/app/src/main/java/com/zterm/android/ImeAnchorPlugin.java`; `native/android/app/src/main/java/com/zterm/android/ImeAnchorInputLogic.java` | app surface coverage, cold-start/foreground viewport remeasurement, IME lift, keyboard visibility, native anchor explicit show/disable policy, viewport safe-area, quickbar focus handoff, committed-text normalization consumed from `client.input_normalizer`, stage shell visual lift even when quickbar editor owns DOM focus, QuickBar measured chrome height under external IME lift, listener cleanup, adjustPan fallback to reported keyboard inset when viewport metrics do not expose IME occlusion | `App.dynamic-refresh.test.tsx`; `terminal-keyboard-lift.test.ts`; `android-ime-anchor-truth.test.ts`; `TerminalPage.android-ime.test.tsx`; `ImeAnchorInputLogicTest`; `ImeAnchorHardwareKeyMappingTest`; `TerminalPage.lifecycle-cleanup.test.tsx`; `TerminalPageStageShell.pane-stage.test.tsx`; `TerminalQuickBar.test.tsx` |
| `client.input_normalizer` | `src/lib/terminal-input-normalization.ts`; `src/lib/terminal-input-normalization.test.ts`; `src/lib/input-normalizer-ownership.test.ts` | unique Android committed-text normalization owner for IME/hardware/paste/quick-action terminal text; preserves CJK/emoji/special symbols, converts terminal-oriented full-width ASCII/punctuation and ideographic space, and turns IME line breaks into text separators; no transport retry, session, backend, or mirror truth | `src/lib/terminal-input-normalization.test.ts`; `src/lib/input-normalizer-ownership.test.ts`; `test:feature-registry` |
| `terminal.schedule` | `src/server/schedule-*.ts` | daemon schedule store/engine/dispatch and page editor wiring | schedule store/engine/dispatch tests; `server.schedule-truth.test.ts` |
| `terminal.remote_screenshot` | `src/lib/remote-screenshot-runtime.ts`; `src/server/terminal-file-transfer-list-runtime.ts`; `src/server/remote-screenshot-daemon.ts`; daemon control runtime | screenshot request aggregation, optional remote-window target manifest request, file-transfer bridge, page preview/save, Android local save, daemon native `capture-screen` window-id/rect capture | runtime/page screenshot tests; `src/server/terminal-file-transfer-list-runtime.test.ts`; file-transfer truth gate |
| `desktop.remote_window_stream` | `src/server/remote-window-stream-daemon.ts`; `packages/shared/src/connection/protocol.ts`; `src/components/terminal/RemoteWindowOverlay.tsx`; `src/components/terminal/WindowGroupLayout.tsx`; `src/lib/remote-window-overlay-runtime.ts`; `src/lib/remote-window-touch-action-runtime.ts`; `src/lib/remote-window-message-runtime.ts`; `src/lib/remote-window-receiver-runtime.ts`; `src/contexts/session-context-remote-window-runtime.ts`; `docs/decisions/2026-07-19-remote-window-stream-truth.md`; `docs/decisions/2026-07-29-remote-window-canvas-pipeline.md` (`binding pending` only for remaining live Android/iTerm2 proof) | remote-window target catalog protocol, daemon/native macOS app-window catalog, iTerm2 pane catalog, split-tree flatten, coordinate manifest with daemon display bounds, tmux reverse lookup, explicit catalog errors, Android picker/floating/fullscreen overlay projection, collapsed same-app picker rows plus active video-layer primary-plus-children sibling window switcher inside the same locked video container with three-child top rail above the primary video and horizontal scrolling for additional children, TerminalPage-measured QuickBar + IME chrome avoidance for the whole locked container, toolbar-owned floating drag, edge-resized floating preview projection, remote-window screenshot intent over the terminal.remote_screenshot transfer owner without focus, daemon-wide target catalog cache keyed by daemon identity with 60-second expiry, target catalog request/response aggregation over the existing session transport, stream start/stop/input/window-resize intents, client touch/action classification separated from overlay projection, route-derived WebRTC ICE/media negotiation, daemon ScreenCaptureKit frame source with low-latency queue depth, Android receiver attach and WebRTC stats sampling, rendered video surface projection, fullscreen aspect-fit drawing with default remote target resize fill request on fullscreen entry, zoom/pan projection with no minimap overlay and no shrink below fit, floating-preview low bitrate plus desktop-area-proportional video quality projection with 30 FPS default / 60 FPS fullscreen ceiling and stats-driven downgrade/restore, generic bring-to-focus OS input injection, and exact stream cleanup, plus daemon-owned raw/layout/encode/focus canvas resources for app-group reflow and declared dual-stream focus mode when ROI is unavailable | `src/lib/remote-window-message-runtime.test.ts`; `src/lib/remote-window-overlay-runtime.test.ts`; `src/lib/remote-window-touch-action-runtime.test.ts`; `src/lib/remote-window-touch-action-sop.test.ts`; `src/lib/remote-window-receiver-runtime.test.ts`; `src/components/terminal/WindowGroupLayout.test.tsx`; `src/components/terminal/RemoteWindowOverlay.test.tsx`; `src/pages/TerminalPage.remote-window-overlay.test.tsx`; `src/contexts/session-context-remote-window-runtime.test.ts`; `src/contexts/session-context-socket-message-runtime.test.ts`; `src/server/remote-window-stream-daemon.test.ts`; `src/server/terminal-message-runtime.test.ts`; `src/lib/remote-window-video-quality.test.ts`; `docs/testing/remote-window-stream-test-design.md`; `docs/testing/remote-window-canvas-pipeline-test-design.md`; resource/function/mainline gates |
| `terminal.open_tabs` | `src/hooks/useOpenTabRuntime.ts`; `src/hooks/useOpenTabLifecycleEffects.ts`; `src/lib/open-tab-persistence.ts` | in-process open-tab projection, explicit open/close policy, foreground/resume native listener lifecycle, and one-way legacy persisted-tab cleanup; no cold restore, tab-list persistence, or render-driven Capacitor listener churn | `src/hooks/useOpenTabLifecycleEffects.test.tsx`; `open-tab-history-truth.test.ts`; persistence/intent/runtime tests; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `terminal.transport_lifecycle` | `packages/shared/src/connection/protocol.ts`; `src/contexts/session-context-transport-runtime.ts`; `src/contexts/session-context-session-runtime.ts`; `src/contexts/session-context-infra-runtime.ts`; `src/contexts/session-context-infra-facade-runtime.ts`; `src/contexts/session-context-tmux-management-runtime.ts`; `src/contexts/session-context-socket-runtime.ts`; `src/contexts/session-context-socket-message-runtime.ts`; `src/contexts/session-context-lifecycle.ts#selectNextPassiveVisibleRefreshCandidate`; `src/contexts/session-transport-open-helpers.ts`; `src/contexts/session-reconnect-helpers.ts`; `src/lib/session-transport-runtime.ts`; `src/lib/terminal-channel-mux-runtime.ts`; `src/lib/tmux-sessions.ts`; `src/pages/TerminalPage.tsx` (status projection only); `src/server/terminal-daemon-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/server/terminal-channel-mux-runtime.ts`; `src/server/rtc-bridge.ts`; `src/server/terminal-mirror-runtime.ts`; `src/server/terminal-transport-runtime.ts`; `src/server/terminal-runtime-types.ts`; `native/android/app/src/main/java/com/zterm/android/MainActivity.java`; `native/android/app/src/main/java/com/zterm/android/BackgroundService.java`; `native/android/app/src/main/AndroidManifest.xml` | SessionContext transport/session lifecycle, route-aware traversal socket factory, daemon-target physical transport reuse, mux capability handshake, terminal channel open/close/message demux through `TerminalChannelMuxStore`, target-level tmux management over existing `mux-target-message` request/response truth, `src/contexts/session-context-transport-orchestration-runtime.ts#resolveMuxChannelClosedWithControlStatusRuntime` as the closed data-channel control-status decision owner, metadata-only transport diagnostics projection including immediate waiting-generation control-directory state, physical socket heartbeat health, daemon stale bound channel/transport cleanup, channel body-subscription intent, socket receive, passive visible-pane freshness round-robin scheduler, transport reuse planner, semantic session reuse across route-identity upgrades, target-scoped tmux management transport reuse with FIFO serialization, daemon WebRTC bridge ICE policy consumption, daemon channel subscriber/body eligibility, per-channel bounded pending-latest backpressure, unified send accounting, flat non-highlighted portrait top status projection with current activity or actual route plus up/down rates and manual Auto/direct/WebRTC route action wiring through the session-open owner, top-level fixed UI projection for network/reconnect status that must not change terminal or remote-window layout size, and Android persistent-background execution support: Activity backgrounding must not pause WebView timers; while a retained Session exists, the foreground service may own one process-level partial WakeLock plus its notification beyond the five-minute body-demand handoff deadline, but it must never own socket/session/route truth, trigger reconnect, or request battery-optimization bypass; foreground return or closing the final retained Session is the normal stop owner; legacy `wsUrl` override is allowed only for non-Relay/non-route-aware hosts and must not bypass route truth; tmux management over a separate `tmux-sessions.ts` transport is allowed only when no matching non-closed open Session/target exists, carries no result cache, and must not hide physical/protocol failure or timeout; daemon types must not retain client active/visible/viewport policy | transport lifecycle/runtime tests; mux protocol/channel tests; `terminal-channel-mux-runtime.test.ts`; `session-context-tmux-management-runtime.test.ts`; `session-context-infra-runtime.test.ts`; `tmux-sessions.test.ts`; `session-context-socket-runtime.test.ts`; `SessionContext.ws-refresh.test.tsx`; `session-context-session-runtime.test.ts`; `session-context-activity-runtime.test.ts`; `session-context-lifecycle.test.tsx`; `session-sync-helpers.test.ts`; `server.daemon-runtime-truth.test.ts`; `server.transport-lifecycle-truth.test.ts`; `terminal-daemon-runtime.test.ts`; `terminal-transport-runtime.test.ts`; `terminal-mirror-runtime.test.ts`; `terminal-runtime.detached-session.test.ts`; `terminal-message-runtime.test.ts`; `rtc-bridge.test.ts`; `src/pages/TerminalPage.network-banner.test.tsx`; `src/pages/TerminalPage.session-drawer.test.tsx`; `src/lib/android-power-policy.test.ts` |
| `terminal.daemon_input` | `src/server/daemon-input-queue-runtime.ts#createDaemonInputQueueRuntime`; `src/server/daemon-input-queue-runtime.ts#handleInputMessage`; `src/server/daemon-input-queue-runtime.ts#enqueueLiveMirrorInput`; `src/server/terminal-reliable-input-ack.ts`; `src/server/terminal-message-runtime.ts`; `src/server/terminal-control-runtime.ts#buildExactTmuxSessionTarget`; `src/server/terminal-control-runtime.ts#buildExactTmuxPaneTarget`; `src/server/terminal-control-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/lib/reliable-input/reliable-input-queue.ts#enqueueReliableInputChunks`; `src/lib/reliable-input/reliable-input-queue.ts#handleTerminalInputAck`; `src/contexts/session-context-input-runtime.ts#sendInputThroughSessionTransport`; `packages/shared/src/connection/protocol.ts` | `resource.platform_input_channel -> resource.client_reliable_input_queue -> resource.session_transport` client seq/one-in-flight/ACK/retry control, daemon input receive/drop/write queue in `daemon-input-queue-runtime.ts`, seq/ack dedupe in `terminal-reliable-input-ack.ts`, exact tmux target normalization split between session target `=<session>` for session/window commands and deterministic `=<session>:.{top-left}` target for pane/capture/input truth, reliable input capability advertisement, retryable nack preservation for stale transport/session-required, non-retryable invalid/oversize rejection, long input frame and tmux write chunking, transport drain, bridge attach/input/message lane scheduling, per-transport input lane bypass of older input work while attach barriers still hold, live-activity scheduling; `session-context-input-runtime.ts` remains a thin bridge and re-export shell | `src/server/daemon-input-queue-runtime.test.ts`; `session-context-input-runtime.test.ts`; `src/lib/reliable-input/reliable-input-queue.test.ts`; `src/lib/reliable-input/reliable-input-ownership.test.ts`; `terminal-bridge-runtime.test.ts`; `terminal-message-runtime.test.ts`; `terminal-control-runtime.input-queue.test.ts`; `terminal-mirror-runtime.test.ts`; scheduler/control/bridge truth gates |
| `terminal.buffer_render` | `packages/shared/src/connection/types.ts`; `src/server/mirror-line-canonicalizer.ts#canonicalizeCapturedMirrorLines`; `src/server/terminal-mirror-runtime.ts`; `src/lib/wire-ingress/buffer-wire-normalize.ts#normalizeIncomingBufferPayload`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly.ts#assembleBufferSyncFrameChunk`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly.ts#expireBufferSyncFrameAssembly`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly-state.ts#clearPendingBufferSyncFrameAssembly`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly-state.ts#resetBufferSyncFrameAssemblyEpoch`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly-state.ts#hasBufferFrameRepairDispatch`; `src/lib/buffer-frame-assembly/session-buffer-frame-assembly-state.ts#recordBufferFrameRepairDispatch`; `src/contexts/session-context-buffer-runtime.ts#applyIncomingBufferSyncRuntime`; `src/contexts/session-context-buffer-runtime.ts#applyResolvedBufferSyncPayloadRuntime`; `src/lib/session-buffer-store.ts#createSessionBufferStore`; `src/lib/session-render-gate.ts`; `src/lib/session-render-buffer-store.ts`; `src/components/TerminalView.tsx`; `src/components/terminal/VisibleRow.tsx`; `src/components/terminal/TerminalPreviewRow.tsx`; `src/components/useMirrorFixedZoomPan.ts`; `packages/shared/src/terminal/cell-render.ts`; `packages/shared/src/terminal/theme.ts`; `src/lib/terminal-follow-scroll-runtime.ts` | mirror writer commits validated source snapshots; mirror store owns canonical revision and runtime scheduling; authoritative range patch and subscriber publication flow through the registered buffer edges, ANSI rendition continuation across tmux physical soft-wrap rows, mirror revision broadcast, oversized body-refresh chunking with authoritative frame identity, unique wire normalization that preserves frame identity, shared protocol bounds for span/chunks/retained bytes/lifetime, bounded client frame assembly with exact continuous coverage, bounded per-session repair dispatch ledger retained across tab/transport resets, reset only on authoritative daemon revision epoch reset, and deleted with the local session, explicit per-session frame error truth plus at most one repair attempt per retained revision epoch, client sparse buffer owns absolute-row immutable truth, one resolved sparse apply/render publication without recursive ingress, next-RAF render gate, renderer publication store, TerminalView follow/reading scroll state machine, client.dom_renderer owns immutable render snapshot to DOM projection through TerminalView/VisibleRow/TerminalPreviewRow/zoom-pan/cell-render/theme, client.terminal_shell owns stage shell/status/quickbar/copy/keyboard-lift projection, and source-to-DOM black-box tests | canonicalizer soft-wrap SGR positive/reset-negative tests, frame assembly positive/negative limit/expiry/ledger/epoch tests, socket normalization, capture/oracle, mirror runtime/backpressure, sparse store isolation, buffer runtime, render gate/store, follow-scroll runtime, TUI content, TerminalView dynamic refresh, TerminalPageStageShell pane stage, useTerminalPageShellActionsRuntime, and multi-pane tests |
| `terminal.workspace_panes` | `src/hooks/useTerminalWorkspace.ts` | pane split/layout mutation owner; persistence validates and stores explicit pane truth, while TerminalPage/Header/StageShell only project numbered empty panes, scoped session-picker intent, and explicit pane context actions (`更改 Pn Session`, `移到 Pn`) through the workspace owner | workspace pane tests; `workspace-persistence.test.ts`; `TerminalPage.render-scope.test.tsx`; `TerminalHeader.test.tsx`; `TerminalPageStageShell.pane-stage.test.tsx` |
| `terminal.session_group_layout` | `src/lib/terminal-layout-profile.ts`; `src/lib/session-group-viewport.ts`; `src/pages/TerminalPage.tsx#resolveTerminalSessionGroupActiveSessionProjection`; `src/pages/TerminalPageStageShell.tsx`; `packages/shared/src/connection/bridge-settings.ts` | app-layer layout/projection framework for existing session pane containers; Phase 1 keeps phone baseline, Phase 2 changes layout modes, Phase 3 adds cross-screen projection/virtualization; `session-group-viewport.ts` owns fixed-slot to boundary-visible viewport projection; `TerminalPage` owns active-session-to-slot synchronization before `StageShell` render so active id and body source cannot split; `terminalSessionGroupLayoutMode` stores the user axis preference while aspect-ratio policy remains the default truth | `terminal-layout-profile.test.ts`; `bridge-settings.test.ts`; `SettingsPage.theme.test.tsx`; `TerminalPage.session-content-identity.test.tsx`; `TerminalPage.session-drawer.test.tsx`; `TerminalPageStageShell.pane-stage.test.tsx`; `useTerminalWorkspace.test.tsx`; `docs/testing/terminal-session-group-layout-test-design.md` |
| `terminal.interaction_runtime` | `src/pages/useTerminalPageInteractionRuntime.ts` | active pane session routing, pane attach intent, swipe/tab isolation | `TerminalPage.tab-isolation.test.tsx`; `TerminalPage.multi-pane-decouple.test.tsx` |
| `terminal.session_drawer` | `src/components/terminal/TerminalSessionDrawer.tsx`; `src/pages/TerminalPage.tsx` (drawerServerIdentityAliases/drawerSessions useMemo); `src/lib/server-identity.ts`; `src/lib/traversal-relay-devices.ts`; `src/App.tsx` (saved/Home alias wiring only); `src/lib/plugin-session-drawer/session-drawer-contract.ts`; `src/lib/plugin-host/session-drawer-ui-plugin.tsx` | drawer UI, content-sized scrollable session list/footer layout, canonical hostKey/hostLabel injection, saved-server identity aliases, and online Relay-directory direct-endpoint aliases into one daemon host rail; rtc-only Relay catalogs supply rows but never infer daemon identity, so direct/stale identities require an exact endpoint or saved/Home endpoint-to-online-daemon binding; active online daemon ids are preserved; disconnected/stale Relay daemon records are filtered from host rails and target lookup; unresolved identities remain separate; Relay-owned catalog rows, including already-open stale direct rows, route through the session-open owner with route-aware target truth instead of raw switch-only bypass; TerminalPage memo identity includes Relay device endpoint/session-catalog inputs plus saved/Home alias inputs; opened-first unopened-sorted ordering, multi-host rail, swipe-close gesture, and row-selection press ownership that rejects the opening gesture's synthetic click; App/TerminalPage render drawer only through the plugin-provided `terminal.session-drawer` typed slot | `TerminalSessionDrawer.test.tsx`; `TerminalPage.session-drawer.test.tsx`; `TerminalPage.session-preview.test.tsx`; `TerminalPage.tab-isolation.test.tsx`; `plugin-host-runtime.test.ts`; `App.dynamic-refresh.test.tsx`; `traversal-relay-devices.test.ts`; `server-identity.test.ts`; `docs/testing/terminal-session-drawer-gesture-test-design.md` |
| `terminal.session_preview` | `src/lib/session-preview-selection.ts`; `src/lib/session-preview-gesture.ts`; `src/components/terminal/WindowGroupLayout.tsx`; `src/components/terminal/TerminalPreviewGrid.tsx`; `src/pages/TerminalPageStageShell.tsx`; `src/pages/TerminalPage.tsx`; `src/contexts/session-context-buffer-runtime.ts` (accepted live preview tail bootstrap only) | ordered 1-6 open-session selection, remote drawer catalog materialization through the existing session-open owner before selection persistence, shared primary-plus-children window-group layout projection, in-place long-press replacement, preview-target removal, right-edge gesture admission, temporary preview live-set projection, bounded one-screen tail bootstrap for selected passive previews without an interactive viewport, read-only shared-renderer grid with local scroll/pan, child-tile promotion without activation, primary-tile activation intent, and cancel-to-entry projection | `src/components/terminal/WindowGroupLayout.test.tsx`; `session-preview-selection.test.ts`; `session-preview-gesture.test.ts`; `TerminalPreviewGrid.test.tsx`; `TerminalPreviewGrid.render-truth.test.tsx`; `session-context-buffer-runtime.test.ts`; `TerminalPage.session-preview.test.tsx`; `scripts/session-preview-source-dom-gate.tsx`; `docs/testing/terminal-session-preview-test-design.md` |
| `terminal.shell_actions` | `src/pages/useTerminalPageShellActionsRuntime.ts`; `src/pages/TerminalPageStageShell.tsx`; `src/pages/terminal-page-shell-ui.tsx`; `src/pages/terminal-page-status-helpers.ts`; `src/pages/TerminalConnectionStatusStrip.tsx`; `src/pages/TerminalPageQuickBarAssembly.tsx`; `src/pages/terminal-page-debug-helpers.ts`; `src/pages/terminal-page-render-keys.ts`; `src/pages/terminal-page-quickbar-adapters.ts`; `src/pages/terminal-keyboard-lift.ts`; `src/pages/TerminalPageCopyMenu.tsx`; `src/pages/useTerminalPageCopyRuntime.ts`; `src/pages/terminal-copy-selection.ts`; `src/lib/terminal-shell-skin.ts` | quick picker, tab manager scope, viewport mode routing, terminal quickbar capability projection owner, and `client.terminal_shell` stage/status/quickbar/copy/keyboard-lift shell projection | `useTerminalPageShellActionsRuntime.test.tsx`; `terminal-page-status-helpers.test.ts`; `TerminalPageStageShell.pane-stage.test.tsx`; `terminal-shell-skin.test.ts` |
| `connections.history_projection` | `src/hooks/useSessionHistoryStorage.ts`; `packages/shared/src/react/use-host-storage.ts`; `src/components/settings/ConnectionConfigSection.tsx#buildConnectionConfigEntries` | saved direct/Tailscale connection truth and bridge server presets remain independent from Relay; session-group history retains normalized Relay endpoint candidates so a later drawer open keeps the selected daemon route; `client.connection_home` owns the unified Settings connection projection and server preset configuration; Home projects one row per server and must not display/manage Session groups | open-tab/history truth; `src/hooks/useSessionHistoryStorage.test.ts`; `src/pages/ConnectionsPage.test.tsx`; `src/pages/SettingsPage.relay-account.test.tsx`; `src/components/settings/connection-config-entries.test.ts`; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `connections.config_share` | `packages/shared/src/connection/connection-config-share.ts`; `src/components/tmux/TmuxSessionPickerSheet.tsx`; `src/pages/ConnectionPropertiesPage.tsx` | canonical connection share payload/link parser; the real `+` add-flow sheet is the primary import/share/QR/scan UI projection; Connection Properties may also project this shared link/QR; import must go through `useHostStorage.upsertHost`; Android native scheme only opens the same parser | `packages/shared/src/connection/connection-config-share.test.ts`; `src/components/tmux/TmuxSessionPickerSheet.test.tsx`; `src/lib/connection-config-share-android-truth.test.ts`; `docs/testing/connection-config-share-test-design.md` |
| `settings.config_transfer` | `src/hooks/useConfigExport.ts`; `src/hooks/useAppUpdate.ts`; `src/lib/app-update.ts`; `src/lib/app-update-runtime.ts`; `src/lib/app-update-relay-manifest.ts`; `src/pages/SettingsPage.tsx`; `src/components/settings/AppUpdateSection.tsx`; `src/App.tsx`; `packages/shared/src/interaction/projection.ts` | settings config export/import writes and reads the canonical config file path, returns explicit success/error results, excludes session/runtime state, and App surfaces those results to the user instead of silent console-only success; app-update runtime is the single owner for manifest URL source (`user-saved` / `relay-injected` / `server-connected`), so Relay-derived update URLs can replace stale private daemon URLs while explicit user-saved URLs remain authoritative; Settings route candidates expose Relay public update route and saved/direct daemon routes without making direct/Tailscale the only check path | `src/hooks/useConfigExport.test.tsx`; `src/hooks/useAppUpdate.test.tsx`; `src/lib/app-update.test.ts`; `src/lib/app-update-relay-manifest.test.ts`; `src/lib/app-update-runtime.test.ts`; `src/pages/SettingsPage.theme.test.tsx`; `src/components/settings/AppUpdateSection.test.tsx`; `docs/testing/config-transfer-test-design.md` |
| `relay.account_directory` | `src/traversal-relay/store.ts`; `src/traversal-relay/server.ts`; `src/server/relay-client.ts`; `src/lib/relay-account-directory.ts`; `src/lib/traversal-relay-client.ts`; `scripts/prepare-relay-server-npm-package.mjs`; `scripts/verify-relay-server-package.mjs`; `packages/shared/src/connection/relay-directory.ts` | account-scoped machine / daemon / endpoint / tmux session directory; each successful login appends an independent token so one account can keep multiple client devices and daemon devices online without replacement; relay server stores and broadcasts directory/control facts and serves public route-scoped Android update assets from `ZTERM_TRAVERSAL_UPDATES_DIR` at `/relay/updates/*`; daemon publishes tmux truth snapshots plus account-scoped direct-route auth token; Android reads directory as remote-machine truth and refreshes `/api/auth/me` control payload before consuming device stream / TURN settings; authoritative HTTP 401/403 or device-stream 4001 invalidates only the rejected local client identity and atomically clears its persisted account, Relay settings, and confirmed directory instead of reconnecting with a dead token | `src/traversal-relay/store.test.ts`; `src/traversal-relay/server.test.ts`; `src/traversal-relay/relay-server-release-package.test.ts`; `src/server/relay-client.test.ts`; `src/lib/relay-account-directory.test.ts`; `src/lib/traversal-relay-client.test.ts`; `src/lib/relay-device-stream-runtime.test.ts`; `scripts/traversal-relay-local-smoke.ts`; `docs/testing/relay-account-directory-test-design.md`; `docs/testing/relay-auth-expiry-and-background-grace-test-design.md` |
| `relay.route_selection` | `src/lib/traversal/route-selector.ts`; `src/lib/traversal/route-health-cache.ts`; `src/lib/traversal/config.ts`; `src/lib/traversal/socket.ts`; `src/lib/traversal/route-diagnostics.ts`; `src/lib/bridge-settings.ts`; `src/pages/TerminalPageDebugOverlay.tsx`; `packages/shared/src/connection/bridge-settings.ts`; `docs/decisions/2026-07-21-auto-route-and-daemon-release-truth.md` | explicit route candidates, Auto priority `private LAN IPv4 -> Tailscale/direct websocket -> WebRTC UDP direct/hole-punch -> TURN Relay`, stale saved traversal priority ignored in Auto, direct IP endpoint facts still selectable when higher-priority route classes are unavailable, probe results, RTT / auth / freshness scoring, selected route diagnostics, selected ICE pair diagnostics, five-minute success/auth truth, one-second ordinary-failure circuit-breaker cooldown followed by fresh probe eligibility, and opened-route close/heartbeat/mux failure recording so the next physical generation can move routes without permanently blacklisting a recovered network | `src/lib/traversal/route-selector.test.ts`; `src/lib/traversal/route-health-cache.test.ts`; `src/lib/traversal/config.test.ts`; `src/lib/traversal/socket.test.ts`; `src/pages/TerminalPageDebugOverlay.test.tsx`; `docs/testing/relay-account-directory-test-design.md`; `docs/testing/session-transport-network-switch-test-design.md` |
| `relay.directory_ui` | `src/lib/home-connection-projection.ts`; `src/lib/relay-device-stream-runtime.ts`; `src/lib/relay-host-recent-connection.ts`; `src/hooks/useRelayDeviceStream.ts`; `src/lib/traversal-relay-devices.ts`; `src/App.tsx` (relay stream hook wiring only); `src/pages/ConnectionsPage.tsx`; `src/components/settings/ConnectionConfigSection.tsx`; `src/pages/SettingsPage.tsx`; `src/hooks/useTraversalRelayAccount.ts`; `src/components/tmux/TmuxSessionPickerSheet.tsx`; `src/hooks/useSessionOpenActions.ts` (Home row open intent only); `src/contexts/session-context-socket-message-runtime.ts` (server-confirmed daemonHostId evidence only) | fixed-domain relay account login belongs to Settings as optional route/sync assurance; Settings renders Relay login alongside bridge presets and online Relay daemons in one unified `ConnectionConfigSection` whose projection is built once by `buildConnectionConfigEntries` (reuse of canonical host merge and not a second local list); App relay stream bootstrap must refresh account control truth before opening the device stream, must not use stale stored TURN/WS settings, and must preserve route-bearing directory endpoint/session truth when later legacy devices snapshots carry only presence; Home projects connectable server rows from saved direct/Tailscale, bridge presets, active Sessions, and online relay directory daemon devices only; disconnected relay devices remain directory facts but are not connectable rows; stale connected records require a fresh daemon directory publish or a client-local recent successful `connected.daemonHostId` handshake, so unupgraded daemon processes remain visible without reviving never-connected half-open identities; same-daemon saved rows merge Relay route candidates without replacing saved direct identity; rows with `relay-rtc` expose one automatic connect/open intent and no separate Relay button; Home row tap opens last-entered remote session when still present, otherwise first live remote session, and creates generated tmux only when remote list is empty; logged-out Home still shows saved direct/Tailscale entries; Session management remains in the terminal drawer/picker | `src/App.relay-stream-lifecycle.test.tsx`; `src/lib/home-connection-projection.test.ts`; `src/lib/relay-host-recent-connection.test.ts`; `src/lib/traversal-relay-devices.test.ts`; `src/lib/traversal-relay-client.test.ts`; `src/contexts/session-context-socket-message-runtime.test.ts`; `src/hooks/useTraversalRelayAccount.test.tsx`; `src/hooks/useSessionOpenActions.test.tsx`; `src/pages/ConnectionsPage.test.tsx`; `src/pages/SettingsPage.relay-account.test.tsx`; `src/components/tmux/TmuxSessionPickerSheet.test.tsx`; `src/components/settings/connection-config-entries.test.ts`; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `daemon.file_transfer` | `contracts/file-transfer-throughput.json`; `src/components/terminal/FileTransferSheet.tsx#dispatchUploadChunk`; `#writeDownloadChunkBatch`; `src/lib/file-transfer-session-runtime.ts`; `src/lib/file-transfer-message-runtime.ts`; `src/lib/file-transfer-throughput-runtime.ts#sendBoundedFileUploadChunks`; `#writeFileTransferChunkBatches`; `src/plugins/StoragePermissionPlugin.ts`; `native/android/app/build.gradle`; `native/android/app/src/main/java/com/zterm/android/StoragePermissionPlugin.java#writeFileChunks`; `native/android/app/src/main/java/com/zterm/android/StorageFileReadLogic.java`; `native/android/app/src/main/java/com/zterm/android/StorageFileWriteLogic.java#writeChunks`; `src/server/terminal-file-transfer-binary-runtime.ts#handleFileUploadChunk`; `#handleFileUploadEnd`; `src/server/terminal-file-transfer-list-runtime.ts#handleFileDownloadRequest` | client file browser projection; one machine-readable throughput contract; one bounded upload-window owner; explicit upload/native-write callback bindings; native external-storage chunk read/batch-write boundary; daemon cumulative upload ACK and exact completion truth; daemon download wire emission | `FileTransferSheet.test.tsx`; `file-transfer-session-runtime.test.ts`; `file-transfer-throughput-contract.test.ts`; `file-transfer-throughput-runtime.test.ts`; `terminal-file-transfer-binary-runtime.test.ts`; `file-transfer-throughput-loopback.test.ts`; `StorageFileReadLogicTest`; `StorageFileWriteLogicTest`; canonical file-transfer truth gate; real transport/ADB SHA-256 gate pending |
| `daemon.runtime_entry` | `src/server/server.ts`; `src/server/daemon-config.ts`; `src/server/daemon-runtime-truth/*` | tmux/transport/mirror/control/schedule/relay/file-transfer orchestrator launched by `scripts/zterm-daemon.sh run`; must remain the only long-running daemon process | `src/server/server.daemon-runtime-truth.test.ts`; `src/server/server.core-support-truth.test.ts`; `src/server/server.http-truth.test.ts`; `src/server/server.debug-truth.test.ts` |
| `daemon.windows_wezterm_backend` | `src/server/wezterm-backend.ts`; `src/server/terminal-backend-selection.ts`; `src/server/terminal-control-runtime.ts`; `scripts/wezterm-backend-remote-smoke.ts`; `scripts/wezterm-backend-input-smoke.ts`; `scripts/wezterm-daemon-protocol-smoke.ts`; `scripts/wezterm-daemon-remote-protocol-smoke.ts` | Windows WezTerm mux adapter and daemon backend selection; parses `wezterm cli list`, consumes `get-text --escapes`, materializes ZTerm-owned absolute mirror snapshots, sends input only through `send-text --no-paste` stdin, closes sessions through the selected backend, and black-box compares real daemon source markers with `buffer-sync` output | `src/server/wezterm-backend.test.ts`; `src/server/wezterm-backend-runtime.test.ts`; `src/server/terminal-backend-selection.test.ts`; `src/server/terminal-control-runtime.input-queue.test.ts`; `scripts/wezterm-daemon-protocol-smoke.ts`; `scripts/wezterm-daemon-remote-protocol-smoke.ts`; `scripts/wezterm-backend-remote-smoke.ts`; `scripts/wezterm-backend-input-smoke.ts`; `docs/testing/windows-daemon-live-protocol-test-design.md` |
| `daemon.cli_shell` | `scripts/zterm-daemon.sh`; `scripts/windows/zterm-daemon.ps1`; `scripts/native/zterm-daemon.swift`; `scripts/install-global-daemon-cli.sh`; `scripts/prepare-global-daemon-release.sh#stage_runtime`; `#normalize_release_tree_metadata`; `#create_deterministic_archive`; `#verify_deterministic_archive`; `scripts/prepare-daemon-npm-package.mjs` | daemon CLI surface (`run/start/stop/restart/status/configure-relay/install-service/uninstall-service/service-status`), macOS launchd bootstrap/teardown, Windows Scheduled Task bootstrap/teardown, npm/global package layout, native screenshot binary, and deterministic daemon release compilation. Release compilation stages one runtime tree, normalizes metadata, writes sorted fixed-header archives, and rejects different output from an independently copied restrictive-umask tree before checksum publication | `src/lib/function-wiki-truth.test.ts`; `src/lib/resource-registry-truth.test.ts` |
| `daemon.cli_node` | `scripts/runtime-debug-remote.ts`; `scripts/collect-runtime-audit.ts`; `scripts/analyze-runtime-debug.ts`; `scripts/terminal-real-device-evidence.ts`; `scripts/traversal-relay-local-smoke.ts`; `scripts/daemon-mirror-close-loop.ts`; `scripts/weak-network-byte-proxy.ts`; `scripts/terminal-performance-probe.ts` | node CLI helpers that run outside the daemon process; weak-network helpers transparently shape bytes and measure protocol behavior without parsing or rewriting terminal payload | `src/lib/function-wiki-truth.test.ts`; `src/lib/weak-network-byte-proxy.test.ts`; `test:terminal:regression` |
| `daemon.support` | `contracts/app-version.json`; `scripts/app-version.mjs`; `scripts/app-version.d.mts`; `scripts/app-version.test.ts`; `scripts/build-android-debug.sh`; `scripts/prepare-update-bundle.mjs`; `scripts/verify-update-bundle.mjs`; `scripts/setup-android-java.sh`; `scripts/ensure-pnpm-install.sh`; `scripts/verify-release-assets.mjs`; `scripts/verify-relay-server-package.mjs`; `scripts/check-wterm-runtime-published.mjs`; `scripts/check-no-source-js-pollution.mjs` | release/install/build prerequisites and verifiers, including Android normal `N`, rollback `N.1`, and next-normal `N+1` version-code slot contract generation and verification | `scripts/app-version.test.ts`; `scripts/verify-update-bundle.mjs`; `src/lib/function-wiki-truth.test.ts`; `release:verify` |
| `windows.desktop_shell` | `win/electron/main.ts`; `win/electron/preload.cts`; `win/src/WindowsDesktopApp.tsx`; `win/src/windows-terminal-session.ts`; `packages/shared/src/terminal/mac-terminal-view.tsx` | Windows desktop window/menu/package/platform integration; consumes shared transport/buffer/renderer semantics and must not import Mac IPC or daemon/mirror owners | `win/src/windows-architecture-truth.test.ts`; `win/src/windows-terminal-session.test.ts`; typecheck/build; packaged Windows smoke pending |
| `client.plugin_host` | `src/lib/plugin-host/plugin-host-runtime.ts`; `src/lib/plugin-host/network-identity-capability-plugin.ts`; `src/lib/plugin-host/debug-console-ui-plugin.tsx`; `src/lib/plugin-host/session-drawer-ui-plugin.tsx`; `src/lib/plugin-host/file-browser-ui-plugin.tsx`; `packages/shared/src/terminal/plugin-contract.ts`; `packages/shared/src/terminal/plugin-capability-registry.ts`; `packages/shared/src/terminal/plugin-ui-slot.ts`; `packages/shared/src/terminal/plugin-ui-slot-registry.ts`; `src/App.tsx` (composition consumer only) | declared manifest install/start/stop/dispose, unique capability provider enforcement, plugin-provided capability removal on stop/dispose, typed UI slot registry consumption, and App-level host capability injection; UI/page/hook/plugin layers never hold host or capability registry access directly | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/lib/plugin-host/plugin-host-ownership.test.ts`; `src/pages/TerminalPageDebugOverlay.test.tsx`; `src/pages/TerminalPage.render-scope.test.tsx`; `src/pages/TerminalPage.remote-window-overlay.test.tsx`; `src/pages/TerminalPage.session-drawer.test.tsx`; `src/pages/TerminalPage.session-preview.test.tsx`; `src/pages/TerminalPage.tab-isolation.test.tsx`; `src/pages/TerminalPage.render-isolation.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.debug_console` | `src/lib/plugin-debug-console/debug-console-contract.ts`; `src/lib/plugin-host/debug-console-ui-plugin.tsx`; `src/pages/TerminalPageDebugOverlay.tsx`; `packages/shared/src/terminal/plugin-ui-slot.ts`; `packages/shared/src/terminal/plugin-ui-slot-registry.ts` | typed debug console UI slot contract, terminal debug session projection, and debug overlay render; App/TerminalPage read the slot render callback, the plugin host provides it through the UI slot registry, and the overlay never owns raw transport/session/mirror/sparse/render/UI projection truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/pages/TerminalPageDebugOverlay.test.tsx`; `src/pages/TerminalPage.render-scope.test.tsx`; `src/pages/TerminalPage.remote-window-overlay.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.session_drawer_ui` | `src/lib/plugin-session-drawer/session-drawer-contract.ts`; `src/lib/plugin-host/session-drawer-ui-plugin.tsx`; `src/pages/TerminalPage.tsx` (slot render consumer); `src/App.tsx` (slot render consumer) | typed session drawer UI slot contract, plugin-provided `terminal.session-drawer` slot render, and App/TerminalPage slot consumption; TerminalPage never imports or renders TerminalSessionDrawer directly and the contract never owns drawer preview selection, transport, session, mirror, sparse, renderer, or UI projection truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/pages/TerminalPage.session-drawer.test.tsx`; `src/pages/TerminalPage.session-preview.test.tsx`; `src/pages/TerminalPage.tab-isolation.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.file_browser_ui` | `src/lib/plugin-file-browser/file-browser-contract.ts`; `src/lib/plugin-host/file-browser-ui-plugin.tsx`; `src/pages/TerminalPage.tsx` (slot render consumer); `src/App.tsx` (slot render consumer) | typed file browser UI slot contract, plugin-provided `terminal.file-browser` slot render, and App/TerminalPage slot consumption; TerminalPage never imports or renders FileTransferSheet directly and the contract never owns transport, session, mirror, sparse, renderer, or UI projection truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/lib/plugin-file-browser/file-browser-ui-ownership.test.ts`; `src/pages/TerminalPage.render-isolation.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.settings_update_ui` | `src/lib/plugin-settings-update/settings-update-contract.ts`; `src/lib/plugin-host/settings-update-ui-plugin.tsx`; `src/pages/SettingsPage.tsx` (slot render consumer); `src/App.tsx` (slot render consumer) | typed settings update UI slot contract, plugin-provided `settings.update` slot render, and App/SettingsPage slot consumption; SettingsPage never imports or renders AppUpdateSection directly and the contract never owns transport, session, mirror, sparse, renderer, open-tab, or UI projection truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/lib/plugin-settings-update/settings-update-ui-ownership.test.ts`; `src/pages/SettingsPage.plugin-render.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.quickbar_ui` | `src/lib/plugin-quickbar/quickbar-contract.ts`; `src/lib/plugin-host/quickbar-ui-plugin.tsx`; `src/pages/TerminalPage.tsx` (slot render consumer); `src/App.tsx` (slot render consumer) | typed quickbar UI slot contract, plugin-provided `terminal.quickbar` slot render, and App/TerminalPage slot consumption; TerminalPage passes backend capability flags from the registered `terminal.shell_actions` projection through typed quickbar props; TerminalPage never imports or renders TerminalQuickBar directly and the contract only binds `resource.platform_input_channel` prop types without owning transport, session, mirror, sparse, renderer, UI projection, Herdr capability truth, or input normalization truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/lib/plugin-quickbar/quickbar-ui-ownership.test.ts`; `src/pages/terminal-page-status-helpers.test.ts`; `src/pages/TerminalPage.render-isolation.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.terminal_shell_ui` | `src/lib/plugin-terminal-shell/terminal-shell-contract.ts`; `src/lib/plugin-host/terminal-shell-ui-plugin.tsx`; `src/pages/TerminalPage.tsx` (slot render consumer); `src/App.tsx` (slot render consumer) | typed terminal shell UI slot contract, plugin-provided `terminal.shell` slot render, and App/TerminalPage slot consumption; TerminalPage never imports or renders TerminalConnectionStatusStrip, TerminalPageCopyMenu, TerminalPageStageShell, terminal-page-shell-ui, TerminalQuickBarShell, or TerminalNetworkBanner directly and the contract never owns transport, session, mirror, sparse, renderer, UI projection, or terminal shell/status/copy/stage truth | `src/lib/plugin-host/plugin-host-runtime.test.ts`; `src/lib/plugin-terminal-shell/terminal-shell-ui-ownership.test.ts`; `src/pages/TerminalPage.render-isolation.test.tsx`; `src/App.dynamic-refresh.test.tsx`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.composition_root` | `src/lib/composition-root/client-composition-root.ts`; `src/App.tsx` (composition consumer only) | bind/resolve/validate declared client runtime ports; rejects duplicate, unbound, and missing ports before use; App binds the `plugin-host` port through this owner; never owns transport/session/buffer/renderer/plugin implementation or business truth | `src/lib/composition-root/client-composition-root.test.ts`; `src/lib/composition-root/client-composition-root-ownership.test.ts`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `client.control_center` | `src/lib/control-center/client-control-center.ts`; `src/lib/plugin-host/plugin-host-control-node.ts`; `packages/shared/src/terminal/control-contract.ts`; `src/App.tsx` (composition consumer only) | capability/authorization gate, deadline, idempotency, correlation, unique command routing, bounded audit, typed plugin-host dispose command, branded shared control contract; never owns transport/session/buffer/renderer/plugin implementation or terminal/file/media body truth | `src/lib/control-center/client-control-center.test.ts`; `src/lib/control-center/client-control-center-ownership.test.ts`; `src/lib/plugin-host/plugin-host-control-node.test.ts`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `daemon.control_center` | `src/server/daemon-control-center-runtime.ts`; `src/server/daemon-control-gateway-runtime.ts`; `src/server/terminal-message-runtime.ts` (routing consumer only) | capability gate, deadline, idempotency, correlation, unique command routing, bounded audit, typed daemon command owner adapters for schedule and tmux control; gateway preserves existing wire response/error semantics and never owns mirror/transport/file/media body truth | `src/server/daemon-control-center-runtime.test.ts`; `src/server/daemon-control-center-ownership.test.ts`; `src/server/terminal-message-runtime.test.ts`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `daemon.session_catalog` | `src/server/daemon-session-catalog-runtime.ts`; `src/server/daemon-control-gateway-runtime.ts` (list-sessions delegation); `src/server/terminal-message-control-runtime.ts` (catalog builder consumption); `src/server/terminal-message-runtime.ts` (gateway routing) | backend session catalog construction, backend-qualified `sessionCatalog` rows, `list-sessions` control dispatch, legacy `sessions` payload, list-time `session-activity` facts, explicit list failure; no client active/session, open-tab, mirror, transport subscriber, renderer, or UI truth | `src/server/daemon-session-catalog-runtime.test.ts`; `src/server/daemon-session-catalog-ownership.test.ts`; `src/lib/module-import-graph-truth.test.ts`; `src/lib/edge-registry-truth.test.ts`; `src/lib/feature-registry-truth.test.ts` |
| `project.loop_governance` | `docs/loops/LOOP.md`; `docs/loops/loop-manifest.json`; `src/lib/loop-governance-truth.test.ts` | L1 report-only loop governance docs, state, constraints, budget, run log, manifest, test design, project entry link, mainline call ID binding, and static truth gate; no product runtime action is enabled | `src/lib/loop-governance-truth.test.ts`; `test:feature-registry` |
| `appsdk.binary_gate` | `scripts/verify-appsdk-binary.mjs#verifyPinnedAppSdkBinary`; `scripts/verify-appsdk-binary.test.mjs`; `package.json#test:appsdk-verify`; `.github/workflows/ci.yml`; `.github/workflows/android-release.yml` | resolve one PATH-selected AppSDK executable, validate its exact version and bytes against `.appsdk/project.json` plus `.appsdk/sdk.lock`, and invoke record-graph verification through that same absolute path; no fallback compiler, bare second invocation, Active/Protected mutation, or product runtime truth | `scripts/verify-appsdk-binary.test.mjs`; `test:appsdk-verify`; `test:feature-registry`; `docs/testing/appsdk-binary-gate-test-design.md` |
| `mainline_source.android` | `android/native/android/app/src/main/**`; `android/capacitor.config.ts`; `android/vite.config.ts`; `android/src/main.tsx`; `android/src/App.tsx`; `android/src/components/TerminalView.tsx`; `android/src/pages/TerminalPage.tsx`; `android/src/lib/terminal-shell-skin.ts#resolveEffectiveTerminalShellSkin`; `android/src/lib/terminal-shell-skin.ts#resolveTerminalRendererThemeForSkin`; `android/src/hooks/useAppPageState.ts` | Android render path and platform shell projection; `MainActivity` owns stopped-task screenshot policy before Capacitor creates the window, and the runtime theme owns the opaque task representation. Persisted shell choice flows through one resolver into TerminalPage and the Header/QuickBar/Drawer/status projections. The built-in/default renderer choice maps to the established Pencil Light/Cobalt2/Classic Dark preset while explicit renderer choices stay independent; page navigation owns Android back intent and consumes system exit on Terminal so the drawer edge is not also an app-exit path | `android-task-transition-truth.test.ts`; `test:terminal:shell-theme`; `terminal-shell-skin.test.ts`; `useAppPageState.test.tsx`; `test:feature-registry`; `docs/testing/terminal-shell-theme-test-design.md`; `docs/architecture.md` cross-reference gate |
| `mainline_source.daemon` | `src/server/server.ts`; `src/server/terminal-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/server/terminal-message-runtime.ts`; `src/server/daemon-control-gateway-runtime.ts`; `src/server/daemon-control-center-runtime.ts`; `src/server/daemon-input-queue-runtime.ts`; `src/server/terminal-reliable-input-ack.ts`; `src/server/terminal-mirror-runtime.ts`; `src/server/terminal-control-runtime.ts`; `src/server/terminal-schedule-runtime.ts`; `src/server/terminal-file-transfer-runtime.ts`; `src/server/remote-screenshot-daemon.ts`; `src/server/remote-window-stream-daemon.ts` | the daemon process mainline; wiki must draw a request path anchored at `server.ts` | `src/server/server.daemon-runtime-truth.test.ts`; `src/server/daemon-input-queue-runtime.test.ts`; `src/lib/function-wiki-truth.test.ts` |
| `mainline_source.cli` | `scripts/zterm-daemon.sh`; `scripts/windows/zterm-daemon.ps1`; `scripts/install-global-daemon-cli.sh`; `scripts/prepare-global-daemon-release.sh#stage_runtime`; `#verify_deterministic_archive`; `#create_deterministic_archive`; `#normalize_release_tree_metadata`; `scripts/prepare-daemon-npm-package.mjs`; `scripts/prepare-relay-server-npm-package.mjs` | the CLI release mainline; wiki must list the install paths, support script names, global bin name `zterm-daemon` (legacy alias `wterm`), and the adjacent deterministic release stages `stage -> verify -> archive -> normalize` bound to real shell symbols | `src/lib/function-wiki-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `release:verify` |

Rules:

- One feature has one owner surface. Shared helpers are allowed only when the registry names them as allowed paths.
- Forbidden paths are hard boundaries. Do not patch behavior there to make a feature pass indirectly.
- Required gates are the minimum verification stack, not a replacement for broader regression when the change has wider impact.

## File Transfer Mainline Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| daemon.file_transfer.message_route | `src/server/terminal-file-transfer-message-runtime.ts#createTerminalFileTransferMessageRuntime`; `src/server/terminal-message-runtime.ts#createTerminalMessageRuntime` | `daemon_mainline:Message->DaemonFileTransferMessageIn01Owner`; `daemon_mainline:DaemonFileTransferUploadIn01MessageDispatch->DaemonFileTransferUploadIn02RuntimeFacade`; `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadIn03CumulativeAckOwner`; `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadEndIn04ExactCompletionOwner` | route paste/attach/list/download/screenshot/upload messages and raw binary chunks only to `daemon.file_transfer`; preserve session-required errors and legacy wire semantics; keep no file transfer business state or handler in `terminal-message-runtime.ts` |
| daemon.file_transfer.client.upload_window | `src/lib/file-transfer-throughput-runtime.ts#sendBoundedFileUploadChunks` | `android_mainline:ClientFileTransferUploadOut01SheetIntent->ClientFileTransferUploadOut02BoundedWindow`; `android_mainline:ClientFileTransferUploadOut02BoundedWindow->ClientFileTransferUploadOut03ChunkDispatch` | expose at most eight unacknowledged 16 KiB chunks, advance only from request-bound cumulative ACK truth, and never restore stop-and-wait or unbounded burst |
| daemon.file_transfer.daemon.upload_ack | `src/server/terminal-file-transfer-runtime.ts#createTerminalFileTransferRuntime`; `src/server/terminal-file-transfer-binary-runtime.ts#handleFileUploadChunk`; `#handleFileUploadEnd`; `src/server/terminal-transport-runtime.ts#sendMessage` | `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadIn03CumulativeAckOwner`; `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadEndIn04ExactCompletionOwner`; `daemon_mainline:DaemonFileTransferUploadIn03CumulativeAckOwner->DaemonFileTransferUploadProgressOut01CumulativeAck`; `daemon_mainline:DaemonFileTransferUploadEndIn04ExactCompletionOwner->DaemonFileTransferUploadSuccessOut01Complete`; `daemon_mainline:DaemonFileTransferUploadIn03CumulativeAckOwner->DaemonFileTransferUploadErrorOut01ChunkRejected`; `daemon_mainline:DaemonFileTransferUploadEndIn04ExactCompletionOwner->DaemonFileTransferUploadErrorOut02CompletionRejected` | accept only valid request/index-bound chunks, advance ACK through the exact contiguous prefix, validate completion only in `handleFileUploadEnd`, keep progress/completion/error branches separate, and converge only at the existing subscriber transport send owner |
| daemon.file_transfer.client.download_persist | `src/lib/file-transfer-session-runtime.ts#createFileTransferSessionRuntime`; `src/lib/file-transfer-throughput-runtime.ts#writeFileTransferChunkBatches`; `native/android/app/src/main/java/com/zterm/android/StoragePermissionPlugin.java#writeFileChunks` | `android_mainline:ClientFileTransferDownloadIn01SocketDispatch->ClientFileTransferDownloadIn02SessionProjection`; `android_mainline:ClientFileTransferDownloadIn03SheetPersistence->ClientFileTransferDownloadIn04NativeWriteBatch`; `android_mainline:ClientFileTransferDownloadIn05NativeWriteDispatch->ClientFileTransferDownloadIn06NativeStore` | preserve ordered wire payload, persist at most eight chunks per native call, and publish success only after exact on-disk size verification |

The active throughput owner is TypeScript plus the JSON contract. The Rust target is `planned` in `docs/testing/file-transfer-test-design.md#rust-migration-register`; it cannot be treated as active before parity gates and physical deletion of the TypeScript owner.

## Remote Window Stream Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| desktop.remote_window_stream.overlay.project | `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay`; `src/lib/remote-window-overlay-runtime.ts#beginRemoteWindowTargetEnumeration`; `#applyRemoteWindowTargetCatalog`; `#selectRemoteWindowTarget`; `#beginRemoteWindowStreamHandoff`; `#commitRemoteWindowStreamHandoff`; `#failRemoteWindowStreamHandoff`; `#failRemoteWindowStreamCleanup`; `#enterRemoteWindowFullscreen`; `#shrinkRemoteWindowOverlay`; `#closeRemoteWindowOverlay` | `android_mainline:TerminalPage->RemoteWindowOverlay` | render only Android picker/floating/fullscreen projection, collapse same-app app-window targets into one picker row, expose sibling window switching with non-active screenshot thumbnails inside the active video layer after stream lock, keep sibling switch as a transaction that preserves the complete active projection until receiver attach commit, refresh the same-app window group periodically from catalog truth and refresh only ready same-app thumbnails through terminal.remote_screenshot while failed/loading thumbnail requests remain terminal until a new target/request identity, keep close controls reachable from the main toolbar and secondary tiles, keep iTerm2 panes collapsed until explicit expansion, project the last session-local catalog snapshot immediately while a stale/forced refresh runs so the picker does not blank, lock a selected target manifest, size and bottom-corner-resize the floating video preview from the selected crop/window aspect ratio before stream start and from daemon capture frame aspect after stream start while capping growth so the toolbar remains reachable, keep default floating/fullscreen placement inside top and side safe boundaries, lift the target-locked floating projection by the current IME bottom inset, lift the fullscreen projection container by the current IME bottom inset without resizing the page shell, move the closed floating entry by threshold drag or long-press-armed drag, move the floating projection through a toolbar-only pointer-captured drag owner, shrink fullscreen on Back/minimize, keep fullscreen drawing/input projection aspect-fit and send a default remote target `window-resize` fill request when fullscreen first has a stream/surface, zoom/pan with local pan only after fullscreen zoom and no minimap/shrink-below-fit projection, expose a floating/fullscreen screenshot button that requests the selected target manifest through `terminal.remote_screenshot` and saves locally without input focus, preserve unzoomed app-window click action, release-time single-finger drag gesture action, realtime two-finger vertical scroll action on pointer move, and wheel input even when the IME inset is present, close UI state explicitly, bound catalog loading with a local UI watchdog, stop active stream and close the overlay when the app backgrounds, and display honest stream setup/handoff/cleanup/error/read-only states without faking terminal buffer/video/input truth |
| desktop.remote_window_stream.client.catalog_request | `src/contexts/session-context-remote-window-runtime.ts#requestRemoteWindowTargetsRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:TerminalPage->RemoteWindowOverlay` | send `remote-window-targets-request` over the active session transport owner, reuse the daemon-wide 60s catalog cache unless the overlay asks for explicit force-refresh, match response/error by request id, timeout explicitly, and never coerce daemon errors into an empty successful catalog |
| desktop.remote_window_stream.daemon.catalog_cache | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#createRemoteWindowStreamDaemonRuntime`; `#listTargets` | `daemon_mainline:Control->RemoteWindowStream` | warm the default app-window+iTerm2 catalog when the daemon runtime starts, keep one process-local 60-second cache per requested source set, return fresh cache without live enumeration, return stale cache immediately while one background refresh runs, await live enumeration for the first uncached request or explicit `forceRefresh`, rewrite cached response/error request ids for the current caller, and expose live failures explicitly when no cached target catalog exists |
| desktop.remote_window_stream.client.response_dispatch | `src/contexts/session-context-socket-message-runtime.ts#handleSocketServerMessageRuntime` | `android_mainline:SessionContext->SocketMessage` | route only remote-window response/start/status/candidate/input-result/error messages to the remote-window message runtime; file transfer and terminal buffer handlers remain separate |
| desktop.remote_window_stream.start.request | `src/contexts/session-context-remote-window-runtime.ts#requestRemoteWindowStreamStartRuntime`; `src/contexts/session-context-remote-window-runtime.ts#resolveRemoteWindowStreamIceServers`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowOverlay->RemoteWindowMessageRuntime` | send `remote-window-stream-start-request` over the existing active session transport with a selected daemon manifest, WebRTC receive offer, route-derived ICE servers for the current traversal path, and the selected video bitrate config; do not create a terminal transport or compute coordinates in Android |
| desktop.remote_window_stream.client.dual_switch | `src/lib/remote-window-dual-stream-runtime.ts#beginRemoteWindowDualStreamSwitch`; `#showRemoteWindowOverviewCrop`; `#acceptRemoteWindowFocusReady`; `#commitRemoteWindowFocusProjection`; `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay` | `android_mainline:RemoteWindowOverlay->RemoteWindowDualStreamSwitch`; `android_mainline:RemoteWindowDualStreamSwitch->RemoteWindowMessageRuntime` | increment one client revision per thumbnail selection, expose the matching overview crop immediately, send one typed focus update, and commit the high-quality video only after matching request/revision/target/stream first-frame ready; stale/error/close results never overwrite the current projection |
| desktop.remote_window_stream.client.sibling_handoff | `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay`; `src/lib/remote-window-overlay-runtime.ts#beginRemoteWindowStreamHandoff`; `#commitRemoteWindowStreamHandoff`; `#failRemoteWindowStreamHandoff`; `#failRemoteWindowStreamCleanup`; `src/contexts/session-context-remote-window-runtime.ts#requestRemoteWindowStreamStartRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowOverlay->RemoteWindowOverlayRuntime`; `android_mainline:RemoteWindowOverlay->RemoteWindowMessageRuntime`; `android_mainline:SocketMessage->RemoteWindowMessageRuntime`; `android_mainline:RemoteWindowMessageRuntime->RemoteWindowReceiver`; `android_mainline:RemoteWindowReceiver->RemoteWindowOverlay`; `daemon_mainline:RemoteWindowStream->RemoteWindowCleanup` | same-app sibling selection records an overlay-owned pending handoff with `{previousStreamId, pendingStreamId, targetId, epoch}`; it sends exactly one start request for the pending target, leaves the previous media/input/projection active until the pending receiver attaches, commits only the latest matching epoch, explicitly stops stale started streams and the previous stream after commit through request-id matched stop promises, and projects handoff or cleanup failures separately from current-stream disconnect errors |
| desktop.remote_window_stream.client.quality_request | `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay`; `src/lib/remote-window-video-quality.ts#buildRemoteWindowVideoBitrateConfig`; `#readRemoteWindowVideoBitratePreset`; `#writeRemoteWindowVideoBitratePreset`; `#resolveEffectiveRemoteWindowVideoBitratePreset`; `#resolveRemoteWindowDesktopCoverageRatio`; `#resolveRemoteWindowVideoAdaptiveDecision`; `src/lib/remote-window-receiver-runtime.ts#createRemoteWindowReceiverRuntime`; `src/contexts/session-context-remote-window-runtime.ts#updateRemoteWindowStreamQualityRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowOverlay->RemoteWindowStreamQualityRuntime` | remember manual bitrate by selected desktop window identity/resolution, keep untouched defaults and floating preview effective stream request at `2 Mbps`, keep Android phone fullscreen from upgrading bitrate by itself, sample WebRTC stats for RTT/available bitrate/FPS/drop/freeze/jitter, downgrade to one-quarter bitrate and half FPS on weak stats, restore only after a stable window, and send `remote-window-stream-quality-request` for the active stream over the existing active session transport without restarting capture, receiver, or terminal transport |
| desktop.remote_window_stream.android.receiver.attach | `src/lib/remote-window-receiver-runtime.ts#createRemoteWindowReceiverRuntime`; `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay` | `android_mainline:RemoteWindowMessageRuntime->RemoteWindowReceiver` | attach the returned WebRTC video stream id to the overlay receiver surface and keep Back/minimize projection-only |
| desktop.remote_window_stream.android.frame.render | `src/components/terminal/RemoteWindowOverlay.tsx#remote-window-video` | `android_mainline:RemoteWindowReceiver->RemoteWindowOverlay` | render only received media frames in the floating/fullscreen video surface; terminal buffer/renderer rows cannot satisfy this state |
| desktop.remote_window_stream.client.touch_action | `src/lib/remote-window-touch-action-runtime.ts#dispatchRemoteWindowTouchInputActionRuntime`; `#isRemoteWindowInputSupportedTarget`; `#resolveRemoteWindowTouchScrollDeltaRuntime`; `#resolveRemoteWindowTouchWheelDeltaRuntime`; `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay` | `android_mainline:RemoteWindowOverlay->RemoteWindowTouchAction` | classify supported app-window input context, turn overlay touch/mouse/wheel/key surface events into explicit action dispatch records, convert tap or mouse click into one `click` action, convert release-time single-finger drag direction into one bounded client-owned absolute visible-source pixel gesture, convert target-locked coherent two-finger vertical movement into realtime bounded pixel `scroll` actions on pointer move with no release-time remote action, keep two-finger scroll vertical-only while clear fullscreen pinch remains local zoom projection, report unsent actions as `missing-context` / `unsupported-target` / `missing-dispatcher`, and never let overlay UI state alone claim a physical transport send |
| desktop.remote_window_stream.client.input_request | `src/lib/remote-window-touch-action-runtime.ts#dispatchRemoteWindowTouchInputActionRuntime`; `src/pages/TerminalPage.tsx#emitRemoteWindowInputEvents`; `src/pages/TerminalPage.tsx#handleQuickBarImagePaste`; `src/lib/remote-window-input-mapping.ts#buildRemoteWindowKeyInputEventsFromSequence`; `#buildRemoteWindowTextInputEvents`; `#buildRemoteWindowBackspaceInputEvents`; `#buildRemoteWindowKeyboardInputEvents`; `src/contexts/session-context-remote-window-runtime.ts#sendRemoteWindowInputRuntime`; `#resizeRemoteWindowTargetRuntime`; `src/contexts/session-context-transfer-runtime.ts#sendImagePasteRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowTouchAction->RemoteWindowInputRuntime`; `android_mainline:TerminalPage->RemoteWindowInputRuntime` | keep the media `<video>` pointer-transparent, map video-surface input/scroll coordinates against the selected crop manifest and actual aspect-fit/zoomed content rect into global macOS top-left coordinates, emit unzoomed touch tap or mouse click as one `click` action, emit unzoomed touch/mouse drag as one release-time absolute visible-source pixel `gesture/swipe` action without raw pointer-move streaming, keep mouse/trackpad wheel input as one pixel `scroll` action, keep zoomed fullscreen one-finger drag local to pan, map target-locked coherent two-finger vertical movement to realtime pixel `scroll` actions at the midpoint without release-time accumulation, and send `window-resize` only from the fullscreen fill owner using current desktop window width plus phone aspect-derived height; setup/projection actions such as target select, stream start, receiver attach, fullscreen entry, IME lift, pinch zoom, and local zoom-pan must not send focus intents; click/gesture/wheel/key/QuickBar/IME operations must leave Android as one user action record without a client-side focus prelude, because daemon input injection remains the only focus truth and checks focus inline before OS injection; expose the active stream input context to `TerminalPage` only when the target is supported app-window `bring-to-focus + os-event`, never publish or send input for current unsupported `tmux-input` / `iterm2-api` routes, route QuickBar key/text/paste/arrow and raw IME committed text/backspace/key events to explicit `remote-window-input` over the existing session transport when that app-window OS-event context is active, route image paste to a remote-window `pasteTarget` only while that context is active and clear the context when terminal surface focus is activated, preserve the terminal image paste Ctrl+V path otherwise, grey terminal/tmux/iTerm-specific QuickBar actions, and keep zoom/pan projection separate from daemon injection truth |
| desktop.remote_window_stream.daemon.stream_start | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#startStream` | `daemon_mainline:Control->RemoteWindowStream` | route `remote-window-stream-start-request` to daemon stream owner, validate target manifest, apply optional bitrate only to existing sender encodings without changing their structure, start capture, negotiate media, and return explicit started/error/status truth |
| desktop.remote_window_stream.daemon.focus_revision | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#updateFocus`; `src/server/remote-window-stream-daemon.ts#sendRemoteWindowVideoFrame` | `daemon_mainline:Control->RemoteWindowStream` | reject non-increasing focus revisions, update only the focus capture source while overview remains alive, return typed `accepted`, and emit typed `ready` only after the first real focus frame for that revision; errors stay on the error chain |
| desktop.remote_window_stream.daemon.capture_frame | `src/server/remote-window-stream-daemon.ts#startScreenCaptureKitFrameSource` | `daemon_mainline:RemoteWindowStream->RemoteWindowCapture` | read real ScreenCaptureKit frames from the selected app window or iTerm2 pane crop and feed only those frames into the media sender |
| desktop.remote_window_stream.daemon.input_inject | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#injectInput`; `#buildRemoteWindowInputConfig`; `#createDefaultRemoteWindowInputHelper` | `daemon_mainline:Control->RemoteWindowStream` | validate stream id, target id, focus policy, input route, click/pointer/scroll/gesture coordinates, pixel deltas, and gesture timing before forwarding generic app input through macOS OS events; accept explicit `focus` intents only as daemon-owned bring-front operations, not as a required prelude for click/gesture/wheel/key input; include target window id/title/bounds in the daemon input config, match the AX window by bounds, check live frontmost PID through System Events, activate and `AXRaise` the target window only when it is not already frontmost/focused, verify frontmost/focused before reporting success, fast-return repeated focus or real input focus preparation when the target app/window is already frontmost/focused, use CGWindowList top-left coordinates directly for event location, translate DOM positive-down/right scroll/gesture deltas to CGEvent wheel signs only in this daemon owner, warm and readiness-check one daemon-owned persistent Swift helper during interactive stream start without emitting focus/input so Swift cold start is not charged to user operations, keep click/legacy pointer/scroll/gesture/key event sequences on that helper instead of compiling `swift -e` per event, apply the one-second stale/drop rule to actual queued input operations while refreshing only same-target action-only bursts after each preceding action succeeds, require burst live probes to send action records without client focus prefixes, allow focus/scroll events to omit `phase`, require gesture events to carry `phase=end`, and return explicit errors for unsupported routes instead of fake success |
| desktop.remote_window_stream.lifecycle.cleanup | `src/server/remote-window-stream-daemon.ts#stopStream`; `src/lib/remote-window-overlay-runtime.ts#closeRemoteWindowOverlay` | `daemon_mainline:RemoteWindowStream->RemoteWindowCleanup` | stop stream on explicit close/error, release capture/media/timers exactly once, and ignore late status/frame events after close |

## Relay Control Chain Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| relay.account_directory.daemon.endpoint_publish | `src/server/daemon-connection-endpoint-runtime.ts#buildDaemonConnectionEndpointCandidates`; `src/server/relay-client.ts#publishRelayDirectoryUpdate`; `src/server/relay-client.ts#createTraversalRelayHostClient` | `daemon_mainline:RelayHostControl->DaemonEndpointDirectory`; `daemon_mainline:DaemonEndpointDirectory->RelayDirectoryPublish` | keep one daemon-role Relay control WebSocket, publish account-scoped LAN, honest RTC-direct signaling/allocation truth, Tailscale, and TURN Relay candidates with the daemon auth token needed for direct WebSocket routes, and never derive a UDP mapping from the control WebSocket TCP peer address. Endpoint or tmux discovery failure is an explicit control error, not a partial successful directory. |
| relay.account_directory.client.control_connection | `src/lib/relay-device-stream-runtime.ts#createRelayDeviceStreamRuntime`; `src/lib/traversal-relay-client.ts#connectTraversalRelayDevicesStream`; `src/hooks/useRelayDeviceStream.ts#useRelayDeviceStream` | `android_mainline:RelayControlRuntime->RelayAccountStore`; `android_mainline:RelayAccountStore->RelayDirectoryProjection` | maintain one authenticated Android Relay control WebSocket independent of terminal transports; reconnect only this socket, retain the last confirmed endpoint directory across transient control failure, and clear it only on explicit account disable/logout. Control payloads cannot contain terminal/session/buffer fields. |
| relay.account_directory.client.directory_truth | `src/lib/client-control-directory-runtime.ts#ClientControlDirectoryRuntime.applyConfirmedDirectoryDevices`; `#mergeHostWithClientControlDirectory` | `android_mainline:RelayDirectoryProjection->TraversalSocketFactory`;  | store confirmed endpoint truth by stable daemon host id, including per-endpoint direct auth token truth, increment generation only when endpoint truth changes, preserve confirmed candidates when presence becomes disconnected or an incremental snapshot omits a host, and inject the newest candidates only when a future daemon-target transport is constructed. Endpoint push must not close or replace an open healthy terminal transport. |
| relay.route_selection.auto_priority | `src/lib/traversal/route-selector.ts#selectTraversalRouteCandidates`; `src/lib/traversal/route-health-cache.ts#TraversalRouteHealthCache`; `src/lib/traversal/config.ts#buildTraversalPlan` | `android_mainline:TraversalSocket->TerminalTransportOut01RouteSelection` | rank Auto candidates in fixed product tiers `LAN > Tailscale > RTC UDP direct > TURN Relay`; health/RTT may order or reject within a tier but cannot promote a healthy lower tier over a healthy higher tier. Persist successful route health by daemon/account/candidate identity without making it endpoint truth. |

## Terminal Transport Multiplex Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| terminal.transport_lifecycle.protocol.mux_contract | `packages/shared/src/connection/protocol.ts#TerminalMuxClientFrame`; `#TerminalMuxServerFrame`; `#buildTerminalMuxHello`; `#buildTerminalMuxPing`; `#buildTerminalMuxChannelOpen`; `#buildTerminalMuxChannelMessage`; `#isTerminalMuxClientFrame`; `#isTerminalMuxServerFrame` | `android_mainline:TargetTransportRuntime->MuxHandshake`; `android_mainline:TargetHeartbeat->TerminalMuxPingBuilder`; `android_mainline:TargetNetworkSignalOrchestration->TerminalMuxPingBuilder`; `daemon_mainline:Bridge->MuxHandshake` | define the only mux-capable wire envelope: target-level control frames, channel-open/channel-close, channel-bound request/response frames, ping/pong, and explicit upgrade-required / unknown-channel errors. Heartbeat and network-generation probes must both use `buildTerminalMuxPing`; all session-bound payloads must carry `channelId`; unwrapped or mismatched payloads must be rejected before reaching buffer/input/file/schedule truth. |
| terminal.transport_lifecycle.target_transport.open | `src/lib/session-transport-runtime.ts#ensureTargetTransportRuntime`; `#buildTransportTargetKey`; `#buildTransportRouteCandidateKey`; `src/lib/client-daemon-connection.ts#createClientDaemonConnection`; `src/contexts/session-context-transport-open-runtime.ts#openSessionMuxChannelByIntentRuntime`; `src/contexts/session-context-transport-orchestration-runtime.ts#handleTargetMuxTransportFailureRuntime` | `android_mainline:TransportOpen->TargetTransportRuntime`; `android_mainline:TraversalSocket->TargetTransportRuntime`; `android_mainline:TargetTransportRuntime->ChannelRuntime`; `android_mainline:MuxHandshake->TerminalTransportError01TargetFailure`; `android_mainline:TerminalTransportError01TargetFailure->TraversalSocket`; `android_mainline:TerminalTransportError01TargetFailure->SessionRuntime` | open or reuse exactly one physical daemon-target transport for the current daemon target, negotiate mux capability, preserve selected route diagnostics as target transport metadata, and never allocate a second physical transport for session switch or route-candidate update while the target transport is open. `TerminalTransportError01TargetFailure` is the sole physical failure owner: heartbeat and mux observers only submit failure, while this owner records route health exactly once, retires and closes that exact generation, preserves logical channel identities, then schedules one target-level rebuild; it must not remain a recent-success route or fan out into per-session physical reconnects. |
| terminal.transport_lifecycle.target_transport.network_probe | `src/hooks/useOpenTabLifecycleEffects.ts#useOpenTabLifecycleEffects`; `src/hooks/useOpenTabRuntime.ts#useOpenTabRuntime`; `src/App.tsx#AppContent`; `src/contexts/SessionContext.tsx#SessionProvider`; `src/contexts/session-context-provider-facade-assemblies.ts#useSessionProviderFacadeAssemblies`; `src/contexts/session-context-public-facade-runtime.ts#createSessionPublicFacadeRuntime`; `src/contexts/session-context-provider-core-assemblies.ts#useSessionProviderCoreAssemblies`; `src/contexts/session-context-transport-orchestration-runtime.ts#createSessionTransportOrchestrationRuntime`; `#notifyTargetNetworkSignalRuntime`; `#routeTargetSocketFailureRuntime`; `src/contexts/session-context-target-network-probe-runtime.ts#createSessionTargetNetworkProbeRuntime`; `src/contexts/session-context-transport-runtime.ts#createSessionContextTransportAccessors`; `#bindTargetMuxTransportSocketLifecycleRuntime`; `src/lib/session-transport-runtime.ts#listTargetTransportRuntimes`; `packages/shared/src/connection/protocol.ts#buildTerminalMuxPing` | `android_mainline:PlatformNetworkSignal->OpenTabNetworkBinding`; `android_mainline:OpenTabNetworkBinding->AppNetworkBinding`; `android_mainline:AppNetworkBinding->SessionContextNetworkFacade`; `android_mainline:SessionContextNetworkFacade->SessionProviderNetworkBinding`; `android_mainline:SessionProviderNetworkBinding->SessionPublicFacadeBinding`; `android_mainline:SessionPublicFacadeBinding->SessionProviderCoreBinding`; `android_mainline:SessionProviderCoreBinding->TargetNetworkSignalOrchestration`; `android_mainline:TargetNetworkSignalOrchestration->TargetTransportAccessors`; `android_mainline:TargetNetworkSignalOrchestration->TerminalMuxPingBuilder`; `android_mainline:TargetTransportAccessors->TargetTransportStoreEnumeration`; `android_mainline:TargetTransportStoreEnumeration->TargetNetworkProbeDispatch`; `android_mainline:TargetNetworkProbeDispatch->TargetNetworkProbe`; `android_mainline:MuxHandshake->TargetMuxFrameLifecycle`; `android_mainline:TargetMuxFrameLifecycle->TargetNetworkActivityBinding`; `android_mainline:TargetNetworkActivityBinding->TargetNetworkProbe`; `android_mainline:TargetNetworkProbe->TargetFailureRouter`; `android_mainline:TargetFailureRouter->TerminalTransportError01TargetFailure`; `android_mainline:TargetFailureRouter->IdleTargetRetirement` | treat Android network callbacks and every foreground generation as ephemeral generation signals, never foreground, active-tab, or endpoint-reachability truth; pass them through the open-tab/App/context/provider/public-facade/core-assembly bindings into the unique daemon-connection owner, enumerate every current daemon-target physical transport only through `createSessionContextTransportAccessors -> listTargetTransportRuntimes`, dedupe by target plus exact socket generation, build the target-level probe through the same `buildTerminalMuxPing` shared contract as the target heartbeat, and resolve a pending probe only when the mux handshake binds `bindTargetMuxTransportSocketLifecycleRuntime`, that lifecycle validates inbound activity, and the orchestration binding records the same target key plus exact socket generation. One bounded probe timeout is diagnostic-only and cannot retire an OPEN generation; synchronous send failure, terminal socket state, or the target heartbeat's consecutive-miss policy owns physical failure. Targets with channels enter the existing target failure owner; retained zero-session targets remain healthy on valid pong/activity and retire only that physical generation on confirmed failure without inventing a session or reconnect job. Session channels cannot own, scope, or multiply this probe. |
| `android_network_identity_snapshot` | `native/android/app/src/main/java/com/zterm/android/NetworkIdentityPlugin.java#snapshot`; `#buildSnapshot`; `src/plugins/NetworkIdentityPlugin.ts#readNativeNetworkIdentitySnapshot`; `src/App.tsx#createAppPluginHost`; `#AppContent`; `src/hooks/useOpenTabLifecycleEffects.ts#refreshNetworkIdentityForForeground`; `src/lib/network-identity.ts#projectNetworkIdentitySnapshotError`; `src/contexts/session-context-target-network-probe-runtime.ts#TargetNetworkProbeError04NativeSnapshot`; `src/contexts/session-context-transport-orchestration-runtime.ts#reportTargetNetworkProbeErrorRuntime`; `#submitTargetNetworkProbeError`; `src/lib/client-daemon-connection.ts#createClientDaemonConnection`; `#reportTargetNetworkProbeError`; `#onTargetNetworkProbeError`; `#readTargetNetworkProbeError`; `#acknowledgeTargetNetworkProbeError`; `src/lib/android-network-identity-truth.test.ts`; `src/lib/network-identity.test.ts`; `src/App.dynamic-refresh.test.tsx`; `src/hooks/useOpenTabLifecycleEffects.test.tsx`; `src/contexts/session-context-transport-orchestration-runtime.test.ts`; `src/lib/client-daemon-connection.test.ts` | `android_mainline:Native->NetworkIdentityWrapper`; `android_mainline:App_createAppPluginHost->PluginHost`; `android_mainline:PluginHost->NetworkIdentityCapability`; `android_mainline:NetworkIdentityCapability->NetworkIdentityRuntime`; `android_mainline:NetworkIdentityRuntime->AppNativeSnapshotErrorProjector`; `android_mainline:AppNativeSnapshotErrorProjector->AppNetworkBinding`; `android_mainline:NetworkIdentityRuntime->LifecycleNativeSnapshotErrorProjector`; `android_mainline:LifecycleNativeSnapshotErrorProjector->OpenTabNetworkBinding`; `android_mainline:OpenTabNetworkBinding->AppNetworkBinding`; `android_mainline:AppNetworkBinding->SessionContextNetworkFacade`; `android_mainline:SessionContextNetworkFacade->SessionProviderNetworkBinding`; `android_mainline:SessionProviderNetworkBinding->SessionPublicFacadeBinding`; `android_mainline:SessionPublicFacadeBinding->SessionProviderCoreBinding`; `android_mainline:SessionProviderCoreBinding->TargetNetworkSignalOrchestration`; `android_mainline:TargetNetworkSignalOrchestration->TargetNetworkProbeError04NativeSnapshot`; `android_mainline:TargetNetworkProbeError04NativeSnapshot->DaemonConnectionErrorChain` | native Android NetworkIdentityPlugin is the unique producer for the ephemeral `resource.platform_network_signal` snapshot; the JS wrapper forwards only the interface fingerprint to the client daemon-connection owner. Initial AppContent and foreground lifecycle snapshot failures each use their real projector, then traverse the same App/SessionContext/provider/public-facade/core/orchestration chain before `reportTargetNetworkProbeErrorRuntime` creates typed `TargetNetworkProbeError04NativeSnapshot`. The daemon-connection owner retains only the latest typed failure, replacing the previous failure instead of appending an unbounded history, and invokes the registered typed error consumer; it does not change target/session transport truth. The native producer never enters daemon or terminal business payloads. Native registration and the JS wrapper are two adjacent observers of the same platform signal resource; `App.createAppPluginHost` provides the wrapper's `readNativeNetworkIdentitySnapshot` to the plugin host through the `network:native-snapshot` capability via `resource.plugin_capability_registry`, and the capability-to-runtime edge traverses the same registry; all direct positive/negative tests are wired through `test:android-network-identity` in prebuild and CI. |
| terminal.transport_lifecycle.target_tmux_management | `src/contexts/session-context-tmux-management-runtime.ts#manageTmuxSessionsOnOpenTransportRuntime`; `#settleSessionTmuxTargetRequestRuntime`; `src/hooks/useSessionOpenActions.ts#manageTmuxSessionsForTarget`; `#handleCloseGroupSession`; `src/hooks/useOpenTabRuntime.ts#auditOpenTabsAgainstRemoteSessions`; `src/lib/remote-tab-audit.ts#auditOpenTabsAgainstRemoteSessions`; `src/lib/open-tab-restore.ts#fetchRemoteTmuxSessionNamesByOwner` | `android_mainline:SessionOpenOwner->SessionContext`; `android_mainline:TargetTransportRuntime->MuxHandshake` | drawer refresh, open-tab audit, quick create, Home no-session discovery, and remote kill must use the existing daemon-target mux physical transport through request-id `mux-target-message` when a matching non-closed Session exists. When the drawer kills a currently connected session, the session owner first pauses that logical session (body subscription off and runtime marked disconnected) without committing a manual-close marker, switching active tabs, or closing the mux channel; it then sends `tmux-kill-session` over that same still-healthy transport when it is the only reusable transport and performs final local runtime cleanup/channel close only after the daemon accepts the kill. If another healthy matching session exists, the kill request uses that sibling instead of the paused channel. Daemon `error`, malformed response, timeout, or not-yet-ready mux target state must explicitly call the SessionContext `reconnectSession` lifecycle transition for each paused matching session before rethrowing; the reconnect transition restores that logical session without touching siblings, switching active tabs, writing a closed tombstone, or rebuilding a healthy shared target transport. The target transport must never be torn down before the kill request is dispatched, and failure must not fallback to a second legacy `TraversalSocket`; the legacy tmux management pool is only for no matching open target. |
| terminal.transport_lifecycle.channel_closed_control_status | `src/contexts/session-context-transport-orchestration-runtime.ts#resolveMuxChannelClosedWithControlStatusRuntime`; `#routeTargetSocketFailureRuntime`; `#handleTargetMuxTransportFailureRuntime`; `src/contexts/session-context-tmux-management-runtime.ts#manageTmuxSessionsOnOpenTransportRuntime` | `android_mainline:TransportOrchestration->TerminalChannelClosedIn01CloseSignal`; `android_mainline:TerminalChannelClosedIn01CloseSignal->TerminalChannelClosedIn02ControlStatusDecision`; `android_mainline:TerminalChannelClosedIn02ControlStatusDecision->TerminalTargetControlOut01ListSessions`; `android_mainline:TerminalChannelClosedIn02ControlStatusDecision->TargetFailureRouter`; `android_mainline:TargetFailureRouter->TerminalTransportError01TargetFailure` | decide one closed mux data-channel outcome from adjacent truth only: stale reopened channels are ignored, control-confirmed existing active/live tmux sessions reopen the logical channel, missing tmux truth closes the session projection, inactive channels stay idle, and active/live target-control unavailable or rejected routes the exact physical daemon-target generation to the target failure owner without session-level fallback reconnect. |
| terminal.transport_lifecycle.channel.open | `src/lib/terminal-channel-mux-runtime.ts#ensureSessionTerminalChannel`; `#getOpeningSessionTerminalChannelsForTarget`; `#updateSessionTerminalChannelState`; `#removeSessionTerminalChannel`; `src/contexts/session-context-transport-runtime.ts#sendSessionMuxChannelOpenRuntime`; `src/contexts/session-context-transport-orchestration-runtime.ts#createSessionTransportOrchestrationRuntime` (active session priority binding) | `android_mainline:TargetMuxFrameLifecycle->ActiveSessionPriority`; `android_mainline:ActiveSessionPriority->ChannelRuntime`; `android_mainline:TargetTransportRuntime->ChannelRuntime`; `daemon_mainline:MuxHandshake->ChannelRegistry` | map one local session to one transport-local channel, carry initial `bodySubscribed` truth in `mux-channel-open` before attach starts, and bind that channel to one daemon subscriber. When one same-target mux transport becomes ready, the mux lifecycle must read the current active session truth and order opening channel replay through `getOpeningSessionTerminalChannelsForTarget` so the active channel opens first; missing or non-opening active truth is a transport lifecycle failure, not an unprioritized fallback. `mux-channel-opened` is allocation truth only; terminal readiness comes from the same channel's real daemon `connected` message after attach/mirror readiness. Channel close detaches the subscriber without killing tmux or mirror truth. |
| terminal.transport_lifecycle.channel.send | `src/contexts/session-context-infra-facade-runtime.ts#wrapSessionPayloadForTargetMuxRuntime`; `#requireTerminalMuxChannel` | `android_mainline:ChannelRuntime->ChannelMessageSend` | send input, resize, body-subscription, buffer-head/range, schedule, file-transfer, screenshot, and terminal-context remote-window intents only inside `mux-channel-message`. UI, drawer, renderer, and buffer manager cannot open sockets or compensate channel identity. |
| terminal.transport_lifecycle.channel.demux | `src/contexts/session-context-transport-runtime.ts#handleTargetMuxServerFrameRuntime`; `src/contexts/session-context-socket-message-runtime.ts#handleSocketServerMessageRuntime`; `src/server/terminal-message-runtime.ts#handleMuxClientFrame` | `android_mainline:SocketMessage->ChannelDemux`; `android_mainline:ChannelDemux->BufferWireNormalize`; `daemon_mainline:Message->ChannelRegistry` | validate `channelId`, dispatch each accepted server frame only to the owning local session wire-normalization owner, and turn unknown/stale channel messages into explicit errors rather than dropping them silently or applying them to the active session. |
| terminal.buffer_render.publisher.mux_send | `src/server/daemon-buffer-publisher-runtime.ts#createDaemonBufferPublisherRuntime`; `#broadcastChangedRangesBufferSyncToSubscribers`; `#flushPendingSubscriberBufferSync`; `#sendBufferHeadToSession`; `src/server/terminal-transport-runtime.ts#sendText` | `daemon_mainline:Mirror->BufferPublisher`; `daemon_mainline:BufferPublisher->TransportSend` | `daemon.buffer_publisher` owns per-subscriber pending-latest, range merge/collapse, backpressure hysteresis, head broadcast cache, oversized contiguous frame split, explicit flush statuses, and physical transport send accounting; old body payloads are not queue truth and mirror runtime only calls the publisher boundary. |
| relay.account_directory.peer_lease.resume | `src/traversal-relay/server.ts#registerClient`; `src/server/relay-client.ts#createTraversalRelayHostClient`; `src/server/rtc-bridge.ts#handleRelaySignal` | `daemon_mainline:Relay->RelayPeerLease`; `daemon_mainline:RelayPeerLease->MuxHandshake` | issue and validate an idle-timeout bounded Relay peer lease for one account + daemon target + concrete client device. Client signaling reconnect may rebind to that same peer lease for 30 minutes before expiry; another phone gets a separate lease, and missing device id, expiry, or host/account/device mismatch must fail explicitly and close the peer. The lease must not store terminal channel, subscriber, tmux, mirror, active tab, foreground, viewport, or UI state. |

## Session Preview Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| terminal.session_preview.selection.normalize | `src/lib/session-preview-selection.ts#normalizeSessionPreviewSelection` | `android_preview:SessionDrawer->PreviewSelectionOwner` | normalize versioned ordered targets, deduplicate by open-session id, cap at six |
| terminal.session_preview.selection.toggle | `src/lib/session-preview-selection.ts#toggleSessionPreviewTarget` | `android_preview:SessionDrawer->PreviewSelectionOwner` | add/remove one valid target and explicitly reject selection seven |
| terminal.session_preview.selection.materialize_remote | `src/pages/TerminalPage.tsx#resolveSessionPreviewTargetFromDrawerSelection`; `src/hooks/useSessionOpenActions.ts#handleOpenGroupSession` | `android_preview:SessionDrawer->RemoteSessionOpenOwner`; `android_preview:RemoteSessionOpenOwner->PreviewSelectionOwner` | remote catalog row selection must materialize an open-session id through the existing drawer remote-open owner with no activation/navigation, then persist only the materialized local session target |
| terminal.session_preview.selection.append | `src/pages/TerminalPage.tsx#handleAddSessionPreview`; `src/lib/session-preview-selection.ts#appendSessionPreviewTarget` | `android_preview:PreviewAddMenu->PreviewSelectionOwner` | append exactly one currently open unselected target after tile removal, preserve existing order, and explicitly reject duplicate or seventh targets |
| terminal.session_preview.selection.replace | `src/lib/session-preview-selection.ts#replaceSessionPreviewTarget` | `android_preview:PreviewReplacementMenu->PreviewSelectionOwner` | replace one selected target in place with one valid currently unselected open-session target |
| terminal.session_preview.selection.persist | `src/lib/session-preview-selection.ts#readSessionPreviewSelection`; `#writeSessionPreviewSelection` | `android_preview:SessionDrawer->PreviewSelectionOwner` | explicit available/empty/invalid read and explicit write result |
| terminal.session_preview.selection.resolve_open_tabs | `src/lib/session-preview-selection.ts#resolveSessionPreviewTargets`; `#pruneSessionPreviewSelectionToOpenSessions` | `android_preview:PreviewSelectionOwner->OpenTabResolver` | project selection through current open-session truth only and physically remove stale or identity-mismatched targets |
| terminal.session_preview.gesture.begin | `src/lib/session-preview-gesture.ts#beginSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | arm only inside the right-edge admission band |
| terminal.session_preview.gesture.update | `src/lib/session-preview-gesture.ts#updateSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | lock one horizontal/vertical owner for the touch sequence |
| terminal.session_preview.gesture.finish | `src/lib/session-preview-gesture.ts#resolveSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | admit only threshold-crossing leftward horizontal motion |
| terminal.session_preview.live_set.project | `src/lib/session-preview-selection.ts#projectSessionPreviewLiveIds` | `android_preview:PreviewModeOwner->PreviewLiveSetProjector`; `android_preview:PreviewLiveSetProjector->SessionBodySubscriptionIntent` | union selected ids while preview is open; foreground lifecycle cannot rewrite the projection because body suppression and delayed live-set handoff belong to the dedicated background handoff owner |
| terminal.session_preview.grid.render | `src/components/terminal/WindowGroupLayout.tsx#WindowGroupLayout`; `#resolveWindowGroupLayoutPlan`; `src/components/terminal/TerminalPreviewGrid.tsx#TerminalPreviewGrid`; `src/components/TerminalView.tsx#TerminalView`; `src/components/terminal/TerminalPreviewRow.tsx#TerminalPreviewRow` | `android_preview:PreviewModeOwner->TerminalPreviewGrid`; `android_preview:TerminalPreviewGrid->WindowGroupLayout`; `android_preview:WindowGroupLayout->TerminalPreviewTile`; `android_preview:TerminalPreviewTile->SharedRenderSurface` | derive a primary-plus-children layout from 1-6 targets, keep one full ANSI/cursor live renderer in the primary slot, project each secondary tail as one passive row node with theme-colored style runs, promote child title/body taps without activation, activate the full shell only from the primary title/body, and never emit tmux/viewport/resize side effects from preview projection |
| terminal.session_preview.grid.replace_menu | `src/components/terminal/TerminalPreviewGrid.tsx#preview-replacement-menu` | `android_preview:TerminalPreviewTile->PreviewReplacementMenu`; `android_preview:PreviewReplacementMenu->PreviewSelectionOwner` | long press one tile, suppress activation, and expose only currently unselected open-session replacement commands |
| terminal.session_preview.tile.remove | `src/pages/TerminalPage.tsx#handleRemoveSessionFromPreview`; `src/lib/session-preview-selection.ts#removeSessionPreviewTarget` | `android_preview:TerminalPreviewTileClose->PreviewSelectionOwner` | remove only the selected preview target, persist the remaining order, and cancel preview when none remain without closing the Session |
| terminal.session_preview.grid.add_menu | `src/components/terminal/TerminalPreviewGrid.tsx#terminal-preview-add-menu` | `android_preview:PreviewAddMenu->PreviewSelectionOwner` | expose every currently open eligible Session absent from the preview and no selected Session |
| terminal.session_preview.tile.activate | `src/pages/TerminalPage.tsx#handleActivateSessionFromPreview`; `#handleActivateOpenSessionInViewport` | `android_preview:TerminalPreviewTile->ActiveSessionIntent`; `android_preview:ActiveSessionIntent->TerminalPage` | project the selected open session into the focused session-group viewport, emit one explicit switch, then close preview |
| terminal.session_preview.mode.cancel_restore | `src/pages/TerminalPage.tsx#handleCancelSessionPreview` | `android_preview:SystemBackIntent->PreviewModeOwner`; `android_preview:PreviewModeOwner->EntrySessionProjection` | consume Back only while preview is open, close preview, and restore the captured entry active/session-group projection |

## Daemon & CLI Owner Map

| command / entry | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| `zterm-daemon.sh run` | `scripts/zterm-daemon.sh` -> `src/server/server.ts` | tsx entry, `~/.wterm/daemon-runtime/server.cjs` staging, `node-pty` + `@roamhq/wrtc` vendor, `ZTERM_DAEMON_NATIVE` handoff | `src/server/server.daemon-runtime-truth.test.ts`; `daemon:install-global` |
| `zterm-daemon.sh start/stop/restart/status` | `scripts/zterm-daemon.sh` (`start_service` / `stop_service` / `restart_service` / `status*`) | launchd label `com.zterm.android.zterm-daemon`; legacy `com.zterm.android.daemon` and `com.wterm.mobile.daemon` cleanup; `wait_for_service_ready` port check; pid file `~/.wterm/run/zterm-daemon.pid` | `scripts/zterm-daemon.sh` smoke; `daemon:install-service` |
| `zterm-daemon.sh configure-relay` | `scripts/zterm-daemon.sh` (`configure_relay`) | writes `~/.wterm/config.json -> mobile.relay`; required flags `--relay-url/--username/--password\|--password-stdin\|--password-env/--host-id`; optional `--device-id/--device-name/--restart-service` | `src/server/daemon-config.test.ts` |
| `zterm-daemon.sh install-service` | `scripts/zterm-daemon.sh` (`install_service`) | prime macOS TCC preflight (file access + Screen Recording), install/refresh plist, `bootstrap_service`, port ready gate | `src/server/launch-agent-plist.test.ts`; `daemon:install-service` |
| `zterm-daemon.sh uninstall-service` | `scripts/zterm-daemon.sh` (`uninstall_service`) | launchd `bootout`, remove plist, remove legacy agents | `src/server/launch-agent-plist.test.ts` |
| `zterm-daemon.sh service-status` | `scripts/zterm-daemon.sh` (`status_service`) | `launchctl print` snapshot; reports `active count` + `last exit code` | `daemon:service-status` |
| `pnpm run daemon:mirror:close-loop` | `scripts/daemon-mirror-close-loop.ts` | tmux oracle + daemon runtime + client mirror + renderer close loop; required by `test:terminal:regression` | `scripts/daemon-mirror-close-loop.ts` self-test |
| `pnpm run daemon:runtime:analyze` / `:remote` / `:capture` | `scripts/analyze-runtime-debug.ts`; `scripts/runtime-debug-remote.ts`; `scripts/collect-runtime-audit.ts` | shared `parseArgs`/`parseCli` helper; reads `/debug/runtime` and `/debug/runtime/logs`; posts expiring lease mutations to `/debug/runtime/control`; never embeds payload | `terminal-debug-runtime.test.ts`; `server.debug-truth.test.ts`; `src/server/terminal-http-runtime.debug-observability.test.ts` |
| `pnpm run build:android` | `scripts/build-android-debug.sh` | `contracts/app-version.json` + `scripts/app-version.mjs` allocate normal `N` and rollback `N.1` inside one monotonic build slot; Vite build + cap sync + both Gradle APK variants + `prepare-update-bundle`; APK manifest version/hash + `update-dist/latest.json` + `~/.zterm/updates/` | `scripts/app-version.test.ts`; `scripts/verify-update-bundle.mjs`; `test:feature-registry`; `prebuild` runs `test:terminal:regression` |

## Mainline Source Index

`docs/wiki/mainline-source.md` is the canonical source for "where does this user-visible surface live". `docs/wiki/mainline-call-map.json` is the machine-readable call-map manifest whose node ids must align with that wiki. Both must stay in lockstep with this map. The `src/lib/function-wiki-truth.test.ts` gate fails when the wiki, the registry, the call-map manifest, and the mainline index disagree.

Rules:

- One feature has one owner surface. Shared helpers are allowed only when the registry names them as allowed paths.
- Forbidden paths are hard boundaries. Do not patch behavior there to make a feature pass indirectly.
- Required gates are the minimum verification stack, not a replacement for broader regression when the change has wider impact.

## Resource Binding Map

`docs/resource-registry.json` is the top-level machine-readable resource truth. `docs/resource-map.md` is the human review surface. `docs/module-registry.json` groups resources into project modules. `docs/edge-registry.json` declares allowed cross-module resource edges. This function map binds feature-local owners to declared global resources and edges only; it must not invent resource ids, module ids, or shortcut edges.

## Module / Edge Binding Map

`docs/module-registry.json` is the module ownership manifest. `docs/edge-registry.json` is the cross-module edge manifest. Runtime refactors must land module/edge changes before adding feature-local function bindings.

| refactor area | module ids | required edge ids | rule |
| --- | --- | --- | --- |
| daemon target physical connection | `client.daemon_connection`, `client.terminal_channel_mux`, `daemon.connection_gateway`, `daemon.channel_mux`, `daemon.transport_subscriber` | `edge.client.active_session_to_session_transport`, `edge.client.session_transport_to_daemon_target_transport`, `edge.client.daemon_target_transport_to_terminal_channel`, `edge.daemon.terminal_channel_to_subscriber` | one client physical connection per daemon target; session is channel, not socket owner. |
| terminal body truth | `daemon.mirror_writer`, `daemon.mirror_store`, `daemon.buffer_publisher`, `daemon.transport_subscriber`, `client.sparse_buffer`, `client.buffer_store`, `client.renderer_window`, `client.dom_renderer`, `client.terminal_shell`, `client.app_shell` | `edge.daemon.mirror_writer_to_backend_truth`, `edge.daemon.mirror_store_to_buffer_publisher`, `edge.daemon.buffer_publisher_to_transport_subscriber`, `edge.daemon.mirror_store_to_client_buffer_frame_assembly`, `edge.client_buffer_frame_assembly_to_client_sparse_buffer`, `edge.client_sparse_buffer_to_renderer`, `edge.client_renderer_to_ui_projection` | mirror writer owns validated source capture and snapshot commit writes; mirror store owns canonical revision/runtime scheduling; daemon.buffer_publisher owns per-subscriber pending/backpressure/head/frame-split publication; frame assembly owns atomic wire-frame publication and exact repair range; client.sparse_buffer owns absolute-row local truth; buffer store orchestrates planner/pull/repair; client.renderer_window owns visible range and immutable render snapshot; client.dom_renderer owns snapshot-to-DOM projection; client.terminal_shell owns stage shell and shell intents. |
| remote-window control | `client.remote_window_overlay`, `daemon.remote_window_stream` | `edge.client_ui_to_remote_window_overlay`, `edge.client_remote_window_overlay_to_touch_action`, `edge.client_touch_action_to_daemon_remote_window_stream`, `edge.daemon_remote_window_stream_to_daemon_process` | UI emits action records; daemon owns focus/capture/input injection; terminal mirror is forbidden video truth. |
| relay peer resume | `relay.peer_lease`, `client.connection_home`, `client.daemon_connection` | `edge.relay_peer_lease_to_transport_target`, `edge.relay_peer_lease_to_daemon_target_transport_via_target` | Relay lease is route/signaling truth only; no channel/subscriber/tmux/mirror/UI truth. |
| release/start | `release.update_artifact`, `release.daemon_artifact`, `daemon.runtime_entry` | `edge.release.update_artifact_to_daemon_artifact`, `edge.release.daemon_artifact_to_daemon_process` | runtime consumes deterministic promoted artifacts; no authoring-source execution path. |

| feature_id | resource_ids | relation notes |
| --- | --- | --- |
| `terminal.keyboard_ime` | `resource.platform_input_channel`, `resource.session_transport`, `resource.daemon_input_queue` | Input is captured by platform input owner, then sent through current session transport and daemon input queue. It must not write `resource.backend_session`, `resource.tmux_session`, or `resource.wezterm_pane` directly. |
| `client.input_normalizer` | `resource.client_input_normalizer` | Normalizes committed text only; it must not read or write session transport, daemon target transport, backend session, tmux, or mirror truth. |
| `terminal.schedule` | `resource.schedule_job`, `resource.backend_session` | Scheduled jobs dispatch only through backend session owner. |
| `terminal.remote_screenshot` | `resource.remote_screenshot`, `resource.backend_session`, `resource.debug_channel` | Screenshot request/result is daemon side state; debug can observe only. |
| `desktop.remote_window_stream` | `resource.remote_window_overlay`, `resource.remote_window_stream`, `resource.remote_window_canvas_raw`, `resource.remote_window_canvas_layout`, `resource.remote_window_canvas_encode`, `resource.remote_window_focus_stream`, `resource.transport_target`, `resource.debug_channel` | Android overlay projects picker/floating/fullscreen/video receiver state, aspect-fit fullscreen drawing, default fullscreen window-resize fill intent, and input intents only. Daemon/native stream truth owns catalog, coordinates, capture frame source, WebRTC media sender, receiver attach status, input target lease, cleanup, explicit policy errors, raw canvas composition, layout generation, canvas encode state, and optional focus stream. Terminal mirror/buffer/render resources are forbidden as video truth. |
| `terminal.open_tabs` | `resource.open_tab`, `resource.active_session` | Open tabs are explicit persisted client truth; transport/runtime facts must not close or merge them. |
| `terminal.transport_lifecycle` | `resource.active_session`, `resource.session_transport`, `resource.daemon_target_transport`, `resource.terminal_channel`, `resource.transport_target`, `resource.pending_open_intent`, `resource.transport_subscriber`, `resource.daemon_channel_mux`, `resource.daemon_buffer_publisher`, `resource.mirror_store`, `resource.debug_channel` | Active session owns current client identity; session transport maps local sessions to daemon-target physical transports and terminal channels; daemon-target transport owns one physical WebSocket/RTC data channel per target; terminal channel owns per-session wire identity; daemon channel mux owns channel registry mutation and release; daemon subscriber owns only channel body eligibility, physical send/backpressure truth, and send accounting; `daemon.buffer_publisher` owns bounded pending-latest and subscriber publication state; trace observes metadata only. |
| `terminal.daemon_input` | `resource.platform_input_channel`, `resource.client_reliable_input_queue`, `resource.session_transport`, `resource.daemon_input_queue`, `resource.backend_session`, `resource.tmux_session` | Client reliable input queue exclusively owns seq/ACK/retry control before the existing session transport. Daemon input queue remains the only write queue into backend sessions and tmux; retry control never enters terminal payload semantics. |
| `terminal.buffer_render` | `resource.tmux_session`, `resource.mirror_store`, `resource.daemon_buffer_publisher`, `resource.transport_subscriber`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window`, `resource.ui_projection`, `resource.shared_terminal_types`, `resource.debug_channel` | `daemon.mirror_writer` commits validated source snapshots; `daemon.mirror_store` owns canonical revision and runtime scheduling; `daemon.buffer_publisher` owns per-subscriber buffer-sync pending/backpressure/head/frame-split publication and sends only current mirror truth through physical transport subscribers; frame assembly publishes only complete continuous frames and owns exact bounded repair truth; client sparse buffer applies absolute rows; client.renderer_window owns visible demand and next-RAF commit only; client.dom_renderer consumes `resource.renderer_window` and `resource.ui_projection` for immutable snapshot-to-DOM projection; client.terminal_shell consumes renderer/DOM projections and emits shell intents only; trace observes metadata only. |
| `terminal.session_group_layout` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.renderer_window` | Layout projects platform surface and panes; it does not own terminal content layout. |
| `terminal.session_drawer` | `resource.ui_projection`, `resource.open_tab`, `resource.active_session` | Drawer displays the session catalog projection and emits intent; tmux and Herdr rows remain backend-qualified even when daemon and session names match, and it cannot open transport sockets directly. |
| `terminal.session_preview` | `resource.session_preview_selection`, `resource.session_preview_mode`, `resource.open_tab`, `resource.client_sparse_buffer`, `resource.renderer_window`, `resource.ui_projection`, `resource.active_session` | Preview resolves selected open tabs. Remote drawer catalog rows must first become `resource.open_tab` through the existing session-open owner; selection storage never stores remote placeholder ids. Preview projects existing immutable render truth and emits active-session intent only on tile activation; it cannot own transport or backend truth. |
| `terminal.quickbar` | `resource.ui_projection`, `resource.platform_input_channel` | Quickbar is shell/input projection; input still routes through platform input and session transport resources. |
| `terminal.workspace_panes` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.renderer_window` | Pane shell arranges projection and renderer containers; terminal layout remains backend/mirror truth. |
| `terminal.interaction_runtime` | `resource.ui_projection`, `resource.active_session` | Interaction runtime resolves active pane/session intent, then delegates to active session owner. |
| `terminal.shell_actions` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.renderer_window`, `resource.active_session`, `resource.session_transport` | `client.terminal_shell` consumes platform/UI/renderer projections and emits high-level intent; it must not bypass session transport owner or own sparse/render truth. |
| `connections.history_projection` | `resource.ui_projection`, `resource.open_tab` | Connections projection displays saved direct/Tailscale/history/runtime facts but does not create current tab truth and does not let Relay own saved Host truth. |
| `relay.account_directory` | `resource.relay_control_connection`, `resource.relay_account_directory`, `resource.debug_channel`, `resource.transport_target`, `resource.relay_peer_lease` | Relay control carries endpoint/presence/control facts on a physical socket separate from terminal data. Confirmed directory truth informs future target resolution and survives transient control failure, but cannot own an open terminal transport, terminal channel, subscriber, tmux, mirror, buffer, or UI truth. Relay peer lease may preserve a per-client-device route/signaling peer id for 30 minutes without storing application session state. |
| `relay.route_selection` | `resource.transport_target`, `resource.session_transport`, `resource.daemon_target_transport`, `resource.relay_peer_lease`, `resource.route_policy`, `resource.debug_channel` | Route selection produces target diagnostics and selected target facts with Auto priority `LAN -> Tailscale -> WebRTC UDP direct -> TURN Relay`; transport owner consumes them and records runtime route failures back into route health. A persisted success may seed a future generation only inside its product tier. When Relay resume is available, route selection consumes `resource.relay_peer_lease` only as route/signaling state, not as channel or session truth. |
| `terminal.transport_lifecycle` | `src/lib/client-control-directory-runtime.ts#ClientControlDirectoryRuntime`; `src/lib/client-control-plane-transport.ts#ClientControlPlaneTransport` | `android_mainline:RelayDirectoryProjection->ClientControlPlaneTransport`; `android_mainline:ClientControlPlaneTransport->TransportTargetResolver` | hold each new Relay-account daemon transport in `CONNECTING` until a fresh server-confirmed directory generation resolves the stable daemon id; cached UI state and disconnected control state cannot start data signaling. |
| `relay.directory_ui` | `resource.ui_projection`, `resource.transport_target` | Directory UI projects optional relay account/device route candidates while preserving direct saved-host and active-session entries; it emits explicit open/resume intent only. |
| `daemon.file_transfer` | `resource.client_file_browser`, `resource.target_mux_request`, `resource.daemon_target_transport`, `resource.transport_subscriber`, `resource.file_transfer`, `resource.backend_session` | Client file browser owns projection plus bounded local flow-control intent; daemon owns cumulative ACK and file truth. Wire frames traverse the target mux/transport subscriber path; the UI cannot write daemon files or bypass the native storage owner. |
| `daemon.mirror_writer` | `resource.terminal_backend`, `resource.mirror_store` | Validated source capture and authoritative mirror snapshot commit writes stay in this owner; `daemon.mirror_store` owns canonical revision and runtime scheduling. Writer never owns revision, subscriber publication, backend session truth, renderer, or client UI truth. |
| `shared.node_contract` | `resource.runtime_node_registry` | Registered node identities and lifecycle/disposal contracts are shared contract truth only; node contracts never own session, transport, mirror, buffer, renderer, UI, or backend business resources. |
| `shared.debug_contract` | `resource.debug_snapshot_registry` | Registered debug snapshot producers and versioned sequences are shared contract truth only; debug contracts never own session, transport, mirror, buffer, renderer, UI, or business payload resources. |
| `client.observability` | `resource.client_debug_hub`, `resource.observability_channel` | Client debug hub owns bounded snapshot/event/export truth and exports only through the dedicated observability channel; it never owns session, transport, mirror, sparse buffer, renderer, or UI truth. |
| `daemon.observability` | `resource.daemon_debug_hub`, `resource.observability_channel` | Daemon debug hub owns bounded event/snapshot/permission truth and serves only through the dedicated observability channel; it never owns active session, transport, mirror, sparse buffer, renderer, or UI truth. |
| `daemon.runtime_entry` | `resource.runtime_home`, `resource.daemon_process`, `resource.terminal_backend`, `resource.debug_channel`, `resource.observability_channel` | Daemon process wires runtime modules and health/debug endpoints; it must not own client active/session state. |
| `daemon.windows_wezterm_backend` | `resource.terminal_backend`, `resource.backend_session`, `resource.wezterm_pane`, `resource.mirror_store` | WezTerm backend owns pane capture/input adapter and feeds mirror store through backend capture. |
| `daemon.cli_shell` | `resource.daemon_runtime_artifact`, `resource.daemon_process`, `resource.release_update_artifact`, `resource.runtime_home` | CLI promotes deterministic runtime artifacts and starts/stops daemon process. |
| `daemon.cli_node` | `resource.debug_channel`, `resource.observability_channel`, `resource.daemon_process`, `resource.mirror_store`, `resource.transport_subscriber`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window` | Node CLI helpers and bounded performance trace observe capture/send/rx/assembly/apply/render metadata; HTTP observability is a dedicated default-deny POST-only side channel independent of session transport and must not store terminal text or become business truth writers. |
| `daemon.support` | `resource.release_update_artifact`, `resource.daemon_runtime_artifact` | Support scripts verify release/install prerequisites and artifacts before promotion. |
| `windows.desktop_shell` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.active_session`, `resource.session_transport`, `resource.client_sparse_buffer`, `resource.renderer_window` | Windows shell owns desktop projection/platform bridge only; terminal content still flows through shared transport, sparse buffer, and renderer owners. |
| `mainline_source.android` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.active_session`, `resource.session_transport`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window`, `resource.shared_terminal_types` | Android mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`; DOM renderer and terminal shell paths are part of the source-to-DOM mainline. |
| `mainline_source.daemon` | `resource.daemon_process`, `resource.terminal_backend`, `resource.backend_session`, `resource.mirror_store`, `resource.daemon_input_queue`, `resource.transport_subscriber`, `resource.debug_channel`, `resource.observability_channel` | Daemon mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`. |
| `mainline_source.cli` | `resource.release_update_artifact`, `resource.daemon_runtime_artifact`, `resource.daemon_process`, `resource.runtime_home` | CLI mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`. |
| `appsdk.binary_gate` | `resource.appsdk_compiler_identity` | Package prebuild, CI, and Android release consume one control resource: the exact PATH-selected compiler identity bound to project/lock version and digest. Record-graph verification is invoked through the same absolute executable; no product resource relation or second bare `appsdk` path exists. |
| `client.plugin_host` | `resource.client_plugin_host`, `resource.plugin_capability_registry`, `resource.plugin_ui_slot_registry` | App composition creates the plugin host; host installs/start/stops/disposes declared manifests; capability registry is shared contract truth and rejects duplicate or undeclared providers; UI slot registry provides unique typed plugin slots; plugins cannot read raw socket/store/backend/UI truth. |
| `client.debug_console` | `resource.plugin_ui_slot_registry`, `resource.debug_console_ui_contract`, `resource.shared_terminal_types` | The plugin host provides `terminal.debug-console` through the typed UI slot registry; App/TerminalPage consume the typed debug console contract and render only the plugin-provided debug overlay. |
| `client.session_drawer_ui` | `resource.session_drawer_ui_contract`, `resource.plugin_ui_slot_registry` | The plugin host provides `terminal.session-drawer` through the typed UI slot registry; App/TerminalPage consume the typed session drawer contract and render only the plugin-provided TerminalSessionDrawer projection. |
| `client.file_browser_ui` | `resource.file_browser_ui_contract`, `resource.plugin_ui_slot_registry` | The plugin host provides `terminal.file-browser` through the typed UI slot registry; App/TerminalPage consume the typed file browser contract and render only the plugin-provided FileTransferSheet projection. |
| `client.settings_update_ui` | `resource.settings_update_ui_contract`, `resource.plugin_ui_slot_registry` | The plugin host provides `settings.update` through the typed UI slot registry; App/SettingsPage consume the typed settings update contract and render only the plugin-provided AppUpdateSection projection. |
| `client.remote_window_ui` | `resource.remote_window_ui_contract`, `resource.plugin_ui_slot_registry` | The plugin host provides `terminal.remote-window` through the typed UI slot registry; App/TerminalPage consume the typed remote window contract and render only the plugin-provided RemoteWindowOverlay projection. |
| `client.quickbar_ui` | `resource.quickbar_ui_contract`, `resource.plugin_ui_slot_registry`, `resource.platform_input_channel` | The plugin host provides `terminal.quickbar` through the typed UI slot registry; App/TerminalPage consume the typed quickbar contract and render only the plugin-provided TerminalQuickBar projection. The contract binds only `resource.platform_input_channel` prop types. |
| `client.terminal_shell_ui` | `resource.terminal_shell_ui_contract`, `resource.plugin_ui_slot_registry` | The plugin host provides `terminal.shell` through the typed UI slot registry; App/TerminalPage consume the typed terminal shell contract and render only the plugin-provided TerminalShellUiProps projection. |
| `client.composition_root` | `resource.client_composition_root`, `resource.client_plugin_host` | App binds the declared `plugin-host` port through the composition root; duplicate/unbound/missing ports fail before use; composition root does not own transport, session, buffer, renderer, plugin implementation, or business truth. |
| `client.control_center` | `resource.client_control_center`, `resource.client_plugin_host` | App routes plugin host disposal through the control center; one command type routes to exactly one owner, capability denial occurs before the owner runs, duplicate idempotency keys never re-run, deadlines return explicit errors, and audit stays bounded. The control center never stores terminal/file/media body truth. |
| `daemon.control_center` | `resource.daemon_control_gateway`, `resource.daemon_control_center`, `resource.schedule_job`, `resource.backend_session` | Daemon message dispatch sends schedule/tmux control through the gateway; the control center routes exactly one owner per command type, gates capability before owner execution, and keeps bounded audit. The center never stores terminal/file/media body truth. |
| `daemon.session_catalog` | `resource.daemon_session_catalog`, `resource.backend_session`, `resource.session_idle_facts` | Daemon catalog builds backend-qualified session rows and list-time idle facts through the session idle facts owner; it must not own client active/session, mirror, transport, renderer, or UI truth. |

### daemon.session_catalog

| command / entry | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| list-sessions control dispatch | `src/server/daemon-session-catalog-runtime.ts#handleListSessionsMessageRuntime` | route list-sessions through gateway to catalog, publish `sessions` and list-time `session-activity`, explicit `list_sessions_failed` | `src/server/daemon-session-catalog-runtime.test.ts`; `src/server/daemon-session-catalog-ownership.test.ts` |
| catalog construction/republish | `src/server/daemon-session-catalog-runtime.ts#buildSessionsCatalogPayload` | backend-qualified `sessionCatalog`, legacy `sessions`, schedule control republish | `src/server/daemon-session-catalog-runtime.test.ts`; `src/server/daemon-session-catalog-ownership.test.ts` |
### daemon.buffer_publisher

| command / entry | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| subscriber buffer-sync pending/backpressure | `src/server/daemon-buffer-publisher-runtime.ts#createDaemonBufferPublisherRuntime` | queue latest per subscriber, merge/collapse absolute ranges, hold on transport backpressure, explicit flush statuses, stale-transport resync | `src/server/daemon-buffer-publisher-runtime.test.ts`; `src/server/terminal-mirror-runtime.test.ts`; `src/server/terminal-mirror-runtime.backpressure.test.ts` |
| head broadcast and frame split | `src/server/daemon-buffer-publisher-runtime.ts#createDaemonBufferPublisherRuntime` | fresh-revision head fanout cache, per-requester replies, oversized contiguous same-revision split for every body span including fresh attach | `src/server/daemon-buffer-publisher-runtime.test.ts`; `src/server/terminal-buffer-sync-wire.test.ts` |

### daemon.session_idle_detection

| command / entry | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| `session-activity idle classification` | `src/server/terminal-session-activity-runtime.ts#classifySessionActivities` | derive stopped status from mirror.lastLiveActivityAt, threshold `SESSION_IDLE_STOPPED_THRESHOLD_MS = 10_000`, guard `lastLiveActivityAt > 0` | `src/server/terminal-session-activity-runtime.test.ts` |
| `session-activity target control publisher` | `src/server/terminal-session-activity-runtime.ts#publishSessionActivitiesRuntime` | publish raw control message only before mux negotiation; after `mux-hello`, publish only `mux-target-message`; never use `mux-channel-message` | `src/server/terminal-session-activity-runtime.test.ts` |
| `session-activity heartbeat broadcast` | `src/server/terminal-daemon-runtime.ts#startHeartbeatLoop` | call the publisher once per open physical transport with subscribers; do not construct wire envelopes | `src/server/terminal-daemon-runtime.test.ts` |
| `session-activity attach-time send` | `src/server/daemon-session-catalog-runtime.ts#handleListSessionsMessageRuntime`, `src/server/terminal-message-control-runtime.ts#handleMuxChannelOpenedMessageRuntime` | call the publisher after list/open; do not construct wire envelopes | `src/server/terminal-message-runtime.test.ts` |

| feature_id | resource_ids | relation notes |
| --- | --- | --- |
| `daemon.session_idle_detection` | `resource.mirror_store`, `resource.session_idle_facts`, `resource.transport_subscriber` | Idle facts are derived from mirror activity timestamps and published over the control channel. Daemon does not own client notification truth. |
| `daemon.attachment_delivery` | `resource.attachment_store`, `resource.attachment_delivery`, `resource.daemon_process` | Agent enqueue enters an atomic daemon outbox. Original and preview are verified independently. Delivery receipts are per target device and never consume another device's pending item. |

| `daemon.attachment_delivery` | `src/server/attachment-delivery-runtime.ts#createAttachmentDeliveryRuntime` | Sole owner for atomic image enqueue, preview/original integrity, per-device receipts, 48-hour expiry, and tombstone cleanup. |
| `daemon.attachment_delivery` | `src/server/terminal-attachment-message-runtime.ts#createTerminalAttachmentMessageRuntime` | Message-side owner for pending/history/asset/receipt wire projection; `terminal-message-runtime.ts` routes these four message types only. |
| `daemon.attachment_delivery` | `src/server/terminal-http-runtime.ts#createTerminalHttpRuntime` | Authenticated HTTP projection for enqueue, missed-delivery listing, asset reads, and receipts; IDs are validated before filesystem access. |
| `daemon.attachment_delivery` | `scripts/zterm-send-image.mjs` | Agent-facing CLI caller; sends image bytes to the daemon API and does not write daemon truth directly. |
