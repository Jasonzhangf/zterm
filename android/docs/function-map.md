# zterm Android Function Map

This map is the human-readable index for `docs/feature-registry.json`.

Before changing a feature, locate its `feature_id`, read its owner paths, and run its required gates. If no row exists, add the feature to the registry and gate it before touching behavior.

| feature_id | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| `terminal.copy_mode` | `src/pages/useTerminalPageCopyRuntime.ts`; `native/android/app/src/main/java/com/zterm/android/MainActivity.java` | copy runtime, TerminalPage wiring, `TerminalView` pointer/touch long-press ownership, `VisibleRow` selection/menu guard, header state, Android native WebView long-clickable gate | `src/pages/useTerminalPageCopyRuntime.test.tsx`; `system-copy-state-machine.test.tsx`; `system-copy-longpress-regression.test.tsx`; `TerminalView.selection-guard.test.tsx`; `VisibleRow.selection.test.tsx` |
| `terminal.quickbar` | `src/components/terminal/TerminalQuickBar.tsx`; `packages/shared/src/shortcuts/terminal-shortcut-composer.ts` | quickbar UI, terminal page shell wiring, shared shortcut composer encoding/decoding, workspace/layout profile | `TerminalQuickBar.test.tsx`; `packages/shared/src/shortcuts/terminal-shortcut-composer.test.ts`; `TerminalPage.real-quickbar-split.test.tsx` |
| `terminal.keyboard_ime` | `src/pages/terminal-keyboard-lift.ts`; `src/pages/TerminalPage.tsx`; `src/pages/TerminalPageStageShell.tsx`; `src/components/terminal/TerminalQuickBar.tsx`; `src/lib/terminal-input-normalization.ts`; `src/lib/android-ime-anchor-truth.test.ts`; `native/android/app/src/main/java/com/zterm/android/ImeAnchorPlugin.java`; `native/android/app/src/main/java/com/zterm/android/ImeAnchorInputLogic.java` | IME lift, keyboard visibility, native anchor explicit show/disable policy, viewport safe-area, quickbar focus handoff, Android committed-text normalization, stage shell visual lift even when quickbar editor owns DOM focus, QuickBar measured chrome height under external IME lift, listener cleanup, adjustPan fallback to reported keyboard inset when viewport metrics do not expose IME occlusion | `terminal-keyboard-lift.test.ts`; `terminal-input-normalization.test.ts`; `android-ime-anchor-truth.test.ts`; `TerminalPage.android-ime.test.tsx`; `ImeAnchorInputLogicTest`; `ImeAnchorHardwareKeyMappingTest`; `TerminalPage.lifecycle-cleanup.test.tsx`; `TerminalPageStageShell.pane-stage.test.tsx`; `TerminalQuickBar.test.tsx` |
| `terminal.schedule` | `src/server/schedule-*.ts` | daemon schedule store/engine/dispatch and page editor wiring | schedule store/engine/dispatch tests; `server.schedule-truth.test.ts` |
| `terminal.remote_screenshot` | `src/lib/remote-screenshot-runtime.ts`; `src/server/terminal-file-transfer-list-runtime.ts`; `src/server/remote-screenshot-daemon.ts`; daemon control runtime | screenshot request aggregation, optional remote-window target manifest request, file-transfer bridge, page preview/save, Android local save, daemon native `capture-screen` window-id/rect capture | runtime/page screenshot tests; `src/server/terminal-file-transfer-list-runtime.test.ts`; file-transfer truth gate |
| `desktop.remote_window_stream` | `src/server/remote-window-stream-daemon.ts`; `packages/shared/src/connection/protocol.ts`; `src/components/terminal/RemoteWindowOverlay.tsx`; `src/components/terminal/WindowGroupLayout.tsx`; `src/lib/remote-window-overlay-runtime.ts`; `src/lib/remote-window-touch-action-runtime.ts`; `src/lib/remote-window-message-runtime.ts`; `src/lib/remote-window-receiver-runtime.ts`; `src/contexts/session-context-remote-window-runtime.ts`; `docs/decisions/2026-07-19-remote-window-stream-truth.md` (`binding pending` only for remaining live Android/iTerm2 proof) | remote-window target catalog protocol, daemon/native macOS app-window catalog, iTerm2 pane catalog, split-tree flatten, coordinate manifest with daemon display bounds, tmux reverse lookup, explicit catalog errors, Android picker/floating/fullscreen overlay projection, collapsed same-app picker rows plus active video-layer primary-plus-children sibling window switcher inside the same locked video container with screenshot thumbnail child previews, portrait child rail above the primary video and landscape child rail beside it, TerminalPage-measured QuickBar + IME chrome avoidance for the whole locked container, toolbar-owned floating drag, edge-resized floating preview projection, remote-window screenshot intent over the terminal.remote_screenshot transfer owner without focus, daemon-wide target catalog cache keyed by daemon identity with 60-second expiry, target catalog request/response aggregation over the existing session transport, stream start/stop/input/window-resize intents, client touch/action classification separated from overlay projection, route-derived WebRTC ICE/media negotiation, daemon ScreenCaptureKit frame source with low-latency queue depth, Android receiver attach and WebRTC stats sampling, rendered video surface projection, fullscreen aspect-fit drawing with default remote target resize fill request on fullscreen entry, zoom/pan projection with no minimap overlay and no shrink below fit, floating-preview low bitrate plus desktop-area-proportional video quality projection with 30 FPS default / 60 FPS fullscreen ceiling and stats-driven downgrade/restore, generic bring-to-focus OS input injection, and exact stream cleanup | `src/lib/remote-window-message-runtime.test.ts`; `src/lib/remote-window-overlay-runtime.test.ts`; `src/lib/remote-window-touch-action-runtime.test.ts`; `src/lib/remote-window-touch-action-sop.test.ts`; `src/lib/remote-window-receiver-runtime.test.ts`; `src/components/terminal/WindowGroupLayout.test.tsx`; `src/components/terminal/RemoteWindowOverlay.test.tsx`; `src/pages/TerminalPage.remote-window-overlay.test.tsx`; `src/contexts/session-context-remote-window-runtime.test.ts`; `src/contexts/session-context-socket-message-runtime.test.ts`; `src/server/remote-window-stream-daemon.test.ts`; `src/server/terminal-message-runtime.test.ts`; `src/lib/remote-window-video-quality.test.ts`; `docs/testing/remote-window-stream-test-design.md`; resource/function/mainline gates |
| `terminal.open_tabs` | `src/hooks/useOpenTabRuntime.ts`; `src/hooks/useOpenTabLifecycleEffects.ts`; `src/lib/open-tab-persistence.ts` | in-process open-tab projection, explicit open/close policy, foreground/resume native listener lifecycle, and one-way legacy persisted-tab cleanup; no cold restore, tab-list persistence, or render-driven Capacitor listener churn | `src/hooks/useOpenTabLifecycleEffects.test.tsx`; `open-tab-history-truth.test.ts`; persistence/intent/runtime tests; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `terminal.transport_lifecycle` | `packages/shared/src/connection/protocol.ts`; `src/contexts/session-context-transport-runtime.ts`; `src/contexts/session-context-session-runtime.ts`; `src/contexts/session-context-infra-runtime.ts`; `src/contexts/session-context-infra-facade-runtime.ts`; `src/contexts/session-context-tmux-management-runtime.ts`; `src/contexts/session-context-socket-runtime.ts`; `src/contexts/session-context-socket-message-runtime.ts`; `src/contexts/session-transport-open-helpers.ts`; `src/contexts/session-reconnect-helpers.ts`; `src/lib/session-transport-runtime.ts`; `src/lib/tmux-sessions.ts`; `src/pages/TerminalPage.tsx` (status projection only); `src/server/terminal-daemon-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/server/rtc-bridge.ts`; `src/server/terminal-mirror-runtime.ts`; `src/server/terminal-transport-runtime.ts`; `src/server/terminal-runtime-types.ts`; `native/android/app/src/main/java/com/zterm/android/BackgroundService.java`; `native/android/app/src/main/AndroidManifest.xml` | SessionContext transport/session lifecycle, route-aware traversal socket factory, daemon-target physical transport reuse, mux capability handshake, terminal channel open/close/message demux, target-level tmux management over existing `mux-target-message` request/response truth, metadata-only transport diagnostics projection, physical socket heartbeat health, daemon stale bound channel/transport cleanup, channel body-subscription intent, socket receive, transport reuse planner, semantic session reuse across route-identity upgrades, target-scoped tmux management transport reuse with FIFO serialization, daemon WebRTC bridge ICE policy consumption, daemon channel subscriber/body eligibility, per-channel bounded pending-latest backpressure, unified send accounting, portrait top status actual-route projection with up/down rates only and manual Auto/direct/WebRTC route action wiring through the session-open owner, top-level fixed UI projection for network/reconnect status that must not change terminal or remote-window layout size, and Android background notification policy that must not acquire CPU WakeLock or request battery-optimization bypass; legacy `wsUrl` override is allowed only for non-Relay/non-route-aware hosts and must not bypass route truth; tmux management over a separate `tmux-sessions.ts` transport is allowed only when no matching non-closed open Session/target exists, carries no result cache, and must not hide physical/protocol failure or timeout; daemon types must not retain client active/visible/viewport policy | transport lifecycle/runtime tests; mux protocol/channel tests; `session-context-tmux-management-runtime.test.ts`; `session-context-infra-runtime.test.ts`; `tmux-sessions.test.ts`; `session-context-socket-runtime.test.ts`; `SessionContext.ws-refresh.test.tsx`; `session-context-session-runtime.test.ts`; `session-context-activity-runtime.test.ts`; `session-sync-helpers.test.ts`; `server.daemon-runtime-truth.test.ts`; `server.transport-lifecycle-truth.test.ts`; `terminal-daemon-runtime.test.ts`; `terminal-transport-runtime.test.ts`; `terminal-mirror-runtime.test.ts`; `terminal-runtime.detached-session.test.ts`; `terminal-message-runtime.test.ts`; `rtc-bridge.test.ts`; `src/pages/TerminalPage.network-banner.test.tsx`; `src/lib/android-power-policy.test.ts` |
| `terminal.daemon_input` | `src/server/terminal-message-runtime.ts`; `src/server/terminal-control-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/contexts/session-context-input-runtime.ts`; `packages/shared/src/connection/protocol.ts` | daemon input receive/drop/write queue, old-daemon string-only compatibility, reliable input capability advertisement, client seq/ack/retry queue, daemon seq dedupe, retryable nack preservation for stale transport/session-required, non-retryable invalid/oversize rejection, long input frame and tmux write chunking, transport drain, bridge attach/input/message lane scheduling, per-transport input lane bypass of older input work while attach barriers still hold, live-activity scheduling | `session-context-input-runtime.test.ts`; `terminal-bridge-runtime.test.ts`; `terminal-message-runtime.test.ts`; `terminal-control-runtime.input-queue.test.ts`; `terminal-mirror-runtime.test.ts`; scheduler/control/bridge truth gates |
| `terminal.buffer_render` | `packages/shared/src/connection/types.ts`; `src/server/terminal-mirror-capture.ts`; `src/server/terminal-mirror-runtime.ts`; `src/contexts/session-wire-helpers.ts#normalizeIncomingBufferPayload`; `src/contexts/session-buffer-frame-assembly.ts#assembleBufferSyncFrameChunk`; `src/contexts/session-buffer-frame-assembly.ts#expireBufferSyncFrameAssembly`; `src/contexts/session-buffer-frame-assembly-state.ts#clearPendingBufferSyncFrameAssembly`; `src/contexts/session-buffer-frame-assembly-state.ts#resetBufferSyncFrameAssemblyEpoch`; `src/contexts/session-buffer-frame-assembly-state.ts#hasBufferFrameRepairDispatch`; `src/contexts/session-buffer-frame-assembly-state.ts#recordBufferFrameRepairDispatch`; `src/contexts/session-context-buffer-runtime.ts#applyIncomingBufferSyncRuntime`; `src/contexts/session-context-buffer-runtime.ts#applyResolvedBufferSyncPayloadRuntime`; `src/lib/session-render-gate.ts`; `src/lib/session-render-buffer-store.ts`; `src/lib/terminal-follow-scroll-runtime.ts` | single-writer tmux capture/full reconciliation and authoritative range patch, mirror revision broadcast, oversized body-refresh chunking with authoritative frame identity, unique wire normalization that preserves frame identity, shared protocol bounds for span/chunks/retained bytes/lifetime, bounded client frame assembly with exact continuous coverage, bounded per-session repair dispatch ledger retained across tab/transport resets, reset only on authoritative daemon revision epoch reset, and deleted with the local session, explicit per-session frame error truth plus at most one repair attempt per retained revision epoch, one resolved sparse apply/render publication without recursive ingress, next-RAF render gate, renderer publication store, TerminalView follow/reading scroll state machine, and source-to-DOM black-box tests | frame assembly positive/negative limit/expiry/ledger/epoch tests, socket normalization, capture/oracle, mirror runtime/backpressure, buffer runtime, render gate/store, follow-scroll runtime, TUI content, TerminalView dynamic refresh, and multi-pane tests |
| `terminal.workspace_panes` | `src/hooks/useTerminalWorkspace.ts`; `src/pages/TerminalPageStageShell.tsx` | pane split/layout/workspace persistence/shared PaneStage | workspace pane tests; `TerminalPageStageShell.pane-stage.test.tsx` |
| `terminal.session_group_layout` | `src/lib/terminal-layout-profile.ts`; `src/lib/session-group-viewport.ts`; `src/pages/TerminalPage.tsx#resolveTerminalSessionGroupActiveSessionProjection`; `src/pages/TerminalPageStageShell.tsx`; `packages/shared/src/connection/bridge-settings.ts` | app-layer layout/projection framework for existing session pane containers; Phase 1 keeps phone baseline, Phase 2 changes layout modes, Phase 3 adds cross-screen projection/virtualization; `session-group-viewport.ts` owns fixed-slot to boundary-visible viewport projection; `TerminalPage` owns active-session-to-slot synchronization before `StageShell` render so active id and body source cannot split; `terminalSessionGroupLayoutMode` stores the user axis preference while aspect-ratio policy remains the default truth | `terminal-layout-profile.test.ts`; `bridge-settings.test.ts`; `SettingsPage.theme.test.tsx`; `TerminalPage.session-content-identity.test.tsx`; `TerminalPage.session-drawer.test.tsx`; `TerminalPageStageShell.pane-stage.test.tsx`; `useTerminalWorkspace.test.tsx`; `docs/testing/terminal-session-group-layout-test-design.md` |
| `terminal.interaction_runtime` | `src/pages/useTerminalPageInteractionRuntime.ts` | active pane session routing, pane attach intent, swipe/tab isolation | `TerminalPage.tab-isolation.test.tsx`; `TerminalPage.multi-pane-decouple.test.tsx` |
| `terminal.session_drawer` | `src/components/terminal/TerminalSessionDrawer.tsx`; `src/pages/TerminalPage.tsx` (drawerServerIdentityAliases/drawerSessions useMemo); `src/lib/server-identity.ts`; `src/lib/traversal-relay-devices.ts`; `src/App.tsx` (saved/Home alias wiring only) | drawer UI, content-sized scrollable session list/footer layout, canonical hostKey/hostLabel injection, saved-server identity aliases, online Relay-directory direct-endpoint aliases, and unambiguous online rtc-only Relay/session-catalog aliases into one daemon host rail; disconnected/stale Relay daemon records are filtered from host rails and target lookup; ambiguous catalogs remain separate; Relay-owned catalog rows, including already-open stale direct rows, route through the session-open owner with route-aware target truth instead of raw switch-only bypass; TerminalPage memo identity includes Relay device endpoint and session-catalog inputs plus saved/Home alias inputs; opened-first unopened-sorted ordering, multi-host rail, swipe-close gesture, and row-selection press ownership that rejects the opening gesture's synthetic click | `TerminalSessionDrawer.test.tsx`; `TerminalPage.session-drawer.test.tsx`; `traversal-relay-devices.test.ts`; `server-identity.test.ts`; `docs/testing/terminal-session-drawer-gesture-test-design.md` |
| `terminal.session_preview` | `src/lib/session-preview-selection.ts`; `src/lib/session-preview-gesture.ts`; `src/components/terminal/WindowGroupLayout.tsx`; `src/components/terminal/TerminalPreviewGrid.tsx`; `src/pages/TerminalPageStageShell.tsx`; `src/pages/TerminalPage.tsx` | ordered 1-6 open-session selection, remote drawer catalog materialization through the existing session-open owner before selection persistence, shared primary-plus-children window-group layout projection, in-place long-press replacement, preview-target removal, right-edge gesture admission, temporary preview live-set projection, read-only shared-renderer grid with local scroll/pan, child-tile promotion without activation, primary-tile activation intent, and cancel-to-entry projection | `src/components/terminal/WindowGroupLayout.test.tsx`; `session-preview-selection.test.ts`; `session-preview-gesture.test.ts`; `TerminalPreviewGrid.test.tsx`; `TerminalPreviewGrid.render-truth.test.tsx`; `TerminalPage.session-preview.test.tsx`; `scripts/session-preview-source-dom-gate.tsx`; `docs/testing/terminal-session-preview-test-design.md` |
| `terminal.shell_actions` | `src/pages/useTerminalPageShellActionsRuntime.ts` | quick picker, tab manager scope, viewport mode routing | `useTerminalPageShellActionsRuntime.test.tsx` |
| `connections.history_projection` | `src/hooks/useSessionHistoryStorage.ts`; `packages/shared/src/react/use-host-storage.ts`; `src/components/settings/RememberedServersSection.tsx` | saved direct/Tailscale connection truth and bridge server presets remain independent from Relay; session-group history retains normalized Relay endpoint candidates so a later drawer open keeps the selected daemon route; Settings owns server preset configuration; Home projects one row per server and must not display/manage Session groups | open-tab/history truth; `src/hooks/useSessionHistoryStorage.test.ts`; `src/pages/ConnectionsPage.test.tsx`; `src/pages/SettingsPage.relay-account.test.tsx`; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `connections.config_share` | `packages/shared/src/connection/connection-config-share.ts`; `src/components/tmux/TmuxSessionPickerSheet.tsx`; `src/pages/ConnectionPropertiesPage.tsx` | canonical connection share payload/link parser; the real `+` add-flow sheet is the primary import/share/QR/scan UI projection; Connection Properties may also project this shared link/QR; import must go through `useHostStorage.upsertHost`; Android native scheme only opens the same parser | `packages/shared/src/connection/connection-config-share.test.ts`; `src/components/tmux/TmuxSessionPickerSheet.test.tsx`; `src/lib/connection-config-share-android-truth.test.ts`; `docs/testing/connection-config-share-test-design.md` |
| `settings.config_transfer` | `src/hooks/useConfigExport.ts`; `src/hooks/useAppUpdate.ts`; `src/lib/app-update.ts`; `src/lib/app-update-runtime.ts`; `src/lib/app-update-relay-manifest.ts`; `src/pages/SettingsPage.tsx`; `src/components/settings/AppUpdateSection.tsx`; `src/App.tsx`; `packages/shared/src/interaction/projection.ts` | settings config export/import writes and reads the canonical config file path, returns explicit success/error results, excludes session/runtime state, and App surfaces those results to the user instead of silent console-only success; app-update runtime is the single owner for manifest URL source (`user-saved` / `relay-injected` / `server-connected`), so Relay-derived update URLs can replace stale private daemon URLs while explicit user-saved URLs remain authoritative; Settings route candidates expose Relay public update route and saved/direct daemon routes without making direct/Tailscale the only check path | `src/hooks/useConfigExport.test.tsx`; `src/hooks/useAppUpdate.test.tsx`; `src/lib/app-update.test.ts`; `src/lib/app-update-relay-manifest.test.ts`; `src/lib/app-update-runtime.test.ts`; `src/pages/SettingsPage.theme.test.tsx`; `src/components/settings/AppUpdateSection.test.tsx`; `docs/testing/config-transfer-test-design.md` |
| `relay.account_directory` | `src/traversal-relay/store.ts`; `src/traversal-relay/server.ts`; `src/server/relay-client.ts`; `src/lib/relay-account-directory.ts`; `src/lib/traversal-relay-client.ts`; `scripts/prepare-relay-server-npm-package.mjs`; `scripts/verify-relay-server-package.mjs`; `packages/shared/src/connection/relay-directory.ts` | account-scoped machine / daemon / endpoint / tmux session directory; relay server stores and broadcasts directory/control facts and serves public route-scoped Android update assets from `ZTERM_TRAVERSAL_UPDATES_DIR` at `/relay/updates/*`; daemon publishes tmux truth snapshots; Android reads directory as remote-machine truth and refreshes `/api/auth/me` control payload before consuming device stream / TURN settings | `src/traversal-relay/store.test.ts`; `src/traversal-relay/server.test.ts`; `src/traversal-relay/relay-server-release-package.test.ts`; `src/server/relay-client.test.ts`; `src/lib/relay-account-directory.test.ts`; `src/lib/traversal-relay-client.test.ts`; `scripts/traversal-relay-local-smoke.ts`; `docs/testing/relay-account-directory-test-design.md` |
| `relay.route_selection` | `src/lib/traversal/route-selector.ts`; `src/lib/traversal/route-health-cache.ts`; `src/lib/traversal/config.ts`; `src/lib/traversal/socket.ts`; `src/lib/traversal/route-diagnostics.ts`; `src/lib/bridge-settings.ts`; `src/pages/TerminalPageDebugOverlay.tsx`; `packages/shared/src/connection/bridge-settings.ts`; `docs/decisions/2026-07-21-auto-route-and-daemon-release-truth.md` | explicit route candidates, Auto priority `Tailscale/direct websocket -> WebRTC UDP direct/hole-punch -> TURN/Relay`, stale saved traversal priority ignored in Auto, direct IP endpoint facts still selectable when higher-priority route classes are unavailable, probe results, RTT / auth / freshness scoring, selected route diagnostics, selected ICE pair diagnostics, five-minute success/auth truth, one-second ordinary-failure circuit-breaker cooldown followed by fresh probe eligibility, and opened-route close/heartbeat/mux failure recording so the next physical generation can move routes without permanently blacklisting a recovered network | `src/lib/traversal/route-selector.test.ts`; `src/lib/traversal/route-health-cache.test.ts`; `src/lib/traversal/config.test.ts`; `src/lib/traversal/socket.test.ts`; `src/pages/TerminalPageDebugOverlay.test.tsx`; `docs/testing/relay-account-directory-test-design.md`; `docs/testing/session-transport-network-switch-test-design.md` |
| `relay.directory_ui` | `src/lib/home-connection-projection.ts`; `src/lib/relay-device-stream-runtime.ts`; `src/hooks/useRelayDeviceStream.ts`; `src/lib/traversal-relay-devices.ts`; `src/App.tsx` (relay stream hook wiring only); `src/pages/ConnectionsPage.tsx`; `src/components/settings/RelayAccountSettingsSection.tsx`; `src/pages/SettingsPage.tsx`; `src/hooks/useTraversalRelayAccount.ts`; `src/components/tmux/TmuxSessionPickerSheet.tsx`; `src/hooks/useSessionOpenActions.ts` (Home row open intent only) | fixed-domain relay account login belongs to Settings as optional route/sync assurance; App relay stream bootstrap must refresh account control truth before opening the device stream, must not use stale stored TURN/WS settings, and must preserve route-bearing directory endpoint/session truth when later legacy devices snapshots carry only presence; Home projects connectable server rows from saved direct/Tailscale, bridge presets, active Sessions, and online relay directory daemon devices only; disconnected/stale relay devices remain directory facts but are not connectable rows; same-daemon saved rows merge Relay route candidates without replacing saved direct identity; rows with `relay-rtc` expose explicit Auto and Relay open intents; Home row tap opens last-entered remote session when still present, otherwise first live remote session, and creates generated tmux only when remote list is empty; logged-out Home still shows saved direct/Tailscale entries; Session management remains in the terminal drawer/picker | `src/App.relay-stream-lifecycle.test.tsx`; `src/lib/home-connection-projection.test.ts`; `src/lib/traversal-relay-devices.test.ts`; `src/lib/traversal-relay-client.test.ts`; `src/hooks/useTraversalRelayAccount.test.tsx`; `src/hooks/useSessionOpenActions.test.tsx`; `src/pages/ConnectionsPage.test.tsx`; `src/pages/SettingsPage.relay-account.test.tsx`; `src/components/tmux/TmuxSessionPickerSheet.test.tsx`; `docs/testing/relay-login-home-and-ephemeral-tabs-test-design.md` |
| `daemon.file_transfer` | `contracts/file-transfer-throughput.json`; `src/components/terminal/FileTransferSheet.tsx#dispatchUploadChunk`; `#writeDownloadChunkBatch`; `src/lib/file-transfer-session-runtime.ts`; `src/lib/file-transfer-message-runtime.ts`; `src/lib/file-transfer-throughput-runtime.ts#sendBoundedFileUploadChunks`; `#writeFileTransferChunkBatches`; `src/plugins/StoragePermissionPlugin.ts`; `native/android/app/build.gradle`; `native/android/app/src/main/java/com/zterm/android/StoragePermissionPlugin.java#writeFileChunks`; `native/android/app/src/main/java/com/zterm/android/StorageFileReadLogic.java`; `native/android/app/src/main/java/com/zterm/android/StorageFileWriteLogic.java#writeChunks`; `src/server/terminal-file-transfer-binary-runtime.ts#handleFileUploadChunk`; `#handleFileUploadEnd`; `src/server/terminal-file-transfer-list-runtime.ts#handleFileDownloadRequest` | client file browser projection; one machine-readable throughput contract; one bounded upload-window owner; explicit upload/native-write callback bindings; native external-storage chunk read/batch-write boundary; daemon cumulative upload ACK and exact completion truth; daemon download wire emission | `FileTransferSheet.test.tsx`; `file-transfer-session-runtime.test.ts`; `file-transfer-throughput-contract.test.ts`; `file-transfer-throughput-runtime.test.ts`; `terminal-file-transfer-binary-runtime.test.ts`; `file-transfer-throughput-loopback.test.ts`; `StorageFileReadLogicTest`; `StorageFileWriteLogicTest`; canonical file-transfer truth gate; real transport/ADB SHA-256 gate pending |
| `daemon.runtime_entry` | `src/server/server.ts`; `src/server/daemon-config.ts`; `src/server/daemon-runtime-truth/*` | tmux/transport/mirror/control/schedule/relay/file-transfer orchestrator launched by `scripts/zterm-daemon.sh run`; must remain the only long-running daemon process | `src/server/server.daemon-runtime-truth.test.ts`; `src/server/server.core-support-truth.test.ts`; `src/server/server.http-truth.test.ts`; `src/server/server.debug-truth.test.ts` |
| `daemon.windows_wezterm_backend` | `src/server/wezterm-backend.ts`; `src/server/terminal-backend-selection.ts`; `src/server/terminal-control-runtime.ts`; `scripts/wezterm-backend-remote-smoke.ts`; `scripts/wezterm-backend-input-smoke.ts`; `scripts/wezterm-daemon-protocol-smoke.ts`; `scripts/wezterm-daemon-remote-protocol-smoke.ts` | Windows WezTerm mux adapter and daemon backend selection; parses `wezterm cli list`, consumes `get-text --escapes`, materializes ZTerm-owned absolute mirror snapshots, sends input only through `send-text --no-paste` stdin, closes sessions through the selected backend, and black-box compares real daemon source markers with `buffer-sync` output | `src/server/wezterm-backend.test.ts`; `src/server/wezterm-backend-runtime.test.ts`; `src/server/terminal-backend-selection.test.ts`; `src/server/terminal-control-runtime.input-queue.test.ts`; `scripts/wezterm-daemon-protocol-smoke.ts`; `scripts/wezterm-daemon-remote-protocol-smoke.ts`; `scripts/wezterm-backend-remote-smoke.ts`; `scripts/wezterm-backend-input-smoke.ts`; `docs/testing/windows-daemon-live-protocol-test-design.md` |
| `daemon.cli_shell` | `scripts/zterm-daemon.sh`; `scripts/windows/zterm-daemon.ps1`; `scripts/native/zterm-daemon.swift`; `scripts/install-global-daemon-cli.sh`; `scripts/prepare-global-daemon-release.sh#stage_runtime`; `#normalize_release_tree_metadata`; `#create_deterministic_archive`; `#verify_deterministic_archive`; `scripts/prepare-daemon-npm-package.mjs` | daemon CLI surface (`run/start/stop/restart/status/configure-relay/install-service/uninstall-service/service-status`), macOS launchd bootstrap/teardown, Windows Scheduled Task bootstrap/teardown, npm/global package layout, native screenshot binary, and deterministic daemon release compilation. Release compilation stages one runtime tree, normalizes metadata, writes sorted fixed-header archives, and rejects different output from an independently copied restrictive-umask tree before checksum publication | `src/lib/function-wiki-truth.test.ts`; `src/lib/resource-registry-truth.test.ts` |
| `daemon.cli_node` | `scripts/runtime-debug-remote.ts`; `scripts/collect-runtime-audit.ts`; `scripts/analyze-runtime-debug.ts`; `scripts/terminal-real-device-evidence.ts`; `scripts/traversal-relay-local-smoke.ts`; `scripts/daemon-mirror-close-loop.ts`; `scripts/weak-network-byte-proxy.ts`; `scripts/terminal-performance-probe.ts` | node CLI helpers that run outside the daemon process; weak-network helpers transparently shape bytes and measure protocol behavior without parsing or rewriting terminal payload | `src/lib/function-wiki-truth.test.ts`; `src/lib/weak-network-byte-proxy.test.ts`; `test:terminal:regression` |
| `daemon.support` | `scripts/setup-android-java.sh`; `scripts/ensure-pnpm-install.sh`; `scripts/verify-release-assets.mjs`; `scripts/verify-relay-server-package.mjs`; `scripts/check-wterm-runtime-published.mjs`; `scripts/check-no-source-js-pollution.mjs` | release/install/build prerequisites and verifiers | `src/lib/function-wiki-truth.test.ts`; `release:verify` |
| `windows.desktop_shell` | `win/electron/main.ts`; `win/electron/preload.cts`; `win/src/WindowsDesktopApp.tsx`; `win/src/windows-terminal-session.ts`; `packages/shared/src/terminal/mac-terminal-view.tsx` | Windows desktop window/menu/package/platform integration; consumes shared transport/buffer/renderer semantics and must not import Mac IPC or daemon/mirror owners | `win/src/windows-architecture-truth.test.ts`; `win/src/windows-terminal-session.test.ts`; typecheck/build; packaged Windows smoke pending |
| `project.loop_governance` | `docs/loops/LOOP.md`; `docs/loops/loop-manifest.json`; `src/lib/loop-governance-truth.test.ts` | L1 report-only loop governance docs, state, constraints, budget, run log, manifest, test design, project entry link, mainline call ID binding, and static truth gate; no product runtime action is enabled | `src/lib/loop-governance-truth.test.ts`; `test:feature-registry` |
| `mainline_source.android` | `android/native/android/app/src/main/**`; `android/capacitor.config.ts`; `android/vite.config.ts`; `android/src/main.tsx`; `android/src/App.tsx`; `android/src/components/TerminalView.tsx`; `android/src/pages/TerminalPage.tsx` | the Android render path; every file in this list is a published user surface | `test:feature-registry`; `docs/architecture.md` cross-reference gate |
| `mainline_source.daemon` | `src/server/server.ts`; `src/server/terminal-runtime.ts`; `src/server/terminal-bridge-runtime.ts`; `src/server/terminal-message-runtime.ts`; `src/server/terminal-mirror-runtime.ts`; `src/server/terminal-control-runtime.ts`; `src/server/terminal-schedule-runtime.ts`; `src/server/terminal-file-transfer-runtime.ts`; `src/server/remote-screenshot-daemon.ts`; `src/server/remote-window-stream-daemon.ts` | the daemon process mainline; wiki must draw a request path anchored at `server.ts` | `src/server/server.daemon-runtime-truth.test.ts`; `src/lib/function-wiki-truth.test.ts` |
| `mainline_source.cli` | `scripts/zterm-daemon.sh`; `scripts/windows/zterm-daemon.ps1`; `scripts/install-global-daemon-cli.sh`; `scripts/prepare-global-daemon-release.sh#stage_runtime`; `#verify_deterministic_archive`; `#create_deterministic_archive`; `#normalize_release_tree_metadata`; `scripts/prepare-daemon-npm-package.mjs`; `scripts/prepare-relay-server-npm-package.mjs` | the CLI release mainline; wiki must list the install paths, support script names, global bin name `zterm-daemon` (legacy alias `wterm`), and the adjacent deterministic release stages `stage -> verify -> archive -> normalize` bound to real shell symbols | `src/lib/function-wiki-truth.test.ts`; `src/lib/resource-registry-truth.test.ts`; `release:verify` |

Rules:

- One feature has one owner surface. Shared helpers are allowed only when the registry names them as allowed paths.
- Forbidden paths are hard boundaries. Do not patch behavior there to make a feature pass indirectly.
- Required gates are the minimum verification stack, not a replacement for broader regression when the change has wider impact.

## File Transfer Mainline Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| daemon.file_transfer.client.upload_window | `src/lib/file-transfer-throughput-runtime.ts#sendBoundedFileUploadChunks` | `android_mainline:ClientFileTransferUploadOut01SheetIntent->ClientFileTransferUploadOut02BoundedWindow`; `android_mainline:ClientFileTransferUploadOut02BoundedWindow->ClientFileTransferUploadOut03ChunkDispatch` | expose at most eight unacknowledged 16 KiB chunks, advance only from request-bound cumulative ACK truth, and never restore stop-and-wait or unbounded burst |
| daemon.file_transfer.daemon.upload_ack | `src/server/terminal-message-runtime.ts#createTerminalMessageRuntime`; `src/server/terminal-file-transfer-runtime.ts#createTerminalFileTransferRuntime`; `src/server/terminal-file-transfer-binary-runtime.ts#handleFileUploadChunk`; `#handleFileUploadEnd`; `src/server/terminal-transport-runtime.ts#sendMessage` | `daemon_mainline:DaemonFileTransferUploadIn01MessageDispatch->DaemonFileTransferUploadIn02RuntimeFacade`; `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadIn03CumulativeAckOwner`; `daemon_mainline:DaemonFileTransferUploadIn02RuntimeFacade->DaemonFileTransferUploadEndIn04ExactCompletionOwner`; `daemon_mainline:DaemonFileTransferUploadIn03CumulativeAckOwner->DaemonFileTransferUploadProgressOut01CumulativeAck`; `daemon_mainline:DaemonFileTransferUploadEndIn04ExactCompletionOwner->DaemonFileTransferUploadSuccessOut01Complete`; `daemon_mainline:DaemonFileTransferUploadIn03CumulativeAckOwner->DaemonFileTransferUploadErrorOut01ChunkRejected`; `daemon_mainline:DaemonFileTransferUploadEndIn04ExactCompletionOwner->DaemonFileTransferUploadErrorOut02CompletionRejected` | accept only valid request/index-bound chunks, advance ACK through the exact contiguous prefix, validate completion only in `handleFileUploadEnd`, keep progress/completion/error branches separate, and converge only at the existing subscriber transport send owner |
| daemon.file_transfer.client.download_persist | `src/lib/file-transfer-session-runtime.ts#createFileTransferSessionRuntime`; `src/lib/file-transfer-throughput-runtime.ts#writeFileTransferChunkBatches`; `native/android/app/src/main/java/com/zterm/android/StoragePermissionPlugin.java#writeFileChunks` | `android_mainline:ClientFileTransferDownloadIn01SocketDispatch->ClientFileTransferDownloadIn02SessionProjection`; `android_mainline:ClientFileTransferDownloadIn03SheetPersistence->ClientFileTransferDownloadIn04NativeWriteBatch`; `android_mainline:ClientFileTransferDownloadIn05NativeWriteDispatch->ClientFileTransferDownloadIn06NativeStore` | preserve ordered wire payload, persist at most eight chunks per native call, and publish success only after exact on-disk size verification |

The active throughput owner is TypeScript plus the JSON contract. The Rust target is `planned` in `docs/testing/file-transfer-test-design.md#rust-migration-register`; it cannot be treated as active before parity gates and physical deletion of the TypeScript owner.

## Remote Window Stream Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| desktop.remote_window_stream.overlay.project | `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay`; `src/lib/remote-window-overlay-runtime.ts#beginRemoteWindowTargetEnumeration`; `#applyRemoteWindowTargetCatalog`; `#selectRemoteWindowTarget`; `#enterRemoteWindowFullscreen`; `#shrinkRemoteWindowOverlay`; `#closeRemoteWindowOverlay` | `android_mainline:TerminalPage->RemoteWindowOverlay` | render only Android picker/floating/fullscreen projection, collapse same-app app-window targets into one picker row, expose sibling window switching with non-active screenshot thumbnails inside the active video layer after stream lock, keep iTerm2 panes collapsed until explicit expansion, project the last session-local catalog snapshot immediately while a stale/forced refresh runs so the picker does not blank, lock a selected target manifest, size and bottom-corner-resize the floating video preview from the selected crop/window aspect ratio before stream start and from daemon capture frame aspect after stream start while capping growth so the toolbar remains reachable, lift the target-locked floating projection by the current IME bottom inset, lift the fullscreen projection container by the current IME bottom inset without resizing the page shell, move the closed floating entry by threshold drag or long-press-armed drag, move the floating projection through a toolbar-only pointer-captured drag owner, shrink fullscreen on Back/minimize, keep fullscreen drawing/input projection aspect-fit and send a default remote target `window-resize` fill request when fullscreen first has a stream/surface, zoom/pan with local pan only after fullscreen zoom and no minimap/shrink-below-fit projection, expose a floating/fullscreen screenshot button that requests the selected target manifest through `terminal.remote_screenshot` and saves locally without input focus, preserve unzoomed app-window click action, release-time drag gesture action, release-time two-finger gesture action, and wheel input even when the IME inset is present, close UI state explicitly, bound catalog loading with a local UI watchdog, stop active stream and close the overlay when the app backgrounds, and display honest stream setup/error/read-only states without faking terminal buffer/video/input truth |
| desktop.remote_window_stream.client.catalog_request | `src/contexts/session-context-remote-window-runtime.ts#requestRemoteWindowTargetsRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:TerminalPage->RemoteWindowOverlay` | send `remote-window-targets-request` over the active session transport owner, reuse the daemon-wide 60s catalog cache unless the overlay asks for explicit force-refresh, match response/error by request id, timeout explicitly, and never coerce daemon errors into an empty successful catalog |
| desktop.remote_window_stream.daemon.catalog_cache | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#createRemoteWindowStreamDaemonRuntime`; `#listTargets` | `daemon_mainline:Control->RemoteWindowStream` | warm the default app-window+iTerm2 catalog when the daemon runtime starts, keep one process-local 60-second cache per requested source set, return fresh cache without live enumeration, return stale cache immediately while one background refresh runs, await live enumeration for the first uncached request or explicit `forceRefresh`, rewrite cached response/error request ids for the current caller, and expose live failures explicitly when no cached target catalog exists |
| desktop.remote_window_stream.client.response_dispatch | `src/contexts/session-context-socket-message-runtime.ts#handleSocketServerMessageRuntime` | `android_mainline:SessionContext->SocketMessage` | route only remote-window response/start/status/candidate/input-result/error messages to the remote-window message runtime; file transfer and terminal buffer handlers remain separate |
| desktop.remote_window_stream.start.request | `src/contexts/session-context-remote-window-runtime.ts#requestRemoteWindowStreamStartRuntime`; `src/contexts/session-context-remote-window-runtime.ts#resolveRemoteWindowStreamIceServers`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowOverlay->RemoteWindowMessageRuntime` | send `remote-window-stream-start-request` over the existing active session transport with a selected daemon manifest, WebRTC receive offer, route-derived ICE servers for the current traversal path, and the selected video bitrate config; do not create a terminal transport or compute coordinates in Android |
| desktop.remote_window_stream.client.quality_request | `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay`; `src/lib/remote-window-video-quality.ts#buildRemoteWindowVideoBitrateConfig`; `#readRemoteWindowVideoBitratePreset`; `#writeRemoteWindowVideoBitratePreset`; `#resolveEffectiveRemoteWindowVideoBitratePreset`; `#resolveRemoteWindowDesktopCoverageRatio`; `#resolveRemoteWindowVideoAdaptiveDecision`; `src/lib/remote-window-receiver-runtime.ts#createRemoteWindowReceiverRuntime`; `src/contexts/session-context-remote-window-runtime.ts#updateRemoteWindowStreamQualityRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowOverlay->RemoteWindowStreamQualityRuntime` | remember manual bitrate by selected desktop window identity/resolution, keep untouched defaults and floating preview effective stream request at `2 Mbps`, keep Android phone fullscreen from upgrading bitrate by itself, sample WebRTC stats for RTT/available bitrate/FPS/drop/freeze/jitter, downgrade to one-quarter bitrate and half FPS on weak stats, restore only after a stable window, and send `remote-window-stream-quality-request` for the active stream over the existing active session transport without restarting capture, receiver, or terminal transport |
| desktop.remote_window_stream.android.receiver.attach | `src/lib/remote-window-receiver-runtime.ts#createRemoteWindowReceiverRuntime`; `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay` | `android_mainline:RemoteWindowMessageRuntime->RemoteWindowReceiver` | attach the returned WebRTC video stream id to the overlay receiver surface and keep Back/minimize projection-only |
| desktop.remote_window_stream.android.frame.render | `src/components/terminal/RemoteWindowOverlay.tsx#remote-window-video` | `android_mainline:RemoteWindowReceiver->RemoteWindowOverlay` | render only received media frames in the floating/fullscreen video surface; terminal buffer/renderer rows cannot satisfy this state |
| desktop.remote_window_stream.client.touch_action | `src/lib/remote-window-touch-action-runtime.ts#dispatchRemoteWindowTouchInputActionRuntime`; `#isRemoteWindowInputSupportedTarget`; `#resolveRemoteWindowTouchScrollDeltaRuntime`; `#resolveRemoteWindowTouchWheelDeltaRuntime`; `src/components/terminal/RemoteWindowOverlay.tsx#RemoteWindowOverlay` | `android_mainline:RemoteWindowOverlay->RemoteWindowTouchAction` | classify supported app-window input context, turn overlay touch/mouse/wheel/key surface events into explicit action dispatch records, convert tap or mouse click into one `click` action, convert release-time drag direction into one bounded client-owned absolute visible-source pixel gesture, convert target-locked two-finger midpoint movement into one release-time `gesture/swipe` action capped by the selected 1/8, 1/4, 1/2, or 1 screen preset with default inverted direction, report unsent actions as `missing-context` / `unsupported-target` / `missing-dispatcher`, and never let overlay UI state alone claim a physical transport send |
| desktop.remote_window_stream.client.input_request | `src/lib/remote-window-touch-action-runtime.ts#dispatchRemoteWindowTouchInputActionRuntime`; `src/pages/TerminalPage.tsx#emitRemoteWindowInputEvents`; `src/pages/TerminalPage.tsx#handleQuickBarImagePaste`; `src/lib/remote-window-input-mapping.ts#buildRemoteWindowKeyInputEventsFromSequence`; `#buildRemoteWindowTextInputEvents`; `#buildRemoteWindowBackspaceInputEvents`; `#buildRemoteWindowKeyboardInputEvents`; `src/contexts/session-context-remote-window-runtime.ts#sendRemoteWindowInputRuntime`; `#resizeRemoteWindowTargetRuntime`; `src/contexts/session-context-transfer-runtime.ts#sendImagePasteRuntime`; `src/lib/remote-window-message-runtime.ts#createRemoteWindowMessageRuntime` | `android_mainline:RemoteWindowTouchAction->RemoteWindowInputRuntime`; `android_mainline:TerminalPage->RemoteWindowInputRuntime` | keep the media `<video>` pointer-transparent, map video-surface input/scroll coordinates against the selected crop manifest and actual aspect-fit/zoomed content rect into global macOS top-left coordinates, emit unzoomed touch tap or mouse click as one `click` action, emit unzoomed touch/mouse drag as one release-time absolute visible-source pixel `gesture/swipe` action without raw pointer-move streaming, keep mouse/trackpad wheel input as one pixel `scroll` action, keep zoomed fullscreen one-finger drag local to pan, map all target-locked two-finger vertical movement to one release-time `gesture/swipe` action with optional direction inversion, and send `window-resize` only from the fullscreen fill owner using current desktop window width plus phone aspect-derived height; setup/projection actions such as target select, stream start, receiver attach, fullscreen entry, IME lift, pinch zoom, and local zoom-pan must not send focus intents; click/gesture/wheel/key/QuickBar/IME operations must leave Android as one user action record without a client-side focus prelude, because daemon input injection remains the only focus truth and checks focus inline before OS injection; expose the active stream input context to `TerminalPage` only when the target is supported app-window `bring-to-focus + os-event`, never publish or send input for current unsupported `tmux-input` / `iterm2-api` routes, route QuickBar key/text/paste/arrow and raw IME committed text/backspace/key events to explicit `remote-window-input` over the existing session transport when that app-window OS-event context is active, route image paste to a remote-window `pasteTarget` only while that context is active and clear the context when terminal surface focus is activated, preserve the terminal image paste Ctrl+V path otherwise, grey terminal/tmux/iTerm-specific QuickBar actions, and keep zoom/pan projection separate from daemon injection truth |
| desktop.remote_window_stream.daemon.stream_start | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#startStream` | `daemon_mainline:Control->RemoteWindowStream` | route `remote-window-stream-start-request` to daemon stream owner, validate target manifest, apply optional bitrate only to existing sender encodings without changing their structure, start capture, negotiate media, and return explicit started/error/status truth |
| desktop.remote_window_stream.daemon.capture_frame | `src/server/remote-window-stream-daemon.ts#startScreenCaptureKitFrameSource` | `daemon_mainline:RemoteWindowStream->RemoteWindowCapture` | read real ScreenCaptureKit frames from the selected app window or iTerm2 pane crop and feed only those frames into the media sender |
| desktop.remote_window_stream.daemon.input_inject | `src/server/terminal-message-runtime.ts#handleMessage`; `src/server/remote-window-stream-daemon.ts#injectInput`; `#buildRemoteWindowInputConfig`; `#createDefaultRemoteWindowInputHelper` | `daemon_mainline:Control->RemoteWindowStream` | validate stream id, target id, focus policy, input route, click/pointer/scroll/gesture coordinates, pixel deltas, and gesture timing before forwarding generic app input through macOS OS events; accept explicit `focus` intents only as daemon-owned bring-front operations, not as a required prelude for click/gesture/wheel/key input; include target window id/title/bounds in the daemon input config, match the AX window by bounds, check live frontmost PID through System Events, activate and `AXRaise` the target window only when it is not already frontmost/focused, verify frontmost/focused before reporting success, fast-return repeated focus or real input focus preparation when the target app/window is already frontmost/focused, use CGWindowList top-left coordinates directly for event location, translate DOM positive-down/right scroll/gesture deltas to CGEvent wheel signs only in this daemon owner, warm and readiness-check one daemon-owned persistent Swift helper during interactive stream start without emitting focus/input so Swift cold start is not charged to user operations, keep click/legacy pointer/scroll/gesture/key event sequences on that helper instead of compiling `swift -e` per event, apply the one-second stale/drop rule to actual queued input operations while refreshing only same-target action-only bursts after each preceding action succeeds, require burst live probes to send action records without client focus prefixes, allow focus/scroll events to omit `phase`, require gesture events to carry `phase=end`, and return explicit errors for unsupported routes instead of fake success |
| desktop.remote_window_stream.lifecycle.cleanup | `src/server/remote-window-stream-daemon.ts#stopStream`; `src/lib/remote-window-overlay-runtime.ts#closeRemoteWindowOverlay` | `daemon_mainline:RemoteWindowStream->RemoteWindowCleanup` | stop stream on explicit close/error, release capture/media/timers exactly once, and ignore late status/frame events after close |

## Relay Control Chain Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| relay.account_directory.daemon.endpoint_publish | `src/server/daemon-connection-endpoint-runtime.ts#buildDaemonConnectionEndpointCandidates`; `src/server/relay-client.ts#publishRelayDirectoryUpdate`; `src/server/relay-client.ts#createTraversalRelayHostClient` | `daemon_mainline:RelayHostControl->DaemonEndpointDirectory`; `daemon_mainline:DaemonEndpointDirectory->RelayDirectoryPublish` | keep one daemon-role Relay control WebSocket, publish LAN, honest RTC-direct signaling/allocation truth, Tailscale, and TURN Relay candidates, and never derive a UDP mapping from the control WebSocket TCP peer address. Endpoint or tmux discovery failure is an explicit control error, not a partial successful directory. |
| relay.account_directory.client.control_connection | `src/lib/relay-device-stream-runtime.ts#createRelayDeviceStreamRuntime`; `src/lib/traversal-relay-client.ts#connectTraversalRelayDevicesStream`; `src/hooks/useRelayDeviceStream.ts#useRelayDeviceStream` | `android_mainline:RelayControlRuntime->RelayControlSocket`; `android_mainline:RelayControlSocket->RelayDirectoryProjection` | maintain one authenticated Android Relay control WebSocket independent of terminal transports; reconnect only this socket, retain the last confirmed endpoint directory across transient control failure, and clear it only on explicit account disable/logout. Control payloads cannot contain terminal/session/buffer fields. |
| relay.account_directory.client.directory_truth | `src/lib/client-control-directory-runtime.ts#ClientControlDirectoryRuntime.applyConfirmedDirectoryDevices`; `#mergeHostWithClientControlDirectory` | `android_mainline:RelayDirectoryProjection->TransportTargetResolver`; `android_mainline:TransportTargetResolver->TraversalSocketFactory` | store confirmed endpoint truth by stable daemon host id, increment generation only when endpoint truth changes, preserve confirmed candidates when presence becomes disconnected or an incremental snapshot omits a host, and inject the newest candidates only when a future daemon-target transport is constructed. Endpoint push must not close or replace an open healthy terminal transport. |
| relay.route_selection.auto_priority | `src/lib/traversal/route-selector.ts#selectTraversalRouteCandidates`; `src/lib/traversal/route-health-cache.ts#TraversalRouteHealthCache`; `src/lib/traversal/config.ts#buildTraversalPlan` | `android_mainline:TraversalSocket->TerminalTransportOut01RouteSelection` | rank Auto candidates in fixed product tiers `LAN > RTC UDP direct > Tailscale > TURN Relay`; health/RTT may order or reject within a tier but cannot promote a healthy lower tier over a healthy higher tier. Persist successful route health by daemon/account/candidate identity without making it endpoint truth. |

## Terminal Transport Multiplex Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| terminal.transport_lifecycle.protocol.mux_contract | `packages/shared/src/connection/protocol.ts#TerminalMuxClientFrame`; `#TerminalMuxServerFrame`; `#buildTerminalMuxHello`; `#buildTerminalMuxPing`; `#buildTerminalMuxChannelOpen`; `#buildTerminalMuxChannelMessage`; `#isTerminalMuxClientFrame`; `#isTerminalMuxServerFrame` | `android_mainline:TargetTransportRuntime->MuxHandshake`; `android_mainline:TargetHeartbeat->TerminalMuxPingBuilder`; `android_mainline:TargetNetworkSignalOrchestration->TerminalMuxPingBuilder`; `daemon_mainline:Bridge->MuxHandshake` | define the only mux-capable wire envelope: target-level control frames, channel-open/channel-close, channel-bound request/response frames, ping/pong, and explicit upgrade-required / unknown-channel errors. Heartbeat and network-generation probes must both use `buildTerminalMuxPing`; all session-bound payloads must carry `channelId`; unwrapped or mismatched payloads must be rejected before reaching buffer/input/file/schedule truth. |
| terminal.transport_lifecycle.target_transport.open | `src/lib/session-transport-runtime.ts#ensureTargetTransportRuntime`; `#buildTransportTargetKey`; `#buildTransportRouteCandidateKey`; `src/lib/client-daemon-connection.ts#createClientDaemonConnection`; `src/contexts/session-context-transport-open-runtime.ts#openSessionMuxChannelByIntentRuntime`; `src/contexts/session-context-transport-orchestration-runtime.ts#handleTargetMuxTransportFailureRuntime` | `android_mainline:TransportOpen->TargetTransportRuntime`; `android_mainline:TraversalSocket->TargetTransportRuntime`; `android_mainline:TargetTransportRuntime->ChannelRuntime`; `android_mainline:MuxHandshake->TerminalTransportError01TargetFailure`; `android_mainline:TerminalTransportError01TargetFailure->TraversalSocket`; `android_mainline:TerminalTransportError01TargetFailure->SessionRuntime` | open or reuse exactly one physical daemon-target transport for the current daemon target, negotiate mux capability, preserve selected route diagnostics as target transport metadata, and never allocate a second physical transport for session switch or route-candidate update while the target transport is open. `TerminalTransportError01TargetFailure` is the sole physical failure owner: heartbeat and mux observers only submit failure, while this owner records route health exactly once, retires and closes that exact generation, preserves logical channel identities, then schedules one target-level rebuild; it must not remain a recent-success route or fan out into per-session physical reconnects. |
| terminal.transport_lifecycle.target_transport.network_probe | `src/hooks/useOpenTabLifecycleEffects.ts#useOpenTabLifecycleEffects`; `src/hooks/useOpenTabRuntime.ts#useOpenTabRuntime`; `src/App.tsx#AppContent`; `src/contexts/SessionContext.tsx#SessionProvider`; `src/contexts/session-context-provider-facade-assemblies.ts#useSessionProviderFacadeAssemblies`; `src/contexts/session-context-public-facade-runtime.ts#createSessionPublicFacadeRuntime`; `src/contexts/session-context-provider-core-assemblies.ts#useSessionProviderCoreAssemblies`; `src/contexts/session-context-transport-orchestration-runtime.ts#createSessionTransportOrchestrationRuntime`; `#notifyTargetNetworkSignalRuntime`; `#routeTargetSocketFailureRuntime`; `src/contexts/session-context-target-network-probe-runtime.ts#createSessionTargetNetworkProbeRuntime`; `src/contexts/session-context-transport-runtime.ts#createSessionContextTransportAccessors`; `#bindTargetMuxTransportSocketLifecycleRuntime`; `src/lib/session-transport-runtime.ts#listTargetTransportRuntimes`; `packages/shared/src/connection/protocol.ts#buildTerminalMuxPing` | `android_mainline:PlatformNetworkSignal->OpenTabNetworkBinding`; `android_mainline:OpenTabNetworkBinding->AppNetworkBinding`; `android_mainline:AppNetworkBinding->SessionContextNetworkFacade`; `android_mainline:SessionContextNetworkFacade->SessionProviderNetworkBinding`; `android_mainline:SessionProviderNetworkBinding->SessionPublicFacadeBinding`; `android_mainline:SessionPublicFacadeBinding->SessionProviderCoreBinding`; `android_mainline:SessionProviderCoreBinding->TargetNetworkSignalOrchestration`; `android_mainline:TargetNetworkSignalOrchestration->TargetTransportAccessors`; `android_mainline:TargetNetworkSignalOrchestration->TerminalMuxPingBuilder`; `android_mainline:TargetTransportAccessors->TargetTransportStoreEnumeration`; `android_mainline:TargetTransportStoreEnumeration->TargetNetworkProbeDispatch`; `android_mainline:TargetNetworkProbeDispatch->TargetNetworkProbe`; `android_mainline:MuxHandshake->TargetMuxFrameLifecycle`; `android_mainline:TargetMuxFrameLifecycle->TargetNetworkActivityBinding`; `android_mainline:TargetNetworkActivityBinding->TargetNetworkProbe`; `android_mainline:TargetNetworkProbe->TargetFailureRouter`; `android_mainline:TargetFailureRouter->TerminalTransportError01TargetFailure`; `android_mainline:TargetFailureRouter->IdleTargetRetirement` | treat Android network callbacks and every foreground generation as ephemeral generation signals, never foreground, active-tab, or endpoint-reachability truth; pass them through the open-tab/App/context/provider/public-facade/core-assembly bindings into the unique daemon-connection owner, enumerate every current daemon-target physical transport only through `createSessionContextTransportAccessors -> listTargetTransportRuntimes`, dedupe by target plus exact socket generation, build the target-level probe through the same `buildTerminalMuxPing` shared contract as the target heartbeat, and resolve a pending probe only when the mux handshake binds `bindTargetMuxTransportSocketLifecycleRuntime`, that lifecycle validates inbound activity, and the orchestration binding records the same target key plus exact socket generation. Targets with channels enter the existing target failure owner; retained zero-session targets remain healthy on valid pong/activity and retire only that physical generation on real failure without inventing a session or reconnect job. Session channels cannot own, scope, or multiply this probe. |
| terminal.transport_lifecycle.target_tmux_management | `src/contexts/session-context-tmux-management-runtime.ts#manageTmuxSessionsOnOpenTransportRuntime`; `#settleSessionTmuxTargetRequestRuntime`; `src/hooks/useSessionOpenActions.ts#manageTmuxSessionsForTarget`; `src/hooks/useOpenTabRuntime.ts#auditOpenTabsAgainstRemoteSessions`; `src/lib/remote-tab-audit.ts#auditOpenTabsAgainstRemoteSessions`; `src/lib/open-tab-restore.ts#fetchRemoteTmuxSessionNamesByOwner` | `android_mainline:SessionOpenOwner->SessionContext`; `android_mainline:TargetTransportRuntime->MuxHandshake` | drawer refresh, open-tab audit, quick create, Home no-session discovery, and remote kill must use the existing daemon-target mux physical transport through request-id `mux-target-message` when a matching non-closed Session exists. Daemon `error`, malformed response, timeout, or not-yet-ready mux target state is surfaced and must not fallback to a second legacy `TraversalSocket`; the legacy tmux management pool is only for no matching open target. |
| terminal.transport_lifecycle.channel.open | `src/lib/session-transport-runtime.ts#ensureSessionTerminalChannel`; `#getOpeningSessionTerminalChannelsForTarget`; `#updateSessionTerminalChannelState`; `#removeSessionTerminalChannel`; `src/contexts/session-context-transport-runtime.ts#sendSessionMuxChannelOpenRuntime`; `src/contexts/session-context-transport-orchestration-runtime.ts#createSessionTransportOrchestrationRuntime` (active session priority binding) | `android_mainline:TargetMuxFrameLifecycle->ActiveSessionPriority`; `android_mainline:ActiveSessionPriority->ChannelRuntime`; `android_mainline:TargetTransportRuntime->ChannelRuntime`; `daemon_mainline:MuxHandshake->ChannelRegistry` | map one local session to one transport-local channel, carry initial `bodySubscribed` truth in `mux-channel-open` before attach starts, and bind that channel to one daemon subscriber. When one same-target mux transport becomes ready, the mux lifecycle must read the current active session truth and order opening channel replay through `getOpeningSessionTerminalChannelsForTarget` so the active channel opens first; missing or non-opening active truth is a transport lifecycle failure, not an unprioritized fallback. `mux-channel-opened` is allocation truth only; terminal readiness comes from the same channel's real daemon `connected` message after attach/mirror readiness. Channel close detaches the subscriber without killing tmux or mirror truth. |
| terminal.transport_lifecycle.channel.send | `src/contexts/session-context-infra-facade-runtime.ts#wrapSessionPayloadForTargetMuxRuntime`; `#requireTerminalMuxChannel` | `android_mainline:ChannelRuntime->ChannelMessageSend` | send input, resize, body-subscription, buffer-head/range, schedule, file-transfer, screenshot, and terminal-context remote-window intents only inside `mux-channel-message`. UI, drawer, renderer, and buffer manager cannot open sockets or compensate channel identity. |
| terminal.transport_lifecycle.channel.demux | `src/contexts/session-context-transport-runtime.ts#handleTargetMuxServerFrameRuntime`; `src/contexts/session-context-socket-message-runtime.ts#handleSocketServerMessageRuntime`; `src/server/terminal-message-runtime.ts#handleMuxClientFrame` | `android_mainline:SocketMessage->ChannelDemux`; `android_mainline:ChannelDemux->BufferWireNormalize`; `daemon_mainline:Message->ChannelRegistry` | validate `channelId`, dispatch each accepted server frame only to the owning local session wire-normalization owner, and turn unknown/stale channel messages into explicit errors rather than dropping them silently or applying them to the active session. |
| terminal.transport_lifecycle.scheduler.mux_send | `src/server/terminal-transport-runtime.ts#sendTransportMessage`; `#sendText`; `src/server/terminal-mirror-runtime.ts#flushPendingSubscriberBody` | `daemon_mainline:Mirror->SendScheduler`; `daemon_mainline:SendScheduler->TransportSend` | maintain one send/accounting owner per physical target transport with priority lanes for errors/open/close/ack/input-adjacent/head before body frames, and keep at most one pending latest body update per channel. Old body payloads are not queue truth. |
| relay.account_directory.peer_lease.resume | `src/traversal-relay/server.ts#registerClient`; `src/server/relay-client.ts#createTraversalRelayHostClient`; `src/server/rtc-bridge.ts#handleRelaySignal` | `daemon_mainline:Relay->RelayPeerLease`; `daemon_mainline:RelayPeerLease->MuxHandshake` | issue and validate an idle-timeout bounded Relay peer lease for one account + daemon target + concrete client device. Client signaling reconnect may rebind to that same peer lease for 30 minutes before expiry; another phone gets a separate lease, and missing device id, expiry, or host/account/device mismatch must fail explicitly and close the peer. The lease must not store terminal channel, subscriber, tmux, mirror, active tab, foreground, viewport, or UI state. |

## Session Preview Function Bindings

| function_id | owner symbol | mainline call id | semantic contract |
| --- | --- | --- | --- |
| terminal.session_preview.selection.normalize | `src/lib/session-preview-selection.ts#normalizeSessionPreviewSelection` | `android_preview:SessionDrawer->PreviewSelectionOwner` | normalize versioned ordered targets, deduplicate by open-session id, cap at six |
| terminal.session_preview.selection.toggle | `src/lib/session-preview-selection.ts#toggleSessionPreviewTarget` | `android_preview:SessionDrawer->PreviewSelectionOwner` | add/remove one valid target and explicitly reject selection seven |
| terminal.session_preview.selection.materialize_remote | `src/pages/TerminalPage.tsx#resolveSessionPreviewTargetFromDrawerSelection`; `src/hooks/useSessionOpenActions.ts#handleOpenGroupSession` | `android_preview:SessionDrawer->RemoteSessionOpenOwner`; `android_preview:RemoteSessionOpenOwner->PreviewSelectionOwner` | remote catalog row selection must materialize an open-session id through the existing drawer remote-open owner with no activation/navigation, then persist only the materialized local session target |
| terminal.session_preview.selection.append | `src/pages/TerminalPage.tsx#handleAddSessionPreview`; `src/lib/session-preview-selection.ts#appendSessionPreviewTarget` | `android_preview:PreviewAddMenu->PreviewSelectionOwner` | append exactly one currently open unselected target after tile removal, preserve existing order, and explicitly reject duplicate or seventh targets |
| terminal.session_preview.selection.replace | `src/lib/session-preview-selection.ts#replaceSessionPreviewTarget` | `android_preview:PreviewReplacementMenu->PreviewSelectionOwner` | replace one selected target in place with one valid currently unselected open-session target |
| terminal.session_preview.selection.persist | `src/lib/session-preview-selection.ts#readSessionPreviewSelection`; `#writeSessionPreviewSelection` | `android_preview:SessionDrawer->PreviewSelectionOwner` | explicit available/empty/invalid read and explicit write result |
| terminal.session_preview.selection.resolve_open_tabs | `src/lib/session-preview-selection.ts#resolveSessionPreviewTargets`; `#pruneSessionPreviewSelectionToOpenSessions` | `android_preview:PreviewSelectionOwner->OpenTabResolver` | project selection through current open-session truth only and physically remove stale or identity-mismatched targets |
| terminal.session_preview.gesture.begin | `src/lib/session-preview-gesture.ts#beginSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | arm only inside the right-edge admission band |
| terminal.session_preview.gesture.update | `src/lib/session-preview-gesture.ts#updateSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | lock one horizontal/vertical owner for the touch sequence |
| terminal.session_preview.gesture.finish | `src/lib/session-preview-gesture.ts#resolveSessionPreviewGesture` | `android_preview:TerminalShellGesture->PreviewModeOwner` | admit only threshold-crossing leftward horizontal motion |
| terminal.session_preview.live_set.project | `src/lib/session-preview-selection.ts#projectSessionPreviewLiveIds` | `android_preview:PreviewModeOwner->PreviewLiveSetProjector`; `android_preview:PreviewLiveSetProjector->SessionBodySubscriptionIntent` | union selected ids only while preview is open and foreground |
| terminal.session_preview.grid.render | `src/components/terminal/WindowGroupLayout.tsx#WindowGroupLayout`; `#resolveWindowGroupLayoutPlan`; `src/components/terminal/TerminalPreviewGrid.tsx#TerminalPreviewGrid` | `android_preview:PreviewModeOwner->TerminalPreviewGrid`; `android_preview:TerminalPreviewGrid->WindowGroupLayout`; `android_preview:WindowGroupLayout->TerminalPreviewTile`; `android_preview:TerminalPreviewTile->SharedRenderSurface` | derive a primary-plus-children layout from 1-6 targets, use portrait top-child rail plus large bottom primary or landscape side-child rail plus large primary, keep every child as its own preview container, promote child title/body taps to primary without activation, keep visual order badges out of tile titlebars, render secondary child previews with compact local typography without tmux/viewport/resize side effects, and render read-only tiles with local scroll/pan through the shared renderer store |
| terminal.session_preview.grid.replace_menu | `src/components/terminal/TerminalPreviewGrid.tsx#preview-replacement-menu` | `android_preview:TerminalPreviewTile->PreviewReplacementMenu`; `android_preview:PreviewReplacementMenu->PreviewSelectionOwner` | long press one tile, suppress activation, and expose only currently unselected open-session replacement commands |
| terminal.session_preview.tile.remove | `src/pages/TerminalPage.tsx#handleRemoveSessionFromPreview`; `src/lib/session-preview-selection.ts#removeSessionPreviewTarget` | `android_preview:TerminalPreviewTileClose->PreviewSelectionOwner` | remove only the selected preview target, persist the remaining order, and cancel preview when none remain without closing the Session |
| terminal.session_preview.grid.add_menu | `src/components/terminal/TerminalPreviewGrid.tsx#terminal-preview-add-menu` | `android_preview:PreviewAddMenu->PreviewSelectionOwner` | expose every currently open eligible Session absent from the preview and no selected Session |
| terminal.session_preview.tile.activate | `src/pages/TerminalPage.tsx#handleActivateSessionFromPreview`; `#handleActivateOpenSessionInViewport` | `android_preview:TerminalPreviewTile->ActiveSessionIntent`; `android_preview:ActiveSessionIntent->TerminalPage` | project the selected open session into the focused session-group viewport, emit one explicit switch, then close preview |
| terminal.session_preview.mode.cancel_restore | `src/pages/TerminalPage.tsx#handleCancelSessionPreview` | `android_preview:SystemBackIntent->PreviewModeOwner`; `android_preview:PreviewModeOwner->EntrySessionProjection` | consume Back only while preview is open, close preview, and restore the captured entry active/session-group projection |

## Daemon & CLI Owner Map

| command / entry | owner | allowed change surface | required gate source |
| --- | --- | --- | --- |
| `zterm-daemon.sh run` | `scripts/zterm-daemon.sh` -> `src/server/server.ts` | tsx entry, `~/.wterm/daemon-runtime/server.cjs` staging, `node-pty` + `@roamhq/wrtc` vendor, `ZTERM_DAEMON_NATIVE` handoff | `src/server/server.daemon-runtime-truth.test.ts`; `daemon:install-global` |
| `zterm-daemon.sh start/stop/restart/status` | `scripts/zterm-daemon.sh` (`start_service` / `stop_service` / `restart_service` / `status*`) | launchd label `com.zterm.android.zterm-daemon`; legacy `com.zterm.android.daemon` and `com.wterm.mobile.daemon` cleanup; `wait_for_service_ready` port check; pid file `~/.wterm/run/zterm-daemon.pid` | `scripts/zterm-daemon.sh` smoke; `daemon:install-service` |
| `zterm-daemon.sh configure-relay` | `scripts/zterm-daemon.sh` (`configure_relay`) | writes `~/.wterm/config.json -> mobile.relay`; required flags `--relay-url/--username/--password\|--password-stdin\|--password-env/--host-id`; optional `--device-id/--device-name/--restart-service` | `src/server/daemon-config.test.ts` |
| `zterm-daemon.sh install-service` | `scripts/zterm-daemon.sh` (`install_service`) | prime macOS TCC preflight (file access + Screen Recording), install/refresh plist, `bootstrap_service`, port ready gate | `src/server/launch-agent-plist.test.ts`; `daemon:install-service` |
| `zterm-daemon.sh uninstall-service` | `scripts/zterm-daemon.sh` (`uninstall_service`) | launchd `bootout`, remove plist, remove legacy agents | `src/server/launch-agent-plist.test.ts` |
| `zterm-daemon.sh service-status` | `scripts/zterm-daemon.sh` (`status_service`) | `launchctl print` snapshot; reports `active count` + `last exit code` | `daemon:service-status` |
| `pnpm run daemon:mirror:close-loop` | `scripts/daemon-mirror-close-loop.ts` | tmux oracle + daemon runtime + client mirror + renderer close loop; required by `test:terminal:regression` | `scripts/daemon-mirror-close-loop.ts` self-test |
| `pnpm run daemon:runtime:analyze` / `:remote` / `:capture` | `scripts/analyze-runtime-debug.ts`; `scripts/runtime-debug-remote.ts`; `scripts/collect-runtime-audit.ts` | shared `parseArgs`/`parseCli` helper; reads `/debug/runtime`, `/debug/runtime/control`, `/debug/runtime/logs`; never embeds payload | `terminal-debug-runtime.test.ts`; `server.debug-truth.test.ts` |
| `pnpm run build:android` | `scripts/build-android-debug.sh` | Vite build + cap sync + Gradle `:app:assembleDebug` + `prepare-update-bundle`; sha256 + `update-dist/latest.json` + `~/.wterm/updates/` | `test:feature-registry`; `prebuild` runs `test:terminal:regression` |

## Mainline Source Index

`docs/wiki/mainline-source.md` is the canonical source for "where does this user-visible surface live". `docs/wiki/mainline-call-map.json` is the machine-readable call-map manifest whose node ids must align with that wiki. Both must stay in lockstep with this map. The `src/lib/function-wiki-truth.test.ts` gate fails when the wiki, the registry, the call-map manifest, and the mainline index disagree.

Rules:

- One feature has one owner surface. Shared helpers are allowed only when the registry names them as allowed paths.
- Forbidden paths are hard boundaries. Do not patch behavior there to make a feature pass indirectly.
- Required gates are the minimum verification stack, not a replacement for broader regression when the change has wider impact.

## Resource Binding Map

`docs/resource-registry.json` is the top-level machine-readable resource truth. `docs/resource-map.md` is the human review surface. `docs/module-registry.json` groups resources into project modules. `docs/edge-registry.json` declares allowed cross-module resource edges. This function map binds feature-local owners to declared global resources and edges only; it must not invent resource ids, module ids, or shortcut edges.

## Module / Edge Binding Map

`docs/module-registry.json` is the module ownership manifest. `docs/edge-registry.json` is the cross-module edge manifest. Runtime refactors must land module/edge changes before adding feature-local function bindings.

| refactor area | module ids | required edge ids | rule |
| --- | --- | --- | --- |
| daemon target physical connection | `client.daemon_connection`, `client.terminal_channel_mux`, `daemon.connection_gateway`, `daemon.transport_subscriber` | `edge.client.active_session_to_session_transport`, `edge.client.session_transport_to_daemon_target_transport`, `edge.client.daemon_target_transport_to_terminal_channel`, `edge.daemon.terminal_channel_to_subscriber` | one client physical connection per daemon target; session is channel, not socket owner. |
| terminal body truth | `daemon.mirror_store`, `daemon.transport_subscriber`, `client.buffer_store`, `client.renderer_window`, `client.app_shell` | `edge.daemon.mirror_store_to_transport_subscriber`, `edge.daemon.mirror_store_to_client_buffer_frame_assembly`, `edge.client_buffer_frame_assembly_to_client_sparse_buffer`, `edge.client_sparse_buffer_to_renderer`, `edge.client_renderer_to_ui_projection` | mirror is daemon writer; frame assembly owns atomic wire-frame publication and exact repair range; sparse buffer applies absolute rows; renderer owns visible range. |
| remote-window control | `client.remote_window_overlay`, `daemon.remote_window_stream` | `edge.client_ui_to_remote_window_overlay`, `edge.client_remote_window_overlay_to_touch_action`, `edge.client_touch_action_to_daemon_remote_window_stream`, `edge.daemon_remote_window_stream_to_daemon_process` | UI emits action records; daemon owns focus/capture/input injection; terminal mirror is forbidden video truth. |
| relay peer resume | `relay.peer_lease`, `client.connection_home`, `client.daemon_connection` | `edge.relay_peer_lease_to_transport_target`, `edge.relay_peer_lease_to_daemon_target_transport_via_target` | Relay lease is route/signaling truth only; no channel/subscriber/tmux/mirror/UI truth. |
| release/start | `release.update_artifact`, `release.daemon_artifact`, `daemon.runtime_entry` | `edge.release.update_artifact_to_daemon_artifact`, `edge.release.daemon_artifact_to_daemon_process` | runtime consumes deterministic promoted artifacts; no authoring-source execution path. |

| feature_id | resource_ids | relation notes |
| --- | --- | --- |
| `terminal.keyboard_ime` | `resource.platform_input_channel`, `resource.session_transport`, `resource.daemon_input_queue` | Input is captured by platform input owner, then sent through current session transport and daemon input queue. It must not write `resource.backend_session`, `resource.tmux_session`, or `resource.wezterm_pane` directly. |
| `terminal.schedule` | `resource.schedule_job`, `resource.backend_session` | Scheduled jobs dispatch only through backend session owner. |
| `terminal.remote_screenshot` | `resource.remote_screenshot`, `resource.backend_session`, `resource.debug_channel` | Screenshot request/result is daemon side state; debug can observe only. |
| `desktop.remote_window_stream` | `resource.remote_window_overlay`, `resource.remote_window_stream`, `resource.transport_target`, `resource.debug_channel` | Android overlay projects picker/floating/fullscreen/video receiver state, aspect-fit fullscreen drawing, default fullscreen window-resize fill intent, and input intents only. Daemon/native stream truth owns catalog, coordinates, capture frame source, WebRTC media sender, receiver attach status, input target lease, cleanup, and explicit policy errors. Terminal mirror/buffer/render resources are forbidden as video truth. |
| `terminal.open_tabs` | `resource.open_tab`, `resource.active_session` | Open tabs are explicit persisted client truth; transport/runtime facts must not close or merge them. |
| `terminal.transport_lifecycle` | `resource.active_session`, `resource.session_transport`, `resource.daemon_target_transport`, `resource.terminal_channel`, `resource.transport_target`, `resource.pending_open_intent`, `resource.transport_subscriber`, `resource.mirror_store`, `resource.debug_channel` | Active session owns current client identity; session transport maps local sessions to daemon-target physical transports and terminal channels; daemon-target transport owns one physical WebSocket/RTC data channel per target; terminal channel owns per-session wire identity; daemon subscriber owns only channel body eligibility, send/backpressure, and bounded pending-latest state; trace observes metadata only. |
| `terminal.daemon_input` | `resource.session_transport`, `resource.daemon_input_queue`, `resource.backend_session`, `resource.tmux_session` | Client input crosses the existing session transport into daemon input queue. New daemon capability enables seq/ack/retry and daemon dedupe; old-daemon compatibility keeps string-only frames. Daemon input queue remains the only write queue into backend sessions and tmux. |
| `terminal.buffer_render` | `resource.tmux_session`, `resource.mirror_store`, `resource.transport_subscriber`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window`, `resource.debug_channel` | tmux capture commits through the only mirror writer; mirror broadcasts current revision through eligible physical subscribers; frame assembly publishes only complete continuous frames and owns exact bounded repair truth; client sparse buffer applies absolute rows; renderer owns visible demand and next-RAF commit only; trace observes metadata only. |
| `terminal.session_group_layout` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.renderer_window` | Layout projects platform surface and panes; it does not own terminal content layout. |
| `terminal.session_drawer` | `resource.ui_projection`, `resource.open_tab`, `resource.active_session` | Drawer displays projection and emits intent; it cannot open transport sockets directly. |
| `terminal.session_preview` | `resource.session_preview_selection`, `resource.session_preview_mode`, `resource.open_tab`, `resource.client_sparse_buffer`, `resource.renderer_window`, `resource.ui_projection`, `resource.active_session` | Preview resolves selected open tabs. Remote drawer catalog rows must first become `resource.open_tab` through the existing session-open owner; selection storage never stores remote placeholder ids. Preview projects existing immutable render truth and emits active-session intent only on tile activation; it cannot own transport or backend truth. |
| `terminal.quickbar` | `resource.ui_projection`, `resource.platform_input_channel` | Quickbar is shell/input projection; input still routes through platform input and session transport resources. |
| `terminal.workspace_panes` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.renderer_window` | Pane shell arranges projection and renderer containers; terminal layout remains backend/mirror truth. |
| `terminal.interaction_runtime` | `resource.ui_projection`, `resource.active_session` | Interaction runtime resolves active pane/session intent, then delegates to active session owner. |
| `terminal.shell_actions` | `resource.ui_projection`, `resource.active_session`, `resource.session_transport` | Shell actions emit high-level intent and must not bypass session transport owner. |
| `connections.history_projection` | `resource.ui_projection`, `resource.open_tab` | Connections projection displays saved direct/Tailscale/history/runtime facts but does not create current tab truth and does not let Relay own saved Host truth. |
| `relay.account_directory` | `resource.relay_control_connection`, `resource.relay_account_directory`, `resource.debug_channel`, `resource.transport_target`, `resource.relay_peer_lease` | Relay control carries endpoint/presence/control facts on a physical socket separate from terminal data. Confirmed directory truth informs future target resolution and survives transient control failure, but cannot own an open terminal transport, terminal channel, subscriber, tmux, mirror, buffer, or UI truth. Relay peer lease may preserve a per-client-device route/signaling peer id for 30 minutes without storing application session state. |
| `relay.route_selection` | `resource.transport_target`, `resource.session_transport`, `resource.daemon_target_transport`, `resource.relay_peer_lease`, `resource.route_policy`, `resource.debug_channel` | Route selection produces target diagnostics and selected target facts with Auto priority `LAN -> WebRTC UDP direct -> Tailscale -> TURN/Relay`; transport owner consumes them and records runtime route failures back into route health. A persisted success may seed a future generation only inside its product tier. When Relay resume is available, route selection consumes `resource.relay_peer_lease` only as route/signaling state, not as channel or session truth. |
| `relay.directory_ui` | `resource.ui_projection`, `resource.transport_target` | Directory UI projects optional relay account/device route candidates while preserving direct saved-host and active-session entries; it emits explicit open/resume intent only. |
| `daemon.file_transfer` | `resource.client_file_browser`, `resource.target_mux_request`, `resource.daemon_target_transport`, `resource.transport_subscriber`, `resource.file_transfer`, `resource.backend_session` | Client file browser owns projection plus bounded local flow-control intent; daemon owns cumulative ACK and file truth. Wire frames traverse the target mux/transport subscriber path; the UI cannot write daemon files or bypass the native storage owner. |
| `daemon.runtime_entry` | `resource.runtime_home`, `resource.daemon_process`, `resource.terminal_backend`, `resource.debug_channel` | Daemon process wires runtime modules and health/debug endpoints; it must not own client active/session state. |
| `daemon.windows_wezterm_backend` | `resource.terminal_backend`, `resource.backend_session`, `resource.wezterm_pane`, `resource.mirror_store` | WezTerm backend owns pane capture/input adapter and feeds mirror store through backend capture. |
| `daemon.cli_shell` | `resource.daemon_runtime_artifact`, `resource.daemon_process`, `resource.release_update_artifact`, `resource.runtime_home` | CLI promotes deterministic runtime artifacts and starts/stops daemon process. |
| `daemon.cli_node` | `resource.debug_channel`, `resource.daemon_process`, `resource.mirror_store`, `resource.transport_subscriber`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window` | Node CLI helpers and bounded performance trace observe capture/send/rx/assembly/apply/render metadata; they must not store terminal text or become business truth writers. |
| `daemon.support` | `resource.release_update_artifact`, `resource.daemon_runtime_artifact` | Support scripts verify release/install prerequisites and artifacts before promotion. |
| `windows.desktop_shell` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.active_session`, `resource.session_transport`, `resource.client_sparse_buffer`, `resource.renderer_window` | Windows shell owns desktop projection/platform bridge only; terminal content still flows through shared transport, sparse buffer, and renderer owners. |
| `mainline_source.android` | `resource.platform_terminal_surface`, `resource.ui_projection`, `resource.active_session`, `resource.session_transport`, `resource.client_buffer_frame_assembly`, `resource.client_sparse_buffer`, `resource.renderer_window` | Android mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`. |
| `mainline_source.daemon` | `resource.daemon_process`, `resource.terminal_backend`, `resource.backend_session`, `resource.mirror_store`, `resource.daemon_input_queue`, `resource.transport_subscriber`, `resource.debug_channel` | Daemon mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`. |
| `mainline_source.cli` | `resource.release_update_artifact`, `resource.daemon_runtime_artifact`, `resource.daemon_process`, `resource.runtime_home` | CLI mainline edges are resource-bound in `docs/wiki/mainline-call-map.json`. |
