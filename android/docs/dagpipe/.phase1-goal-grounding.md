# Android DAGpipe Phase1 — Goal grounding brief

Calling session verified these facts. Do not re-invent; reuse.

## Truth sources (all present on origin/main)
- `android/docs/dagpipe/android-phase0-design-slice.md` — Chinese semantic design slice; states Phase0 is static only, no Operator registration, no new Cargo.toml.
- `android/docs/dagpipe/README.md`
- `android/docs/dagpipe/android-dag-approval.html` (approved)
- `android/docs/dagpipe/android-phase1-goal.md` (this goal's text source)
- Six graphs: `android-connection-lifecycle`, `android-buffer-management`, `android-buffer-render`, `android-input-dispatch` (Android 4) + `daemon-mirror-publish`, `daemon-control-dispatch`
- `android/package.json` already has `test:dagpipe-phase0` = `node ./scripts/validate-dagpipe-graphs.mjs`

## Key precedent: daemon-side Phase1 already exists, unmerged — mirror its pattern
- Branch `feature/dagpipe-phase1`, commit `166ba892`, worktree `/private/tmp/zterm-dagpipe-phase1`.
- Pattern to mirror for Android side (do NOT invent a new wiring approach):
  - `android/native/dagpipe/` Rust crate: `name=zterm-dagpipe`, `crate-type=["cdylib","rlib"]`, `features default=["napi"]`, deps `pipeline_runtime={path="/Users/fanzhang/.local/share/dagpipe/sdk"}`, `serde_json="1"`, `napi`/`napi-derive` (optional), build-dep `napi-build` (optional), dev-dep `tempfile="3"`.
  - `src/lib.rs` exposes napi fns `compile_phase0` / `run_mirror_publish` / `run_control_dispatch`, returning JSON strings (`{ok:true,...}` / `{ok:false,error}`).
  - `src/core.rs` defines Operators + `compile_phase0_graphs()`.
  - `tests/phase0.rs`
  - `android/src/server/dagpipe-bridge.ts`: `createRequire` loads `index.node`; candidate paths `ZTERM_DAGPIPE_NATIVE` / source `native/dagpipe/index.node` / runtime `cwd/dagpipe.node`; exports `compilePhase0`/`runMirrorPublish`/`runControlDispatch`.
  - `android/scripts/build-dagpipe-native.sh`: `cargo build` then `cp target/debug/libzterm_dagpipe.dylib index.node` (PROFILE=debug|release).
  - `android/scripts/run-dagpipe-phase1-tests.sh`: build → `cargo fmt --check` → `cargo clippy --no-default-features -- -D warnings` → `cargo test --no-default-features` → vitest `dagpipe-bridge.test.ts` → `pnpm test:dagpipe-phase0`.
  - `package.json` adds scripts `test:dagpipe-phase1` / `build:dagpipe-native`.
- CAUTION: the daemon branch's `phase0-design-slice.md`/`README.md` are the daemon variant, different from the Android variant on `main`. The Android worktree MUST start from `origin/main`; do not reuse that branch's files, and do not mix daemon-side implementation into Android scope.

## Environment
- `dagpipe` CLI: `/Users/fanzhang/.cargo/bin/dagpipe`; SDK path (`dagpipe sdk path`) = `/Users/fanzhang/.local/share/dagpipe/sdk`
- cargo 1.97.1
- `main` HEAD == `origin/main` == `0874bf8605c2831ce9db58f589602913bf87ebe3`
- Current working tree has PRE-EXISTING dirt (`android/.appsdk/*` etc.). Must be preserved; no `stash`/`reset`/`checkout` cleanup.
- Existing worktrees `/private/tmp/z3070` (detached) and `/private/tmp/zterm-dagpipe-phase1` (`feature/dagpipe-phase1`) are NOT created by this run. Do not delete or overwrite them.

## Project hard rules (zterm/AGENTS.md)
- Before editing code read `architecture.md` + `architecture-boundary-remediation.md` + relevant decision; state functional block / unique owner / boundary-violation class (physical removal / separate-and-sink / explicit-compat-retain) / allowed-forbidden paths / required gate.
- No `sed`/`awk`/python semantic bulk replacement; per-file apply_patch.
- No `pkill`/`killall`/`kill $(...)`.
- No `--no-verify` or hook bypass.
- Merge only by master; no unauthorized merge/push/APK/OTA publish.
- Android build unique entry `pnpm --dir android run build:android` (bumps buildNumber, builds normal/rollback, writes local OTA); pure audit must not call it.
- Terminal chain: update docs/AGENTS/skill → add tests → change code.
- With no online device, state the L5 gap explicitly; do not substitute candidate APK or source tests for device acceptance.

## Execution requirement
- Follow the real project flow: worktree → implement → applicable tests/build → review → merge authorization.
- Each stage must produce verifiable evidence; do not pass off source or candidate evidence as runtime/device evidence.
