interface:
  display_name: "AppSDK Project Governance"
  short_description: "Guide and govern the full project lifecycle"
  default_prompt: "Use $appsdk-project-governance. If governance exists without Guide, run the read-only bootstrap intake, read project documents and local Skills, and present GuidanceSetupProposal for approval before compile. Otherwise initialize the task plan, execute the next legal step, verify evidence, deliver, freeze, and clean up."
